# CLAUDE.md — Proyecto USM: Prototipo UX del SAE + Memoria de título

## Qué es este proyecto

Repositorio con dos subproyectos activos y su documentación de respaldo:

| Carpeta | Contenido | Agente responsable |
|---|---|---|
| `sae-react/` | Aplicación React funcional: prototipo de mejora UX del Sistema de Admisión Escolar (SAE) del Mineduc Chile | `code-agent` |
| `proyecto-tesis/` | Memoria de título en LaTeX: "Mitigación de Sesgos en IA: Análisis del Sistema de Admisión Escolar en Chile" (UTFSM) | `writing-agent` |
| `docs/` | Investigación y planificación que fundamentan ambos | — |
| `archivo/` | Versiones superadas (prototipos HTML v1/v2, docs antiguos). No modificar | — |

El prototipo demuestra cómo aplicar transparencia algorítmica al SAE; la memoria documenta el proceso. Ambos comparten cifras y terminología: **mantenerlos sincronizados es la regla más importante del repo.**

## Documentos de referencia (leer antes de tareas grandes)

- `docs/CONTEXTO_CLAUDE_CODE.md` — contexto técnico del prototipo React (parcialmente desactualizado: `/comparador` ya está implementado).
- `docs/planificacion/plan_mejora_sae.md` — matriz de 22 secciones e incisos a–t del plan de mejora; origen de los códigos de trazabilidad `S<sección>-<inciso>` usados en comentarios del código. Estado: 100 % (87/87 puntos aplicables; S22 cerrada el 2026-08-04).
- `docs/investigacion/` — informe heurístico Fondecyt N.º 1250492 (baseline: 51 % sitio informativo, 61 % plataforma, 0 % en transparencia e inclusión), revisión de 96 papers sobre transparencia algorítmica y resumen ejecutivo.

## Reglas globales

1. **No cambiar el rumbo.** Stack, arquitectura, estructura de capítulos y estilo de citas ya están decididos. Los agentes implementan y mejoran dentro de lo definido; no proponen migraciones ni rediseños.
2. **Separación estricta:** el trabajo de código va en `sae-react/` (code-agent), el de redacción en `proyecto-tesis/` (writing-agent). Ninguno toca el territorio del otro.
3. **No inventar cifras.** Todo número citado debe provenir del informe heurístico, la revisión de literatura, el plan de mejora o el código. Si un cambio de código altera un número citado en la memoria, avisar al usuario.
4. **Trazabilidad:** cambios funcionales en el prototipo llevan comentario en español con su código `S<sección>-<inciso>` del plan.
5. `archivo/` es historia del proyecto: se conserva, no se edita.
6. **Cierre de tarea:** todo agente termina su trabajo actualizando los archivos de contexto afectados — este `CLAUDE.md` (sección "Estado del proyecto"), `docs/CONTEXTO_CLAUDE_CODE.md` y/o `docs/planificacion/plan_mejora_sae.md` — para que la siguiente sesión retome sin re-explorar el repo. Solo se registra lo efectivamente hecho y validado, con fecha; nunca avances proyectados.

## Persona y principios de diseño (vigentes para el prototipo)

- **Persona objetivo:** Daniela González, 35 años, RM/Valparaíso, acceso principal por móvil, alfabetización digital básica-intermedia, percibe el SAE como "tómbola". Necesita que le expliquen la asignación antes de postular.
- **Principios (respaldados por la literatura, ver `NotasPage.jsx`):** divulgación progresiva (Springer & Whittaker), explicabilidad contextualizada (Nefedov), controles interactivos con retroalimentación (Kim; Feddersen), gestión de expectativas, información multidimensional con gráficos (Glazerman).
- **UI:** español chileno con tuteo, lenguaje claro nivel 6° básico, mobile-first 375px, contraste WCAG AA mínimo, paleta institucional Mineduc (azul `#0057B7`, azul claro `#E8F1FB`, verde `#1A7F37`, naranja `#E07B00`, rojo `#C0392B`).

## Comandos de validación

- Prototipo (`sae-react/`): `npm run lint` y `npm run build`. No hay suite de tests; no agregar frameworks de test sin petición explícita.
- Memoria (`proyecto-tesis/`): `latexmk main.tex` (salida en `build/`); revisar `build/main.log` y `main.blg` por citas o referencias rotas.

## Estado del proyecto (agosto 2026)

- Auditoría de lenguaje antropomórfico en `/algoritmo` (2026-08-26) — pendiente de lenguaje del `AlgoSimuladorPasos` de `docs/planificacion/bitacora_flujo_postulacion_y_resultado.md` §5 (listado como **P1** tras la última renumeración; originalmente **P3**). Fundamento NN/g (`investigacion_ux_guide_ai_systems.md` §5): describir el procesamiento como aplicación de reglas sobre datos, no como una "elección" del sistema. Auditado todo el texto visible y los `aria-label` de `sae-react/src/components/AlgoSimuladorPasos.jsx` y `sae-react/src/pages/AlgoritmoPage.jsx`. **Único cambio:** el párrafo de intro de la sección "Reproducir el algoritmo paso a paso" pasó de "Mira cómo el sistema evalúa cada uno de tus colegios en orden, hasta encontrar una asignación" a "Mira cómo el sistema revisa tus colegios en el orden que tú elegiste. En cada uno aplica las prioridades que fija la ley y cuenta los cupos disponibles, hasta asignarte uno". El resto de la prosa de `AlgoritmoPage.jsx` ya usaba lenguaje de proceso ("cruza la lista con las prioridades y los cupos", "revisa la N.°2", "el sistema aplica prioridades definidas por ley") y no se tocó; tampoco lógica, animación, `asignacion.js`, `probAsignacion` ni el umbral 65. Trazabilidad `S13 (refinamiento) · auditoría NN/g`; **no se creó sección del plan, no se sumó ningún punto, 87/87 intacto** (`plan_mejora_sae.md` e `incisos.js` sin cambios), sin impacto en cifras de `proyecto-tesis/`. `npm run lint` y `npm run build` limpios (2026-08-26). Detalle en `docs/CONTEXTO_CLAUDE_CODE.md` sección 20.

- Visualización del formato de frecuencia + encuadre HAX G1 en `/postulacion` — pendientes **P1 y P2** de `docs/planificacion/bitacora_flujo_postulacion_y_resultado.md` §5 (2026-08-26). **P1:** nuevo componente compartido `sae-react/src/components/ProbabilidadVisual.jsx` (sin dependencias) que representa la cifra de `probAsignacion()` como "X de cada 100" — barra proporcional en `ColegioAnalisis` (paso 2 de `PostulacionPage.jsx`) e icon array 10×10 en la ficha de colegio (`ColegioPage.jsx`, bloque "Si postulas sin ninguna prioridad", `probAsignacion(5, demanda)`). Contenedor `role="img"` con la cifra en palabras, distinción lleno/vacío por relleno+borde+número (no solo color), `prefers-reduced-motion` respetado. **P2:** `InfoBox` "Antes de empezar: cómo se decide tu resultado" siempre visible al inicio del paso 1 — resume qué hace el algoritmo (orden de la lista + prioridades legales PIE→hermanos→15 %→funcionario→exalumno + vacantes + sorteo por colegio) y qué no (no hay puntaje/notas/mérito; poner primero lo que más quieres no perjudica), con enlace a `/algoritmo`. Trazabilidad `P1 · S22-11 (refinamiento)` y `P2 · HAX G1 — S22 (refinamiento)`; **no se creó sección del plan, no se sumó ningún punto, 87/87 intacto** (`plan_mejora_sae.md` e `incisos.js` sin cambios), sin impacto en cifras de `proyecto-tesis/` (`probAsignacion` y el umbral 65 no se tocaron). `npm run lint` y `npm run build` limpios (2026-08-26). Detalle en `docs/CONTEXTO_CLAUDE_CODE.md` sección 19.

- Auditoría de lenguaje de los avisos del flujo de postulación — pendientes **P3 y P4** de `docs/planificacion/bitacora_flujo_postulacion_y_resultado.md` §5 (2026-08-26). Solo microcopy en `sae-react/src/pages/PostulacionPage.jsx` (no se tocó lógica, tabla `probAsignacion` ni umbral 65; `SeguimientoPage.jsx` no requirió cambios). **P4 (HAX G5/G6):** se retiró "vulnerabilidad socioeconómica verificada" como rótulo repetido de la cuota SEP en `PRIORIDADES_INFO.prioritario` y en la InfoBox del orden de prioridades del paso 2, reencuadrada como derecho que determina el Estado; el aviso de lista corta (S22-14) atribuye ahora el riesgo a "más postulantes que vacantes" en vez de un fraseo tipo "apuntaste muy alto". **P3 (NN/g):** el mensaje de `ColegioAnalisis` cuando un colegio no publica vacantes para el nivel pasó de genérico a accionable (explica de qué se calcula el % y remite a la ficha del colegio). Ya cumplían y quedaron sin cambio: las tres explicaciones de probabilidad del paso 3, el aviso de lista corta en su parte accionable, el bloque "¿Y si no quedo en ninguna?" y la explicación contextualizada de `SeguimientoPage`. Trazabilidad `S22-6 / S22-11 / S22-14 (refinamiento)`; **no se creó sección del plan, no se sumó ningún punto, 87/87 intacto** (`plan_mejora_sae.md` e `incisos.js` sin cambios), sin impacto en cifras de `proyecto-tesis/`. `npm run lint` y `npm run build` limpios (2026-08-26). Detalle en `docs/CONTEXTO_CLAUDE_CODE.md` sección 18.

- Prioridad por colegio en el flujo de postulación (2026-08-26, refinamiento **S22-11 / S22-6**, resuelve el pendiente P6 de `docs/planificacion/bitacora_flujo_postulacion_y_resultado.md`): la prioridad dejó de ser un valor global aplicado a toda la lista. `prioritario` (cuota SEP 15 %) sigue siendo booleano global de perfil; `hermano`, `funcionario` y `exalumno` son ahora específicas del establecimiento y se declaran por colegio (`perfil.prioridadesPorColegio`). `src/utils/asignacion.js` incorpora `nivelPrioridadEnColegio(perfil, colegioId)` y `calcularResultado` resuelve el nivel, la probabilidad y la etiqueta **por colegio** en cada entrada de `detalles[]` (`resultado.nivel` de nivel superior queda como valor representativo = el del colegio asignado). `src/data/colegios.js` añade el campo `casoPrioridades` a los 6 colegios (mapeado por nombre desde la Tabla 1 del caso Muñoz González: Los Andes → hermano, Villa del Sol → funcionario, República de Chile → exalumno, San Martín/Simón Bolívar/Los Quillayes → sin prioridad específica). `PostulacionPage.jsx` gana un control por colegio en el paso 2 (`PrioridadColegioControl`, chips `aria-pressed`, `aria-live`), con pre-marcado desde `casoPrioridades`; el paso 3 y el comprobante muestran la prioridad de cada colegio y se corrigió el texto que afirmaba que la prioridad era única para toda la lista. `SeguimientoPage.jsx` (`generarExplicacion`) explica el resultado según la prioridad real en el colegio asignado. `AlgoritmoPage.jsx` sin cambio funcional (usa el fallback documentado de `nivelPrioridadEnColegio`). No se creó sección nueva del plan, no se sumó ningún punto y **no se tocó la cifra 87/87** ni `src/data/incisos.js`; no impacta cifras citadas en la memoria (las probabilidades y el umbral 65 de `asignacion.js` no cambiaron). `npm run lint` y `npm run build` limpios (2026-08-26). Detalle en `docs/CONTEXTO_CLAUDE_CODE.md` sección 17.

- Seguimiento de la reunión con el profesor guía del 2026-08-13 (ver `docs/planificacion/reunion_profesor_guia_2026-08-13.md`): el profesor revisó el caso de estudio y dio el visto bueno general, con tres pedidos resueltos el mismo día — detalle completo en `docs/CONTEXTO_CLAUDE_CODE.md` sección 16. (1) Guion de moderador para que Diego lo lea textualmente al presentar la prueba a participantes: `docs/investigacion/guion_moderador_prueba_usabilidad.md` y su PDF. (2) Paso de "resultado inmediato" agregado a `PostulacionPage.jsx` (paso 3, tras confirmar): botón que lleva a `/seguimiento`, reutilizando la lógica de resultado ya existente en `SeguimientoPage.jsx` — comentado como extensión fuera de la matriz del plan de mejora (no se tocó la cifra 87/87; queda pendiente que el usuario decida si formalizarla como S23 en `plan_mejora_sae.md`). (3) Cuestionario final ampliado con los ítems C5 y F5 sobre la reacción al resultado, y la lista de tareas dirigidas del caso de estudio pasó de 7 a 8 puntos (§6); actualizado también el material para participantes (`docs/investigacion/material_prueba_usabilidad_postulacion.pdf`). **Pendiente:** `npm run lint` y `npm run build` no se pudieron correr en el sandbox de esta sesión — el registro de npm devolvió `403` desde el proxy (confirmado con `curl`) y un intento de instalación dejó `node_modules` en estado parcial en la carpeta del proyecto (no afecta al repositorio, está en `.gitignore`, pero hay que correr `rm -rf node_modules && npm install` localmente antes de validar). Si además se requiere profundizar la investigación de UX-IA para el diseño del paso de resultado, queda para el chat "Estudio Guía de UX IA" — por ahora se reutilizó el patrón de explicación contextualizada ya validado en `SeguimientoPage.jsx`, sin necesidad de investigación nueva.

- Desarrollo de `docs/investigacion/caso_estudio_prueba_usabilidad_postulacion.md` (2026-08-13, v2, propuesta pendiente de revisión del profesor guía): (1) actualizado el estado del documento para reflejar que el diseño de advertencias que estaba pendiente en §7 ya se resolvió e implementó (ver entradas de `investigacion_ux_guide_ai_systems.md` y S22-14 más abajo) — §3.1 y la tarea 5 de §6 ahora describen el texto corregido y señalan como pendiente confirmar `npm run lint`/`npm run build`; (2) agregada metodología completa de la prueba (§7 nueva): N=8 participantes, criterios de reclutamiento e inclusión, mezcla deseable de postulantes SAE previos/nunca postulantes, nota de reclutamiento por conveniencia como limitación, modalidad presencial ~50–60 min con tabla de bloques de tiempo, guion de moderador; (3) agregado instrumento de medición (§8 nueva): ítem de línea base pre-tarea sobre percepción "tómbola", batería Likert 1–5 de 11 ítems (4 comprensión, 4 confianza —incluida la pregunta original sobre Colegio San Martín—, 3 percepción de justicia) y cierre abierto; (4) renumerado el antiguo §7 "Pendiente de tu parte" a §9, actualizado para reflejar qué sigue abierto: variantes de grupo, alcance de alta exigencia académica, necesidad de comité de ética UTFSM, verificación de lint/build, y el gap de datos `funcionario`/`exalumno` en `colegios.js` (§5, sin resolver, sigue siendo trabajo de `sae-react/`). No se tocó código ni la memoria; documento listo para llevar a reunión con el profesor guía.

- Aplicación de recomendaciones de `investigacion_ux_guide_ai_systems.md` (2026-08-13): implementadas las 5 recomendaciones priorizadas del documento (§8) en `PostulacionPage.jsx`, paso 3 (bloque de explicación por colegio en el resumen de confirmación). (1) Corregida la inconsistencia de mensajería: se retiró "considera ponerlo más abajo en tu lista" del texto de probabilidad baja, que contradecía el consejo de strategy-proofness del paso 2. (2) La explicación de probabilidad baja ahora separa el dato que la fundamenta (demanda, y cuando hay datos de nivel: postulantes año anterior y rango de vacantes) de una frase explícita de que reordenar el colegio no cambia esa cifra ni las chances en los demás — riesgo real vs. falso riesgo estratégico. (3) Formato de frecuencia ("de cada 100 postulantes...") extendido a probabilidad media y baja, no solo alta. (4) Categoría cualitativa "Certeza muy alta" para el caso de mayor certeza rastreado por el prototipo (hermano/a matriculado/a con prob. ≥90%); PIE y continuidad de colegio de origen no se implementaron por no existir como campos en este prototipo. (5) Verificada por trazado de código la preservación de datos al editar desde el paso 3 — no requirió cambios. Comentario de trazabilidad: `S22-14 (refinamiento)`; no se creó sección nueva del plan ni se tocó la cifra 87/87. **Pendiente:** el sandbox de esta sesión no pudo completar `npm install` (bloqueos/timeouts de red del entorno) por lo que `npm run lint` y `npm run build` no se ejecutaron — el cambio fue revisado manualmente pero falta correr ambos comandos en un entorno local antes de cerrar. Detalle en `docs/CONTEXTO_CLAUDE_CODE.md` (sección 15).

- Nueva investigación (2026-08-13): `docs/investigacion/investigacion_ux_guide_ai_systems.md` — revisión de las Microsoft Guidelines for Human-AI Interaction (18 guías, HAX Toolkit) y del capítulo "Explainability + Trust" del Google PAIR Guidebook, complementada con Nielsen Norman Group (IA explicable), Brookings (riesgos y transparencia en algoritmos de asignación K-12) y literatura de comunicación de riesgo/probabilidad para audiencias con baja numeracidad. Resuelve el pendiente de `caso_estudio_prueba_usabilidad_postulacion.md` §7 sobre el diseño de advertencias/alertas. Hallazgo nuevo: inconsistencia de mensajería detectada en `PostulacionPage.jsx` — el consejo del paso 2 ("ordena por tu preferencia real", correcto, refleja strategy-proofness de DA) contradice el texto de probabilidad baja del paso 3 ("considera ponerlo más abajo en tu lista"), que reproduce el mito de riesgo estratégico que el propio caso de estudio Muñoz González busca corregir. El documento entrega 5 recomendaciones priorizadas (corrección de esa inconsistencia, rediseño de la advertencia del "colegio en mente", formato de frecuencia extendido a los tres niveles de probabilidad, categorías cualitativas para certeza muy alta, verificación de preservación de datos al editar). Pendiente: decisión del usuario sobre cuáles recomendaciones pasar a tareas de código (`sae-react/`, code-agent).

- Mantenimiento correctivo (2026-08-05): scroll al navegar y auditoría de overflow móvil a 375px. Nuevo componente global `ScrollToTop.jsx` (montado en `App.jsx`) reemplaza el `window.scrollTo(0,0)` local de `ColegioPage.jsx`; verificado sin conflicto con el ancla de `InicioPage`, el auto-scroll del `ChatAyuda` ni el `scrollIntoView` del tour guiado. Auditoría de las 13 rutas a 375px (estático y con estados dinámicos: postulación con 6 colegios, comparador con 3 colegios, seguimiento confirmado, texto grande, chat abierto): un solo desborde real encontrado y corregido — en `PostulacionPage.jsx` (S22), `ColegioAnalisis` quedaba comprimido en la columna central de la tarjeta de colegio (~142px); se reubicó como fila propia de ancho completo. No se detectó overflow horizontal en ninguna ruta ni se necesitó `overflow-x: hidden` como red de seguridad. `npm run lint` y `npm run build` limpios. Detalle en `docs/CONTEXTO_CLAUDE_CODE.md` (sección 14).

- Prototipo: v4.5, todas las rutas implementadas (incluido `/comparador`, `/proceso` y tour guiado) y plan al 100 % (87/87 aplicables tras añadir y cerrar S22 el 2026-08-04). Páginas internas: `/cumplimiento`, `/roadmap`, `/notas`.
- Sincronización código↔memoria S22 (2026-08-04, completa): cifras del plan actualizadas a **87/87 (100 %) y 22 categorías** en cap. 03 (nueva fila agosto semana 1 en la cronología —rediseño del flujo de postulación: 6 errores de fidelidad corregidos, comprobante descargable, reordenamiento por arrastre, borrador, postulación familiar en bloque, orientación estratégica— y "Estado actual" con 87/87), cap. 04 (22 categorías/87 puntos; dos categorías añadidas en agosto) y cap. 06 (matriz de 87 puntos). Cap. 00 no citaba cifras de la matriz (sin cambios). Se conservan como historia la fila de junio (62 puntos) y la ampliación intermedia a 21 categorías/72 puntos en la cronología. PDF recompilado sin errores (`latexmk main.tex`, 0 citas indefinidas; solo warnings preexistentes de `empty year` en el .blg) — queda resuelto el pendiente de recompilación de la sincronización anterior. Nota: en shell externo bibtex requirió `BIBINPUTS` explícito hacia `bibliografia/`; desde VS Code el `.latexmkrc` lo resuelve solo.
- Sincronización código↔memoria (2026-08-04, completa): cifra de trazabilidad 72/72 (100 %) y estado actual reflejados en toda la memoria — cap. 00 (iteraciones "mayo y agosto de 2026"), cap. 03 (cronología ampliada con filas julio semana 5 —refactor vitrina v2— y agosto semana 1 —/proceso y ampliación de la matriz a 21 categorías/72 puntos—; "Estado actual" con cuatro funcionalidades adicionales y 72/72; nota de commits generalizada), cap. 04 (21 categorías, 72/72, lista incluye /proceso) y cap. 06 (matriz de 72 puntos). Referencias históricas a 62 puntos en cap. 03 (fila de junio) se conservan deliberadamente. (El pendiente de recompilación de esta entrada quedó resuelto el 2026-08-04, ver entrada S22.)
- Memoria: Capítulos 0–4 redactados; Capítulos 5 y 6 **pendientes por diseño** hasta ejecutar la validación (Sección 3.5). No completarlos sin datos de validación.
- Detalle histórico de especificaciones del prototipo HTML v1: `archivo/CLAUDE_v2.md` (solo referencia).
- Refactor vitrina colegios (2026-07-30): `colegios.js` migrado a esquema v2 (vacantes como array con rango min/max, postulantesAnterior, jornada, copago por nivel; nuevos campos rbd, director, dependencia, orientacion, categoriaDesempeno, categoriaAnio, simceAnio, gseComparacion, gseNumColegios). `ColegioPage.jsx` actualizado con identidad institucional en hero, tabla de vacantes rica, sección "Categoría de desempeño" con escala 4 pasos, comparación SIMCE GSE con badge, nota de procedencia + descargables simbólicos. `InicioPage` añade selector "Ordenar por" (SIMCE/vacantes/distancia/demanda). `ComparadorPage` y `SeguimientoPage` actualizados para schema v2. Build limpio post-refactor.
- Nueva investigación (2026-07-30): `docs/investigacion/investigacion_vitrina_sae.md` — recorrido en vivo de la vitrina oficial (Admisión 2027): flujos, inventario de información de la ficha y feedback accionable para `InicioPage`/`ColegioPage` (adoptar: postulantes año anterior, rangos de vacantes, categoría de desempeño, pago por nivel; conservar: CTA integrada, comparador, secciones simultáneas).
- Nueva investigación (2026-07-30): `docs/investigacion/investigacion_algoritmo_sae.md` — funcionamiento detallado del algoritmo DA del SAE (paper Correa et al.), argumentos de implementación, cifras 2018 y debate/reforma 2026. Fuente de respaldo para memoria y prototipo.
- Nueva investigación (2026-08-04): `docs/investigacion/investigacion_paso_a_paso_sae.md` — recorrido de las 5 etapas del menú "Paso a paso" del sitio oficial (Postulación, Asignación, Resultados, Periodo Complementario, Matrícula), calendario Admisión 2027 verificado, hallazgos UX y 7 recomendaciones de implementación.
- Nueva investigación (2026-08-04): `docs/investigacion/analisis_flujo_postulacion.md` — análisis profundo del flujo de postulación: auditoría de `PostulacionPage.jsx` (6 errores factuales detectados E1–E6, p. ej. restricción regional y tope de 8 colegios inexistentes en el SAE real), benchmark (plataforma oficial, NYC MySchools, buenas prácticas multipaso, postulación en bloque) y plan en 3 fases con prompt de implementación incluido (§6, propuesta de sección S22 en el plan de mejora). Implementado el 2026-08-04 (ver entrada S22 más abajo).
- Vista "El proceso paso a paso" (2026-08-04): nueva ruta `/proceso` con `ProcesoPage.jsx` — línea de tiempo de 5 etapas (Postulación, Asignación, Resultados, Periodo Complementario, Matrícula) con estado calculado por fecha, divulgación progresiva, 5 reglas de alto riesgo como avisos prominentes, tabla comparativa Principal vs. Complementario, checklist de matrícula, calendario Admisión 2027 como tabla HTML accesible. Enlace en Navbar y paso en tour guiado. Build limpio. (S21, 10 puntos)
- Rediseño del flujo de postulación (2026-08-04, S22, 15 puntos): `PostulacionPage.jsx` reescrita según `analisis_flujo_postulacion.md` — Fase 1 fidelidad: E1 región como filtro (no restricción), E2 sin tope de 8 colegios (recomendación de al menos 6, corregido también en `InicioPage`, `ColegioPage`, `ComparadorPage`, `AlgoritmoPage`, `CalendarioPage`, `ChatAyuda`, `TourContext`), E3 cierre 27 de agosto 14:00, E4 resultados 15–21 de octubre de 2026, E5 desempate aleatorio por colegio, E6 orden real de prioridades (PIE → hermanos → 15 % reserva → funcionario → exalumno), comprobante .txt descargable con folio/lista/fechas. Fase 2: drag-and-drop nativo + botones ↑↓ con anuncio `aria-live`, borrador visible ("Borrador guardado" + aviso de reanudación), enlaces "Editar" por sección en paso 3, ColegioAnalisis con postulantes año anterior y vacantes por nivel (esquema v2), confirmación explícita del nivel al vincular. Fase 3: opción "¿Postulas a hermanos?" (postulación en bloque simulada), consejo estratégico DA + aviso lista corta/alta demanda, bloque "¿Y si no quedo en ninguna?" con enlaces a `/proceso`. CSS S22 en `index.css`. Lint y build limpios.
- Saneamiento de lint (2026-08-04): la base tenía 15 errores preexistentes de ESLint (react-hooks v6: `set-state-in-effect`, `refs`; `no-unused-vars`; `no-undef` en `vite.config.js`; regla `react/no-danger` inexistente). Corregidos sin cambios de comportamiento: cargas de localStorage movidas a inicializadores perezosos de `useState` (`InicioPage`, `ColegioPage`, `PerfilPage`, `PostulacionPage`), supresiones justificadas en `GuidedTour`/contextos (react-refresh), limpieza de variables sin uso y `import.meta.dirname` en `vite.config.js`. `npm run lint` queda en 0 errores / 0 warnings.
- Optimización divulgación progresiva en S22 (2026-08-06): aplicada a `PostulacionPage.jsx` Paso 2 (prioridades) según recomendación de `feedback_sae_problemas.md` §14e (legibilidad, siglas sin explicación). Cambios: (1) explicaciones de prioridades trasladadas de texto siempre visible a modal HTML `<dialog>` accesible con botón "?" discreto (`PrioridadModal`), reduciendo texto en pantalla de 4 párrafos a 1 línea + chip; (2) `ColegioAnalisis` simplificado a 4 datos esenciales (demanda, vacantes, postulantes, prioridad), eliminando 3 tips redundantes consolidados en hint del picker; (3) siglas `PIE` y `SEP` actualizadas con `<abbr title="...">` accesible inline, reemplazando explicaciones textuales largas; (4) textos reducidos sin perder precisión (target legibilidad Spaulding ~95 → ~80, nivel 6°). Estilos nuevos: `.chip-help-btn` (botón "?"), `.prio-modal` (modal con animaciones), `.prio-modal__*` (estructura interna). HTML5 `<dialog>` nativo: sin dependencias nuevas. `npm run lint` y `npm run build` limpios; navegación por teclado del modal verificada (ESC cierra, clic fuera cierra).
- Optimización renderización SeguimientoPage (2026-08-06): CSS puro, mejora de jerarquía visual y legibilidad en "Mi postulación". Cambios: (1) stats bar desktop: padding 16px → 20px, números 1.15rem → 1.3rem, labels 0.72rem → 0.80rem; (2) stats bar móvil: mantiene 2×2 grid (4 → 2 columnas en <768px), font-sizes adaptadas (1.15rem números, 0.75rem labels), gap 1px → 2px; (3) preferencias: padding 16px consistente, estado/probabilidad más destacados; (4) meta info: `flex-direction: column` en móvil. Sin overflow detectado en 375px. `npm run lint` y `npm run build` limpios; jerarquía visual clara en todos los breakpoints.
- Visualización animada del algoritmo en AlgoritmoPage (2026-08-06): nueva sección "▶️ Reproducir el algoritmo" tras el simulador existente, visible solo si usuario seleccionó colegios. Componente `AlgoSimuladorPasos` muestra tabla animada (paso a paso) de cómo el SAE procesa la lista del usuario: fila por colegio, estado (✅ Asignado / ❌ Sin cupos / 🟡 Prioridad insuficiente / ⏭ No evaluado), pausa 1.5s entre pasos, anima hasta encontrar asignación. Botón "Volver a reproducir" reinicia. CSS puro sin dependencias nuevas (`@keyframes algo-fade-in`, `.algo-pasos-viz`, `.algo-pasos-tabla`, `.algo-pasos-row`). Accesible: `aria-live` anuncia cada paso a lectores de pantalla. ~300 líneas de código. `npm run lint` y `npm run build` limpios.
