# Bitácora de decisiones — flujo de postulación y paso de resultado

**Creado:** 2026-08-26
**Ámbito:** `sae-react/src/pages/PostulacionPage.jsx` (flujo `/postulacion`, 3 pasos) y el paso de resultado inmediato que enlaza a `/seguimiento` (`SeguimientoPage.jsx`).
**Mantenido por:** `bitacora-agent` (ver `.claude/agents/bitacora-agent.md`). Actualizar **cada vez** que se toca el flujo o el paso de resultado, en la misma sesión del cambio.

---

## 1. Para qué existe este archivo

La memoria de título todavía no redacta los capítulos que dependen del prototipo (4 resultados, 5 discusión, 6 conclusiones) porque falta terminar el desarrollo del flujo de postulación y ejecutar la prueba de usabilidad. Cuando llegue ese momento, cada decisión de diseño del flujo tendrá que quedar **justificada por una fuente**, no por criterio propio (regla 3 de `CLAUDE.md`: no inventar; regla de honestidad epistémica de la memoria).

Esta bitácora es el registro único de esa trazabilidad: **qué se cambió en el flujo → qué guía o estudio lo fundamenta → en qué capítulo de la memoria se escribe → si está validado (`npm run lint` / `npm run build`)**. Sirve para que, al escribir, no se pierda ningún detalle ni haya que reconstruir el "por qué" desde el código.

**Regla de honestidad de esta bitácora:** si un cambio del flujo **no** tiene fundamento en una guía o estudio, se registra igual y se marca explícitamente `⚠ SIN FUNDAMENTO DOCUMENTADO`. No se inventa una cita a posteriori. Algunos cambios de S22 nacieron de la auditoría de fidelidad (`analisis_flujo_postulacion.md`, 2026-08-04) y no de las guías de UX-IA (`investigacion_ux_guide_ai_systems.md`, 2026-08-13); en esos casos se distingue **fundamento de origen** (lo que motivó el cambio) de **mapeo a guía** (la guía de Microsoft/Google con la que el cambio es coherente y bajo la cual se puede argumentar en la memoria).

---

## 2. Marco de referencia (las "guías investigadas")

Documento base: **`docs/investigacion/investigacion_ux_guide_ai_systems.md`** (2026-08-13). Resume:

| Sigla en esta bitácora | Fuente | Uso |
|---|---|---|
| **HAX G1…G18** | Microsoft *Guidelines for Human-AI Interaction* (HAX Toolkit, CHI 2019) — 18 guías en 4 momentos | Cada guía citable por número. Las más usadas en el flujo: G1 (qué puede hacer el sistema), G2 (qué tan bien lo hace), G4 (info contextualmente relevante), G5/G6 (normas sociales / sesgos), G10 (delimitar ante duda), G11 (por qué el sistema hizo lo que hizo), G16 (comunicar consecuencias de las acciones del usuario) |
| **PAIR-ET** | Google *People + AI Guidebook*, capítulo "Explainability + Trust" | Conceptos: calibrar la confianza (no maximizarla); explicaciones parciales; explicación general del sistema vs. específica del resultado; visualización de confianza/certeza (el número presupone alfabetización probabilística); gestionar la influencia en las decisiones del usuario (confianza engañosa induce acciones no deseadas) |
| **NNG-XAI** | Nielsen Norman Group, "Explainable AI in Chat Interfaces" (dic. 2025) | Ubicación visible de avisos (no en pie ni tras ícono); lenguaje accionable y no genérico; evitar lenguaje antropomórfico al describir cómo "decide" el sistema |
| **BROOK** | Brookings — Kasman & Valant, "The opportunities and risks of K-12 student placement algorithms" (2019) | Comunicar **qué NO puede hacer** el sistema (no crea cupos, no garantiza primera preferencia); el mayor riesgo es de percepción, no técnico; videos animados para explicar el algoritmo |
| **RISK-NUM** | Comunicación de riesgo / numeracidad. **Fuentes separadas tras la revisión del 2026-09-08** (`investigacion_guias_ux_transparencia_sae.md`): **Galesic et al. 2009** (icon arrays, baja numeracidad — dominio médico); **Visschers et al. 2009** + revisión sistemática PMC11067312 (formato de frecuencia natural, efecto dependiente del contexto); **Karagappa et al. 2024 / arXiv:2408.12365** = *visualización de incertidumbre en series temporales*, complementaria — **NO** es la fuente de icon arrays ni de frecuencias, se citaba mal | Formato de frecuencia ("X de cada 100") **puede** facilitar la interpretación (no "siempre mejor que %"); icon arrays / barras proporcionales = decisión fundamentada, eficacia en el SAE = hipótesis a validar |
| **FORM-MS** | Trámites multipaso. **Fuente preferida (revisión 2026-09-08): GOV.UK Design System** (question pages / check answers / confirmation pages / validation; declara investigación con usuarios). WeWeb / Venture Harbour quedan como secundarias sin evidencia académica | Indicador de progreso; guardado/reanudación (retirado a propósito en el prototipo); volver a cualquier paso sin perder datos; validación en tiempo real. **La cifra "−20–25 % abandono" NO tiene fuente primaria — retirada; no citar en la memoria** |

Documentos previos que también fundamentan el flujo (fidelidad y algoritmo, no UX-IA):

- **`analisis_flujo_postulacion.md`** (2026-08-04) — auditoría de fidelidad, errores E1–E6, benchmark NYC MySchools, plan en 3 fases. Origen de los códigos `S22-1…S22-15`.
- **`investigacion_algoritmo_sae.md`** — mecánica del algoritmo DA, orden real de prioridades (PIE → hermanos → 15 % → funcionario → exalumno), desempate por lotería por colegio, strategy-proofness (sec. 4.2).
- **`investigacion_paso_a_paso_sae.md`** — calendario Admisión 2027, reglas oficiales de cada etapa.
- **`analisis_video_paso_a_paso_sae.md`** (2026-08-27) — inventario estructural del flujo real de postulación (pantallas, campos, información en pantalla, aceptaciones obligatorias) levantado del **video oficial del MINEDUC** ("Paso a paso de Sistema de Admisión Escolar 2025", canal @mineducchile). Su sec. 2 tiene la tabla de brechas de fidelidad **A–I** contra `PostulacionPage.jsx`, clasificadas indispensable / deseable / fuera de alcance. Fuente de fidelidad al SAE real, junto a `analisis_flujo_postulacion.md` e `investigacion_paso_a_paso_sae.md`.
- **`caso_estudio_prueba_usabilidad_postulacion.md`** — caso Muñoz González; sec. 3.1 define la distinción **riesgo real / falso riesgo estratégico / consejo complementario** que es el eje del texto de advertencia del paso 3.
- **`comparativa_flujo_postulacion_v_final.md`** (2026-09-01) — documento de **apoyo**, no fuente de fidelidad nueva por sí mismo: consolida `analisis_video_paso_a_paso_sae.md` + 14 capturas de una versión anterior de la plataforma (`postulaciones2019`) + informe heurístico en un inventario **input por input** del flujo del prototipo vs. el SAE oficial. Encuadre "capa de transparencia, no clon", decisiones de alcance conscientes para el cap. 3 (sec. 5) y **roadmap fase 2 (R1–R6)** para el cap. 6 (sec. 6). Se usó para presentar la versión casi final a la profesora guía (reunión 2026-09-04).

---

## 3. Mapa a la memoria

Dónde se escribe cada tipo de decisión del flujo cuando se redacten los capítulos:

| Capítulo | Qué del flujo va aquí |
|---|---|
| **Cap. 2 — Marco teórico** | Presentación de HAX y PAIR-ET como marcos de diseño de interacción humano-IA. **Decisión abierta:** citarlos directamente (sec. 2.x nuevo) o dejarlos solo como respaldo de diseño en el cap. 3. Reordenar/destacar de la revisión de 96 papers los que tratan formularios multipaso y puntos de decisión irreversibles. |
| **Cap. 3 — Metodología** | Justificación de cada decisión de diseño del flujo (esta bitácora es el insumo directo). Encuadre "riesgo real vs. falso riesgo estratégico". Metodología de la prueba de usabilidad (N=8, tareas dirigidas centradas en completar una postulación, guion de moderador, instrumento Likert de 11 ítems + línea base). |
| **Cap. 4 — Resultados** | Correcciones de fidelidad E1–E6 como mejoras medibles (¿el usuario entiende ahora el tope de colegios, el desempate, el orden de prioridades?). Resultados de la prueba de usabilidad, con detalle del flujo de postulación y resumen del resto. |
| **Cap. 5 — Discusión** | Hallazgos de postulación contra literatura de formularios de decisión multipaso y transparencia algorítmica. Si el texto corregido del paso 3 desactiva o no el mito del riesgo estratégico (tarea 5 del caso de estudio). |
| **Cap. 6 — Conclusiones** | Qué queda fuera de alcance (vitrina, seguimiento como línea futura). Recomendaciones para implementación institucional. |

---

## 4. Registro de cambios

Formato de cada entrada: **ID · fecha · estado de validación**, luego qué se cambió, fundamento (origen + guía), archivo/ancla, capítulo de la memoria.

Estados de validación: `✅ lint+build OK` · `⏳ pendiente lint/build` · `— sin código (doc/decisión)`.

### 4.0 Verificación de validación global

`npm run lint` y `npm run build` corridos limpios el **2026-08-26** sobre el estado actual de `PostulacionPage.jsx` (incluye S22-1…S22-15, el refinamiento S22-14 del 2026-08-13 y el paso de resultado inmediato). Queda así resuelto el pendiente que arrastraban `CLAUDE.md` y `CONTEXTO_CLAUDE_CODE.md` secs. 15–16. **Toda entrada nueva vuelve a requerir su propia verificación.**

---

### Bloque A — Correcciones de fidelidad (S22-1 … S22-7)

Origen: `analisis_flujo_postulacion.md` sec. 2 (auditoría contra reglas oficiales). Mapeo a guía: mayormente HAX G1 (qué puede/no puede hacer el sistema) y G11 (por qué). Todas coherentes con el argumento de transparencia de la memoria: una herramienta que se presenta como transparente no puede contener afirmaciones falsas sobre el sistema real.

| ID | Cambio | Corrige | Fundamento origen | Mapeo a guía | Ancla | Cap. memoria |
|---|---|---|---|---|---|---|
| **S22-1** | La región pasa de restricción ("solo colegios de tu región") a filtro de exploración, con nota de que se puede postular a otras comunas y regiones | E1 | `analisis_flujo_postulacion.md` sec. 2 · "Paso a paso para postular" (sitio oficial) | HAX G1 | `PostulacionPage.jsx` `// S22-1` (~545) | 4 (medible: ¿el usuario cree que hay restricción regional?) |
| **S22-2** | Se elimina el tope duro de 8 colegios; sin límite, recomendación visible de al menos 6 + refuerzo positivo al alcanzarlos. Propagado a `InicioPage`, `ColegioPage`, `ComparadorPage`, `AlgoritmoPage`, `CalendarioPage`, `ChatAyuda`, `TourContext` | E2 | ídem · NYC MySchools sin límite desde 2024-25 | HAX G1 · FORM-MS (menos fricción artificial) | `// S22-2` (~800, ~947) | 4 |
| **S22-3** | Cierre real del Periodo Principal 2027: **27 de agosto, 14:00** (antes "30 de agosto") | E3 | Calendario oficial 2027 (`investigacion_paso_a_paso_sae.md`) | HAX G1, G2 | `// S22-3` (~995, ~1163) | 4 |
| **S22-4** | Resultados: rango real **15–21 de octubre de 2026** (antes "5 días hábiles para aceptar/rechazar", que no existe) | E4 | ídem | HAX G1 | `// S22-4` (~1113) | 4 |
| **S22-5** | Nota del sorteo reescrita: desempate aleatorio **por colegio** (lotería independiente en cada establecimiento), sin "certificado por MINEDUC" | E5 | `investigacion_algoritmo_sae.md` sec. 3.2 | HAX G11 · NNG-XAI (no antropomorfizar, describir la mecánica) | `// S22-5` (~788) | 3, 4 |
| **S22-6** | Prioridades presentadas según el procesamiento real: **PIE → hermanos → 15 % prioritarios → funcionario → exalumno**, aclarando que el 15 % es reserva de asientos, no un ranking 1–4 | E6 | `investigacion_algoritmo_sae.md` sec. 3.2 | HAX G4, G11 | `// S22-6` (~35, ~742) | 3, 4 |
| **S22-7** | Comprobante `.txt` descargable (folio, lista ordenada, fechas siguientes) + aviso "tu postulación es válida cuando descargas el comprobante" | (sitio oficial exige descarga) | `analisis_flujo_postulacion.md` sec. 2 | FORM-MS · HAX G16 | `// S22-7` (~332, ~1141) | 4 |

**Para la memoria:** E1–E6 son el material más concreto del cap. 4 — son afirmaciones falsas del prototipo previo, corregidas y **medibles** en la prueba (el instrumento Likert C1–C4 y la observación de la tarea 5 tocan directamente el tope de colegios, el desempate y el orden de prioridades).

---

### Bloque B — Flujo y accesibilidad (S22-8 … S22-13)

| ID | Cambio | Fundamento origen | Mapeo a guía | Ancla | Cap. memoria |
|---|---|---|---|---|---|
| **S22-8** | Reordenamiento de la lista por arrastrar y soltar (HTML5 nativo, sin librerías) + botones ↑↓ como alternativa + anuncio `aria-live` del nuevo orden | NYC MySchools (drag-and-drop) · WCAG AA · accesibilidad de formularios multipaso (anunciar cambios, mantener foco) | FORM-MS · HAX G3/G4 | `// S22-8` (~263, ~285, ~390, ~885) | 3 (decisión de diseño), 4 (usabilidad del reordenamiento en móvil) |
| **S22-9** | Guardado visible del borrador: indicador "Borrador guardado" en cada acción + aviso de reanudación al volver con borrador | FORM-MS (guardado/reanudación explícitos, −20–25 % abandono) · NNG-XAI (visibilidad) | FORM-MS | `// S22-9` (~231, ~261, ~267, ~871) | 3 |
| **S22-10** | Paso 3 con enlaces "Editar" por sección (identificación / prioridades / lista) que vuelven al paso sin perder estado | FORM-MS (volver a cualquier paso sin perder datos — fallo común) · benchmark sec. 3.3 | FORM-MS · PAIR-ET (explicaciones parciales: revisar sin rehacer) | `// S22-10` (~1000, ~1021, ~1043) | 3 |
| **S22-11** | `ColegioAnalisis`: se muestran **postulantes del año anterior y vacantes por nivel** (esquema v2 de `colegios.js`) como fundamento del % estimado, en vez de una probabilidad "mágica" | NYC MySchools (postulantes por cupo en el punto de decisión) · `analisis_flujo_postulacion.md` sec. 4 | **HAX G2** (de qué está hecha la estimación) · **PAIR-ET** (explicación específica del resultado) · RISK-NUM | `// S22-11` (~159, ~176) | 3, 4, 5 (dato crudo + % es el diferencial frente al sitio oficial) |
| **S22-12** | Confirmación explícita del nivel/curso antes de vincular al estudiante ("Verifica el curso: es el error más frecuente") | Prensa + página oficial de Postulación (el error de curso es la advertencia principal) · FORM-MS (validación en tiempo real) | FORM-MS · HAX G16 | `// S22-12` (~259, ~661) | 3, 4 |
| **S22-13** | Opción "¿Postulas a hermanos?" → postulación familiar en bloque simulada, con explicación del reordenamiento automático de preferencias del menor | Regla oficial "Postulación familiar en bloque" (ausente en el prototipo previo) · persona Daniela (>1 hijo) | HAX G1 · BROOK (gestión de expectativas) | `// S22-13` (~260, ~630) | 3, 4 |

---

### Bloque C — Diferenciales de transparencia (S22-14, S22-15)

| ID | Cambio | Fundamento origen | Mapeo a guía | Ancla | Cap. memoria |
|---|---|---|---|---|---|
| **S22-14** (versión original, 2026-08-04) | Paso 2: consejo "ordena por tu preferencia real — poner primero uno 'más fácil' no mejora tus opciones". Aviso si la lista tiene <6 opciones y todas de alta demanda | strategy-proofness del DA (`investigacion_algoritmo_sae.md` sec. 4.2) · `caso_estudio…` sec. 3.1 | **HAX G16** · **PAIR-ET** (gestionar la influencia en decisiones) · BROOK | `// S22-14` (~381, ~959, ~964) | 3, 5 |
| **S22-15** | Paso 3: bloque "¿Y si no quedo en ninguna?" con los dos casos (con/sin colegio de origen) + enlaces a `/proceso` | `analisis_flujo_postulacion.md` sec. 4 · BROOK (comunicar qué NO puede hacer el sistema, dirigir la frustración a la escasez de cupos y no al algoritmo) | **BROOK** · HAX G1 · gestión de expectativas | `// S22-15` (~1118) | 3, 5, 6 |

---

### Bloque D — Refinamiento S22-14 por las guías de UX-IA (2026-08-13)

Origen directo: **`investigacion_ux_guide_ai_systems.md` sec. 4 y sec. 8** (este bloque es el que nace de las guías, no de la auditoría de fidelidad). Comentario de trazabilidad en código: `S22-14 (refinamiento)`. No creó sección nueva del plan; no tocó la cifra 87/87.
Ancla: `PostulacionPage.jsx` `/* S22-14 (refinamiento… */` (~1060) y el bloque `resultado.detalles.map` que le sigue (~1084–1108).
Estado de validación: **✅ lint+build OK (2026-08-26)** (el 2026-08-13 quedó `⏳`; se resolvió en la verificación global 4.0).

| # | Cambio | Fundamento | Cap. memoria |
|---|---|---|---|
| **D1** | Se retira "considera ponerlo más abajo en tu lista" del texto de probabilidad baja. Era una contradicción interna real: el paso 2 dice "ordena por tu preferencia real" (correcto, strategy-proofness) y el paso 3 sugería lo contrario, reforzando el mito del riesgo estratégico que el caso Muñoz González busca detectar | `investigacion_ux_guide_ai_systems.md` sec. 4 (hallazgo) · **HAX G16** (comunicar consecuencias reales, no sugerir una acción basada en un mito) · **PAIR-ET** (confianza engañosa induce acción no deseada) | **3** (es el texto que la prueba evalúa) y **5** |
| **D2** | El texto de probabilidad baja separa el **dato que fundamenta el %** (demanda; y cuando hay datos de nivel: postulantes año anterior y rango de vacantes) de una **frase explícita e independiente**: "Cambiar el orden de este colegio en tu lista no cambia esta cifra ni tus chances en los demás — el sistema siempre evalúa según tu preferencia real." Riesgo real vs. falso riesgo estratégico, sin que la familia tenga que inferir la propiedad | **PAIR-ET** (explicación específica por resultado; evitar confianza engañosa) · **HAX G11** · `caso_estudio…` sec. 3.1 | **3**, **5** |
| **D3** | Formato de frecuencia ("de cada 100 postulantes con tu misma condición, aproximadamente X quedan asignados") extendido a los **tres** niveles de probabilidad (antes solo el alto lo tenía) | **RISK-NUM** (formato de frecuencia > porcentaje puro para baja numeracidad) · **PAIR-ET** (el número presupone alfabetización probabilística) | **3**, **4** |
| **D4** | Categoría cualitativa "Muy alta" / "🟢 Certeza muy alta… prácticamente asegurada" cuando la prioridad es hermano/a matriculado/a (nivel 1) y la probabilidad calculada es ≥90 %, en vez de solo el porcentaje puntual. PIE y continuidad de colegio de origen **no** se modelan como checkbox en este prototipo, así que no se les aplica esta categoría (documentado en el código) | **PAIR-ET** (mostrar "92 %" donde el usuario espera "asegurado" sugiere riesgo real donde no lo hay) | **3**, **5** |
| **D5** | Verificación (sin cambio de código) de que editar desde el paso 3 no descarta selecciones del paso 2. Confirmado por trazado: los botones "Editar" solo llaman `setPaso(1)`/`setPaso(2)`; ningún `useState` de datos se reinicia al cambiar de paso | **FORM-MS** sec. 7 (preservar datos hacia adelante al retroceder — fallo común) | 3 |

---

### Bloque E — Paso de resultado inmediato (2026-08-13)

Origen: reunión con el profesor guía del 2026-08-13 (`reunion_profesor_guia_2026-08-13.md`) — pedido de poder observar la reacción de la familia al resultado dentro de la misma sesión de prueba. En el sistema real el resultado tarda hasta octubre.
Ancla: `PostulacionPage.jsx`, bloque `confirmado` del paso 3 — `InfoBox` "Solo para esta prueba: mira tu resultado ahora" con `<Link to="/seguimiento">` (~1157–1188 en el diff del 2026-08-13).
Estado de validación: **✅ lint+build OK (2026-08-26)**.

| # | Aspecto | Detalle | Fundamento | Cap. memoria |
|---|---|---|---|---|
| **E1** | Enlace al resultado tras el comprobante | No se duplicó lógica: `SeguimientoPage.jsx` ya calcula y muestra el resultado completo (hero de asignación, explicación contextualizada, detalle por preferencia, aceptar/rechazar) desde el mismo `STORAGE_KEY` que `PostulacionPage` escribe al confirmar. Solo faltaba el enlace | **PAIR-ET** (explicación general del sistema vs. **específica del resultado** — la que más ayuda en el punto de decisión) · necesidad metodológica del caso de estudio (tarea 7) | 3, 4 |
| **E2** | Nota explícita de que es solo para la prueba | El `InfoBox` dice que en el proceso real el resultado tarda hasta octubre y que aquí se adelanta | **HAX G1** (qué puede/no puede hacer el sistema) · **BROOK** (gestión de expectativas) | 3 |
| **E3** | Explicación contextualizada "por qué te asignaron este colegio" (reutilizada de `SeguimientoPage.jsx`) | Es lo que el ítem Likert **C5** ("Entendí por qué el resultado que vi fue ese, y no otro") y **F5** miden en la tarea 7 | **PAIR-ET** (explicación específica del resultado) · **BROOK** (explicación accesible del algoritmo) | 3, 4, 5 |
| **E4** | Marcado como "extensión fuera de la matriz del plan de mejora (S1–S22)" | No se inventó un código `S<sección>-<inciso>`; **no se tocó la cifra 87/87** de la memoria. Decisión pendiente del usuario: formalizarlo como S23 en `plan_mejora_sae.md` o dejarlo como funcionalidad exclusiva de la prueba | regla 3 y 6 de `CLAUDE.md` | 3 (nota de alcance), 6 |

---

### Bloque F — Mantenimiento correctivo (no son cambios de diseño)

| ID | Fecha | Cambio | Naturaleza | Validación |
|---|---|---|---|---|
| **F-2026-08-05** | 2026-08-05 | `ColegioAnalisis` se saca de la columna central de la tarjeta (se comprimía a ~142 px en 375 px) y pasa a fila propia de ancho completo. Parte de la auditoría de overflow de las 13 rutas | Corrección de layout móvil (mobile-first 375 px), **sin fundamento en guía** — es cumplimiento del principio UI vigente del proyecto | ✅ lint+build OK (2026-08-05) |
| **F-2026-08-06** | 2026-08-06 | Paso 2: explicaciones de prioridades movidas de texto siempre visible a modal `<dialog>` accesible (`PrioridadModal`, botón "?"); `ColegioAnalisis` reducido a 4 datos; siglas PIE/SEP con `<abbr>` | Divulgación progresiva (principio vigente del proyecto, `NotasPage.jsx`; `feedback_sae_problemas.md` sec. 14e) · coherente con **PAIR-ET** (explicaciones parciales) | ✅ lint+build OK (2026-08-06) |
| **F-2026-09-03** | 2026-09-03 | Los 3 pasos del flujo comparten la ruta `/postulacion`, así que `ScrollToTop.jsx` (global, se dispara por cambio de pathname) no aplicaba: al avanzar/retroceder de paso, vincular al estudiante o confirmar, la vista quedaba a mitad de página / al final. Nuevo `useEffect` en `PostulacionPage.jsx` que hace `window.scrollTo(0, 0)` cuando cambian `paso`, `alumnoOk` o `confirmado` | Corrección de UX de navegación (buena práctica de formularios multipaso — `investigacion_ux_guide_ai_systems.md` sec. 7 nombra "volver a cualquier paso sin perder datos"; restaurar el scroll al tope es del mismo espíritu). Solo `PostulacionPage.jsx`, sin cambios de lógica ni de `asignacion.js` | ✅ lint+build+test OK (2026-09-03); verificado en navegador: scroll vuelve a 0 al pasar 2→3, al Confirmar y al ← Atrás |

---

### Bloque G — Modelo de prioridad por colegio (refinamiento S22-11 · S22-6, 2026-08-26)

**Fundamento de origen:** fidelidad al algoritmo. `investigacion_algoritmo_sae.md` sec. 3.2 define la prioridad de hermanos como "postulantes con un hermano o hermana ya matriculado o admitido **en el establecimiento**" y la de funcionario como "hijos de trabajadores **del establecimiento**", y exige (sec. 3.2, párrafo de requisitos legales) que los desempates se sorteen "de forma independiente en cada colegio" — son prioridades específicas de establecimiento, no atributos globales del postulante. La única transversal es la cuota de estudiante prioritario (15 %). Verificado también contra sec. 4.2 (strategy-proofness / preferencias verdaderas). Segundo motivo: `caso_estudio_prueba_usabilidad_postulacion.md` sec. 3.1 y Tabla 1 — Colegio San Martín es el colegio-control "sin ningún vínculo (ni hermano, ni funcionario, ni exalumno, ni PIE)" y pierde su función de prueba si el prototipo, con el modelo global, le contagia la prioridad de hermano que la familia tiene en otro colegio.

Implementa el pendiente **P6** de la sec. 5 (gap de datos `funcionario`/`exalumno` en `colegios.js`). No creó sección del plan ni sumó punto: **87/87 intacto** (`plan_mejora_sae.md` e `incisos.js` sin cambios, confirmado). Comentarios de trazabilidad en código: `S22-11 (refinamiento)` y `S22-6`.
**Estado de validación:** ✅ lint+build OK (2026-08-26) — `npm run lint` y `npm run build` corridos en esta sesión sobre el estado con el cambio aplicado, ambos limpios (0 errores; `PostulacionPage` compila a 39.81 kB).
**Impacto en cifras de la memoria:** ninguno.

Qué cambió, en una frase: el modelo de prioridad pasa de **global** (`nivelPrioridad(perfil)`, único para toda la lista) a **por colegio** — `prioritario` sigue transversal; `hermano` (nivel 1), `funcionario` (nivel 3) y `exalumno` (nivel 4) se resuelven contra el colegio concreto donde la familia declara el vínculo. La tabla `probAsignacion` y el umbral `>= 65` no se tocaron; solo cambia a qué colegio se asocia cada nivel.

Verificación funcional (Node, code-agent): en el caso Muñoz González, Colegio San Martín pasa de nivel 1 (96 %) a nivel 2 ("Estudiante prioritario (15 %)", 90 %) — el bug del colegio-control queda corregido. Colegio Los Andes mantiene nivel 1 (hermano, 92 %).

| # | Cambio | Ancla | Mapeo a guía | Cap. memoria |
|---|---|---|---|---|
| **G1** | `nivelPrioridadEnColegio(perfil, colegioId)`: resuelve el mejor nivel entre `prioritario` transversal y las prioridades específicas declaradas para ese colegio. `calcularResultado`: cada entrada de `detalles[]` lleva su `nivel`/`prioridadLabel` por colegio; `resultado.nivel` de nivel superior pasa a ser valor representativo (el del colegio asignado). `nivelPrioridad` global se conserva como fallback. Exporta `PRIORIDADES_POR_COLEGIO` | `asignacion.js` `/* S22-11 (refinamiento) · S22-6 */` (~11, ~35) | **HAX G11** (la razón del número mostrado es ahora real y específica del colegio, no genérica — `investigacion_ux_guide_ai_systems.md` L39) · **PAIR-ET** (explicación específica del resultado vs. general del sistema — doc L53) | 3, 4 |
| **G2** | `colegios.js`: campo nuevo `casoPrioridades` en los 6 colegios del caso Muñoz González, mapeado por nombre desde la Tabla 1: Los Andes `['hermano']`, Villa del Sol `['funcionario']`, República de Chile `['exalumno']`, San Martín / Simón Bolívar / Los Quillayes `[]`. El array antiguo `prioritarios` (solo alimenta `ColegioPage`, nunca cableado al algoritmo) queda intacto | `colegios.js` `// S22-11 (refinamiento) · S22-6` (~4) | fidelidad (`caso_estudio…` Tabla 1) · **HAX G11** | 3, 4 |
| **G3** | `PostulacionPage.jsx`: estado `prioridadesPorColegio` + `perfilCompleto` (useMemo). Componente `PrioridadColegioControl` (chips `aria-pressed`, fila full-width, cambios anunciados por región `aria-live` `anuncioPrioridad`) por cada colegio de la lista en el paso 2. Pre-marcado desde `casoPrioridades` al agregar un colegio, solo entre las condiciones que la familia marcó; el usuario puede sobreescribir. `ColegioAnalisis` y el resumen del paso 3 muestran el % y la prioridad por colegio; el comprobante `.txt` lista la prioridad por colegio | `PostulacionPage.jsx` `// S22-11 (refinamiento)` (~170, ~287, ~316, ~400, ~1078) | **HAX G4** (info contextual por colegio en el punto de decisión) · **FORM-MS** (estado/validación visibles) · **RISK-NUM** (el % por colegio se apoya en el dato crudo del esquema v2) | 3, 4 |
| **G4** | Se reemplaza el texto ahora falso ("Tu prioridad se aplica a **todos** los colegios… No puedes tener distinta prioridad por colegio") por la explicación transversal (15 %) vs. específica (hermano/funcionario/exalumno). InfoBox nueva "¿Dónde tienes esa prioridad?" en el paso 2 | `PostulacionPage.jsx` `// S22-11 (refinamiento)` (~922, ~1180, ~1195) | **HAX G1** (qué hace / qué no hace el sistema) · **HAX G11** | 3, 4 |
| **G5** | La categoría cualitativa "certeza muy alta" de D4 (`d.nivel === 1 && d.prob >= 90`) ahora usa el nivel **por colegio**: solo aparece donde la prioridad de hermano/a aplica de verdad, no en toda la lista | `PostulacionPage.jsx` bloque `resultado.detalles.map` (~1215) | **PAIR-ET** (no mostrar "asegurado" donde sí hay riesgo real) — continuidad de D4 sobre el nuevo modelo | 3, 5 |
| **G6** | `SeguimientoPage.jsx`: `generarExplicacion` conmuta por `asignado.nivel` (nivel real en el colegio asignado) en vez de los booleanos globales del perfil; hero stat, "Prioridad aplicada" y detalle por preferencia coherentes | `SeguimientoPage.jsx` `S22-11 (refinamiento)` (~20, ~355) | **HAX G11** · **PAIR-ET** (explicación específica del resultado) | 3, 4, 5 |
| **G7** | `AlgoritmoPage.jsx`: sin cambio funcional (usa el fallback global, documentado en el código); `explicacionSim` conmuta por `asignado.nivel` para coherencia de redacción | `AlgoritmoPage.jsx` `S22-11 (refinamiento)` (~227, ~256) | — (mantenimiento de coherencia, sin fundamento en guía) | — |
| **G8** | `index.css`: `.post-prio-colegio` — fila full-width, paleta naranja Mineduc, `margin-left: 0` en < 480 px (mismo patrón anti-compresión a 375 px que `ColegioAnalisis`, cf. F-2026-08-05) | `index.css` `/* S22-11 (refinamiento) */` (~5840) | — (cumplimiento del principio UI mobile-first del proyecto, **sin fundamento en guía**) | — |

**Para la memoria (observación, no editar):** `caso_estudio_prueba_usabilidad_postulacion.md` sec. 5 y sec. 9 describen este gap de datos como pendiente abierto. Con este cambio queda **resuelto**, pero ese documento no se editó — actualizarlo es trabajo del `writing-agent` / decisión del usuario.

---

### Bloque H — Auditoría de microcopy de avisos (P3 · P4, 2026-08-26)

**Fundamento de origen:** los pendientes **P3** y **P4** de la sec. 5 de esta bitácora. P4 se liga además al tema central de la memoria: la mitigación de sesgos no solo en el algoritmo, sino en cómo el sistema comunica. Auditoría de redacción de todos los avisos del flujo — **sin cambios de lógica, tabla de probabilidades ni umbral**. Commit `b18a7a7`.

**Mapeo a guía:**
- **P4 → HAX G5** (respeta las normas sociales relevantes) y **G6** (mitiga sesgos sociales). Verificado contra `investigacion_ux_guide_ai_systems.md` L35: la guía pide explícitamente "evitar que el aviso sobre un colegio de alta demanda suene a juicio sobre la familia (p. ej., insinuar que 'apuntó muy alto')" y "revisar que ningún dato mostrado refuerce estigmas socioeconómicos entre colegios de distinta categoría".
- **P3 → NNG-XAI.** Verificado contra `investigacion_ux_guide_ai_systems.md` L76: "Lenguaje claro y accionable, no genérico: un disclaimer vago ('esto es una estimación') es menos útil que uno que dice qué hacer"; el propio documento nombra `ColegioAnalisis` como el lugar a auditar.

Comentarios de trazabilidad en código: `S22-6 (refinamiento)`, `S22-11 (refinamiento)`, `S22-14 (refinamiento)`. No creó sección del plan ni sumó punto: **87/87 intacto** (`plan_mejora_sae.md` e `incisos.js` sin cambios).
**Estado de validación:** ✅ lint+build OK (2026-08-26) — `npm run lint` y `npm run build` limpios (code-agent, reconfirmados en la sesión principal).
**Impacto en cifras de la memoria:** ninguno (solo redacción de avisos).

| # | Cambio | Ancla | Mapeo a guía | Cap. memoria |
|---|---|---|---|---|
| **P4-1** | `PRIORIDADES_INFO.prioritario.que_es`: deja de usar "vulnerabilidad socioeconómica verificada por el MINEDUC" como rótulo de la persona. Nuevo texto: la cuota SEP es un derecho que "el Estado lo determina según la situación socioeconómica de tu familia, con información que el MINEDUC ya tiene. No es algo que decidas tú ni el colegio" | `PostulacionPage.jsx` `S22-6 (refinamiento) · auditoría P4` (~41, ~59) | **HAX G5 · G6** (doc L35) | 3, 5 |
| **P4-2** | InfoBox "¿En qué orden se revisan las prioridades?" (paso 2): el 15 % pasa de "estudiantes con vulnerabilidad socioeconómica verificada" a "estudiantes prioritarios/as, definidos por la situación socioeconómica que el Estado ya tiene registrada" | `PostulacionPage.jsx` `S22-6 (refinamiento) · P4` (~885) | **HAX G6** (doc L35) | 3, 5 |
| **P4-3** | Aviso de lista corta (S22-14): título "Tu lista es corta y toda de alta demanda" (antes "Lista corta y toda de alta demanda"); el cuerpo reencuadra el riesgo en "en todos hay más postulantes que vacantes" y "si en ninguno queda cupo para tu prioridad", en vez de atribuirlo a la elección de la familia ("así podrías quedar sin asignación") | `PostulacionPage.jsx` `S22-14 (refinamiento)` (~1124) | **HAX G6** (doc L35) · continuidad de S22-14 / D1–D2 | 3, 5 |
| **P3-1** | `ColegioAnalisis`, mensaje cuando el colegio no publica vacantes para el nivel: antes solo informaba la carencia ("Este colegio no publica vacantes para {nivel}"); ahora añade de qué se calcula el % ("solo con su demanda general") y una acción ("Abre la ficha del colegio para confirmar que ofrece ese nivel antes de dejarlo en tu lista") | `PostulacionPage.jsx` `S22-11 (refinamiento) · auditoría P3` (~247) | **NNG-XAI** (doc L76) | 3 |
| **P3-2** | Aviso de lista corta (mismo bloque que P4-3): la parte accionable se afina — cierra con "agrega más colegios — al menos 6 — e incluye alguno de demanda media o baja", sin sugerir reordenar por probabilidad (strategy-proofness intacto) | `PostulacionPage.jsx` `S22-14 (refinamiento)` (~1124) | **NNG-XAI** (doc L76) · strategy-proofness (`investigacion_algoritmo_sae.md` sec. 4.2) | 3 |

**Avisos auditados que ya cumplían ambos criterios (sin cambio, registrado para trazabilidad):** para P4 — `ColegioAnalisis` (no muestra SIMCE/GSE/categoría ni contrapone colegios), las 3 explicaciones de probabilidad del paso 3 (atribuyen la baja probabilidad a la demanda del colegio, no a la familia — ya refinadas en D2), el consejo "ordena por tu preferencia real", el bloque "¿Y si no quedo en ninguna?" (S22-15), `SeguimientoPage.jsx generarExplicacion`, las entradas hermano/funcionario/exalumno de `PRIORIDADES_INFO`, `colegios.js` (`casoPrioridades` son tokens, `proyecto` es descripción institucional no comparativa). Para P3 — las explicaciones de probabilidad media/baja/alta y "certeza muy alta", y los InfoBox "al menos 6", "Verifica el curso" (S22-12) y "descarga tu comprobante" (S22-7) ya son accionables o son refuerzo, no advertencia; los InfoBox neutros/tutoriales (orden de prioridades, cuándo sabrás el resultado, transversal vs. específica) son informativos — el criterio de acción no aplica.

**Para la memoria (observación, no editar):** P4 es material directo del cap. 5 — la mitigación de sesgos en la capa de comunicación del sistema, no solo en el algoritmo, es una dimensión del tema central de la tesis.

---

### Bloque I — Comunicación visual de probabilidad y encuadre del algoritmo (P1 · P2, 2026-08-26)

**Fundamento de origen:** los pendientes **P1** y **P2** de la sec. 5 de esta bitácora (ambos nacen de las guías de UX-IA, no de la auditoría de fidelidad). Commit `b9ba358`. Comentarios de trazabilidad en código: `P1 · S22-11 (refinamiento)` y `P2 · HAX G1 — S22 (refinamiento)` (con nota a S22-6 y S13). **Sin cambios de lógica:** `probAsignacion()` y el umbral `>= 65` no se tocaron; P1 muestra la **misma cifra** de otra forma. No creó sección del plan ni sumó punto: **87/87 intacto** (`plan_mejora_sae.md` e `incisos.js` sin cambios).
**Estado de validación:** ✅ lint+build OK (2026-08-26) — `npm run lint` (0 errores / 0 warnings) y `npm run build` (limpio; `ProbabilidadVisual` compila a chunk propio de 0.84 kB, `PostulacionPage` 41.58 kB) corridos en esta sesión sobre el estado con el cambio aplicado.
**Impacto en cifras de la memoria:** ninguno.

**Componente nuevo reutilizable:** `sae-react/src/components/ProbabilidadVisual.jsx` (sin dependencias, solo CSS/DOM). Recibe `prob` (cifra de `probAsignacion()`, no inventa), `sentencia` (texto en palabras para el `aria-label`) y `variante`. Hoy se usa en el paso 2 del flujo y en la ficha de colegio; queda disponible por si se reutiliza en el paso 3 (resumen de confirmación) o en `/algoritmo` más adelante.

| # | Cambio | Ancla | Mapeo a guía | Cap. memoria |
|---|---|---|---|---|
| **P1-1** | `ProbabilidadVisual.jsx`: dibuja "X de cada 100" a partir de la cifra de `probAsignacion()` en dos variantes — `barra` (barra proporcional segmentada, compacta) y `grid` (icon array 10×10, 100 celdas, N rellenas). Accesible: contenedor `role="img"` con `aria-label` = la cifra en palabras; dibujo `aria-hidden`; distinción lleno/vacío por relleno + borde + número (no solo color); texto visible "{N} de cada 100 postulantes"; `@media (prefers-reduced-motion: reduce)` desactiva la transición; paleta Mineduc por categoría (alta/media/baja) | `ProbabilidadVisual.jsx` `// P1 · S22-11 (refinamiento)` · `index.css` `.probviz*` / `.probviz-block*` | **RISK-NUM** (`investigacion_ux_guide_ai_systems.md` §6: "el formato de frecuencia… es más comprensible que la probabilidad porcentual pura para personas con numeracidad baja"; "los arreglos icónicos (icon arrays) son la técnica más recomendada… directamente trasladables a mostrar cupos y postulantes"; §8 rec. 3) · **PAIR-ET** (§3: los indicadores numéricos de confianza "presuponen que el usuario tiene una base de comprensión de probabilidad", recomienda probar "visualizaciones gráficas") | 3 (decisión de diseño), 4 (comprensión medible — ítems C2/J2) |
| **P1-2** | `PostulacionPage.jsx`, `ColegioAnalisis` (paso 2): variante `barra` bajo el nombre del colegio, por el ancho reducido dentro de `.post-item` a 375 px. **Acompaña** —no reemplaza— al chip "{prob}% estimado" y a la categoría cualitativa (alta/media/baja, "certeza muy alta") | `PostulacionPage.jsx` `// P1 · S22-11 (refinamiento)` (~231) | ídem P1-1 · continuidad de S22-11 (dato crudo + % en el punto de decisión) y D3 (formato de frecuencia en los tres niveles) | 3, 4 |
| **P1-3** | `ColegioPage.jsx` (ficha): bloque nuevo `.probviz-block` "Si postulas sin ninguna prioridad" con la variante `grid`, usando `probAsignacion(5, colegio.demanda)` — la ficha no conoce el perfil del usuario, muestra el caso base nivel 5 y remite al simulador de `/algoritmo` para el caso propio | `ColegioPage.jsx` `// P1 · S22-11 (refinamiento)` (~171, ~359) | ídem P1-1 (§6 y §8 rec. 3 nombran explícitamente "la ficha de colegio" como lugar a reforzar) | 3, 4 |
| **P2-1** | `PostulacionPage.jsx`, paso 1 ("Identifícate"), primer elemento del `CardContent`, **antes** del bloque de ClaveÚnica: `InfoBox` **siempre visible** (no depende del modo tutorial) "Antes de empezar: cómo se decide tu resultado". Encuadra que la asignación se arma con el orden de la lista + prioridades legales (PIE → hermanos → 15 % SEP → funcionario → exalumno) + vacantes, con sorteo aleatorio por colegio; que **no hay puntaje ni notas ni ranking por mérito** (salvo alta exigencia académica, fuera de este prototipo); y que poner primero el colegio preferido **nunca perjudica**. Enlace "Ver cómo funciona en detalle" a `/algoritmo`; no repite el contenido extenso de `/algoritmo` ni `/proceso` | `PostulacionPage.jsx` `// P2 · HAX G1 — S22 (refinamiento)` (~600) | **HAX G1** (`investigacion_ux_guide_ai_systems.md` §2 "Al inicio": "conviene revisar que la primera pantalla de `/postulacion` explicite, antes de pedir datos, qué hace el algoritmo y qué no decide la familia directamente — orden de prioridad legal, no 'puntaje'") · relacionado con S22-6 (orden legal) y S13 (transparencia) | 3 |

---

### Bloque J — Lenguaje no antropomórfico en `AlgoSimuladorPasos` (2026-08-26)

**Fundamento de origen:** el pendiente que figuraba en la sec. 5 de esta bitácora sobre el lenguaje del `AlgoSimuladorPasos` (listado como **P1** tras la última renumeración; originalmente **P3**). Ámbito `/algoritmo`, no el flujo de postulación, pero la sec. 5 ya lo listaba como pendiente derivado de las guías ("en `/algoritmo`, no en el flujo, pero relacionado"). Commit `04d2f2b`. Comentario de trazabilidad en código: `S13 (refinamiento) · auditoría NN/g`.

**Mapeo a guía:** **NNG-XAI** — verificado contra `investigacion_ux_guide_ai_systems.md` sec. 5, L77: "Evitar lenguaje antropomórfico al describir cómo 'decide' el sistema — relevante para el texto del `AlgoSimuladorPasos`, que debe describir el procesamiento en términos de reglas y datos, no de 'elección' del sistema." El documento nombra `AlgoSimuladorPasos` explícitamente.

Auditoría de todo el texto visible y los `aria-label` de `AlgoSimuladorPasos.jsx` y `AlgoritmoPage.jsx` (prosa de los 4 pasos, `ESCENARIOS`, `CONTEXTO_MUNDIAL`, `tutoriales`, `explicacionSim`, render de `resultado`, FAQ). **Sin cambios de lógica, animación, `asignacion.js`, tabla `probAsignacion` ni umbral 65.** No creó sección del plan ni sumó punto: **87/87 intacto** (`plan_mejora_sae.md` e `incisos.js` sin cambios).
**Estado de validación:** ✅ lint+build OK (2026-08-26) — `npm run lint` (0 errores / 0 warnings) y `npm run build` limpios (code-agent, reconfirmados en la sesión principal). `AlgoritmoPage` compila a 182.64 kB.
**Impacto en cifras de la memoria:** ninguno.

| # | Cambio | Ancla | Mapeo a guía | Cap. memoria |
|---|---|---|---|---|
| **J1** | `AlgoSimuladorPasos.jsx`, párrafo de intro de la sección "Reproducir el algoritmo paso a paso". Antes: "Mira cómo el sistema **evalúa** cada uno de tus colegios en orden, hasta encontrar una asignación". Después: "Mira cómo el sistema revisa tus colegios en el orden que **tú elegiste**. En cada uno aplica las prioridades que fija la ley y cuenta los cupos disponibles, hasta asignarte uno". El texto previo no nombraba reglas ni datos; el nuevo encuadra el proceso como aplicación de prioridades legales + conteo de cupos y refuerza que el orden lo define la familia | `AlgoSimuladorPasos.jsx` `// S13 (refinamiento) · auditoría NN/g` (~83) | **NNG-XAI** (doc L77) · relacionado con S22-6 (orden legal) y S13 (transparencia del algoritmo) | 3 (decisión de diseño del módulo de transparencia) |
| **J2** | Revisado y **ya cumplía (sin cambio)**: etiquetas de estado de la tabla ("Asignado aquí", "Sin cupos", "Prioridad insuficiente", "No evaluado"), `aria-label` de la sección, `perfilLabel`, y toda la prosa de `AlgoritmoPage.jsx` — usa verbos de proceso ("el sistema cruza la lista… con las prioridades y los cupos", "revisa la N.°2", "el sistema activa una segunda etapa", "aplica prioridades definidas por ley"). Los verbos con sujeto "tú/la familia" son correctos. Controles ▶️ / ⟲ / ⏸ no son antropomorfismo | `AlgoritmoPage.jsx` (prosa completa) | **NNG-XAI** (doc L77) | 3 |

**Para la memoria (observación, no editar):** este cambio no toca el flujo de postulación; alimenta la descripción del módulo de transparencia (`/algoritmo`) en el cap. 3, no los resultados de la prueba de usabilidad del flujo.

---

### Bloque K — `/perfil` como fuente única de datos del estudiante y SEP como condición registrada (2026-08-27)

**Fundamento de origen (doble):**
1. **Fidelidad al SAE real.** `investigacion_algoritmo_sae.md` L53 define la condición de estudiante prioritario/a como el "15 % de los cupos por nivel reservados para el tercio de menores ingresos **según el Registro Social de Hogares**" — es decir, la determina el Estado con datos que el MINEDUC ya tiene, no es un dato que la familia autodeclare al postular. El prototipo previo la pedía como una casilla más en `/perfil` y en el paso 2 del flujo, lo que contradecía la mecánica real.
2. **Arquitectura de información (observación directa del usuario, no un pendiente numerado).** Las prioridades se pedían en dos lugares desconectados (`/perfil` y `/postulacion` paso 2) que pedían lo mismo. Principio del usuario: **lo que la familia no puede modificar se muestra como información/alerta, no como control.**

**Mapeo a guía:**
- **HAX G1** — verificado contra `investigacion_ux_guide_ai_systems.md` L27: la guía pide que "la primera pantalla de `/postulacion` explicite, antes de pedir datos, qué hace el algoritmo y qué **no decide la familia** directamente (orden de prioridad legal, no 'puntaje')". Reencuadrar la SEP como condición del Estado (no una casilla) es exactamente esa distinción "lo decide el Estado vs. lo elige la familia".
- **HAX G4** — verificado contra L33: `ColegioAnalisis` ya muestra "información contextualmente relevante junto a cada opción en el momento de decidir". La InfoBox de solo lectura del paso 1 ("Condición registrada…/Sin condiciones prioritarias registradas", con enlace "Editar en Mis datos") lleva ese mismo criterio a la condición SEP: se informa en el punto de decisión sin convertirla en control.
- **PAIR-ET** (coherencia, no motivación) — L53: "explicación general del sistema vs. explicación específica del resultado". El encuadre "no hay un campo para marcar la SEP porque la determina el MINEDUC" es explicación general del sistema, disponible antes de pedir datos.
- **FORM-MS** — la precarga de nombre/RUT/nivel desde `/perfil` al paso 1 (con posibilidad de corregir ahí) y la validación de RUT en vivo en `/perfil` son las buenas prácticas de "no volver a pedir datos" y "validación en tiempo real".

**Trazabilidad en código:** `S4 (refinamiento — arquitectura de información)` y `S22-6 / S22-11 (refinamiento)`. **No se creó sección del plan, no se sumó punto: 87/87 intacto** (`plan_mejora_sae.md` e `incisos.js` sin cambios). Commit `fe75d1f`.
**Estado de validación:** ✅ lint+build OK (2026-08-27) — `npm run lint` y `npm run build` limpios (code-agent, reconfirmados en la sesión principal).
**Impacto en cifras de la memoria:** ninguno — `asignacion.js`, la tabla `probAsignacion` y el umbral `>= 65` no se tocaron. Verificación caso Muñoz González end-to-end (funciones puras de `asignacion.js`, code-agent): `prioritario` activo en `/perfil` + hermano/funcionario/exalumno sembrados de `casoPrioridades` → Los Andes nivel 1 (hermano, 92 %), San Martín nivel 2 (prioritario, 90 %), resto nivel 2 (República de Chile y Villa del Sol resuelven a nivel 2 y no 4/3 porque la cuota SEP supera a exalumno/funcionario vía `Math.min` — comportamiento preexistente de `nivelPrioridadEnColegio`, no alterado).

**Contrato de datos nuevo:**
- `localStorage['sae_react_perfil']` (`PERFIL_KEY`) — lo escribe **solo** `/perfil`; fuente única del nivel estudiante: `{ nombre, rut, nivel, prioritario }`. `nivel` con el mismo vocabulario que el `<select>` del paso 1. El loader tolera perfiles antiguos (`condiciones.prioritario`).
- `localStorage['sae_react_postulacion']` (`STORAGE_KEY`) — lo escribe `PostulacionPage` al confirmar, **misma forma que antes**; ahora `perfilCompleto = { prioritario: <de /perfil>, prioridadesPorColegio: {...} }` (ya no lleva hermano/funcionario/exalumno globales). `SeguimientoPage` lo lee igual (`generarExplicacion` conmuta por `asignado.nivel` desde el Bloque G).
- `sae_react_postulacion_draft_list` sin cambios.

| # | Cambio | Ancla | Mapeo a guía | Cap. memoria |
|---|---|---|---|---|
| **K1** | Contrato de datos unificado: `PERFIL_KEY` pasa a ser la fuente única de los datos a nivel estudiante (`{ nombre, rut, nivel, prioritario }`), escrito solo por `/perfil`. `PostulacionPage` incorpora `cargarPerfilEstudiante()` (forma estable, tolera `condiciones.prioritario` antiguo) y `perfilCompleto = { prioritario: perfilEstudiante.prioritario, prioridadesPorColegio }`. `STORAGE_KEY` conserva su forma para `SeguimientoPage` | `PostulacionPage.jsx` `// S4 (refinamiento — arquitectura de información)` (~16, ~330) | **FORM-MS** (no volver a pedir datos ya ingresados) · coherente con **HAX G4** | 3 (arquitectura de información) |
| **K2** | `PerfilPage.jsx` reescrita: bloque 1 = nombre + **RUT nuevo con validación en vivo** + nivel (vocabulario unificado con el paso 1); bloque 2 = **solo** el toggle `prioritario`, reencuadrado ("no la eliges tú… el MINEDUC la determina según el RSH… aquí la activas solo para la simulación") con la advertencia de anulación por marcarla sin serlo; bloque 3 = nota de que hermano/funcionario/exalumno se declaran al postular, por colegio. Guarda `{ nombre, rut, nivel, prioritario }` | `PerfilPage.jsx` `S4 (refinamiento — arquitectura de información)` (~6, ~135) | **HAX G1** (doc L27 — qué decide el Estado vs. qué elige la familia; lo no editable no es una casilla) · **PAIR-ET** (explicación general del sistema) | 3, 5 |
| **K3** | Paso 1 (`Identifícate`): tras vincular al estudiante, `InfoBox` de **solo lectura** — "Condición registrada: Estudiante prioritario/a (SEP)… la determina el MINEDUC con el Registro Social de Hogares, se aplica en todos los colegios" o "Sin condiciones prioritarias registradas…", ambas con enlace "Editar en Mis datos" → `/perfil`. Precarga de RUN/nombre/nivel desde `/perfil` (corregibles) | `PostulacionPage.jsx` `// S4 (refinamiento — arquitectura de información)` (~886) | **HAX G4** (doc L33 — info contextual en el punto de decisión, sin convertirla en control) · **HAX G1** | 3, 4 (fidelidad medible: ¿el usuario entiende que la SEP no se declara?) |
| **K4** | Paso 2: **se quitan los cuatro chips globales** (hermano/funcionario/exalumno + prioritario) y su lógica `togglePerfil`/pre-marcado condicionado. En su lugar: leyenda **no interactiva** de las 3 prioridades declarables + botón "?" (modal `PRIORIDADES_INFO`/`PrioridadModal` conservados) + línea sobre la SEP que remite al paso 1 / `/perfil`. `PrioridadColegioControl` ahora se muestra en **todos** los colegios de la lista y se siembra directo desde `casoPrioridades` al agregar el colegio (ya no depende de que se haya activado un chip global). La InfoBox "¿Dónde tienes esa prioridad?" pasa a depender solo de `modoTutorial` | `PostulacionPage.jsx` `S22-11 (refinamiento) · S4 (refinamiento)` (~196, ~360, ~415, ~930, ~1000, ~1161) | continuidad del **Bloque G** (S22-11 — prioridad por colegio) · **HAX G1** (lo no editable no es un control) · **HAX G4** | 3, 4 |
| **K5** | `src/utils/rut.js` (nuevo): `formatearRut` / `rutValido` extraídos **verbatim** de `PostulacionPage`, ahora compartidos por `/perfil` y `/postulacion` (misma regla de validación del RUT del estudiante, sin duplicar lógica). `RegistroPage.jsx` conserva las suyas (no se tocó) | `rut.js` (archivo nuevo) · `PostulacionPage.jsx` import (~8) · `PerfilPage.jsx` import (~4) | **FORM-MS** (validación en tiempo real) · la extracción en sí es DRY/mantenimiento, **sin fundamento en guía** | 3 (menor) |
| **K6** | Paso 3 y comprobante `.txt`: la SEP se muestra como "condición registrada — la determina el MINEDUC" + enlace a `/perfil` (antes: "Prioridad transversal: Estudiante prioritario/a (15 %) — vale en todos tus colegios", fraseo de elección). El enlace "Editar" del bloque de prioridades del paso 3 pasa a "Editar por colegio" | `PostulacionPage.jsx` `S22-11 (refinamiento) · S4 (refinamiento)` (~1256) | **HAX G1** · continuidad de G4/G6 del Bloque H (no rotular a la familia) | 3, 4 |
| **K7** | `AlgoritmoPage.jsx`: **sin cambio funcional** — solo una nota en el `form-hint` del simulador aclarando que es exploratorio (`prioritario` local permitido ahí) y que en la postulación real la SEP la determina el MINEDUC, no se elige | `AlgoritmoPage.jsx` `S4 (refinamiento)` (~397) | **HAX G1** (aclara el alcance del simulador vs. el flujo real) | — |

**Para la memoria (observación, no editar):**
- **K3/K4 son material directo del cap. 4** (fidelidad medible): la prueba de usabilidad puede verificar si el usuario entiende que la condición SEP no se autodeclara. El instrumento Likert de comprensión (C1–C4) toca el orden de prioridades; conviene revisar al redactar si se agrega un ítem sobre "¿de quién depende la condición de estudiante prioritario/a?".
- **PIE sigue sin modelarse** como condición en el prototipo (ya anotado en el Bloque D, D4). Si más adelante se incorpora, el encuadre natural es el mismo que ahora tiene `prioritario`: condición que determina el Estado, se muestra como información y no como casilla que la familia elige. No bloquea la prueba.

---

### Bloque L — Fidelidad del flujo oficial: declaración de apoderado/a, aceptaciones al agregar colegio, aviso de pérdida de cupo (A · C · D, 2026-08-27)

**Fundamento de origen:** fidelidad al SAE real. `analisis_video_paso_a_paso_sae.md` (2026-08-27) — inventario estructural del flujo oficial levantado del video del MINEDUC (@mineducchile, "Paso a paso de Sistema de Admisión Escolar 2025"). Su sec. 2 clasifica las brechas del prototipo; este bloque implementa las tres marcadas **Indispensable** que no requieren decisión aparte del usuario:
- **A** (Paso 2 del video): casilla "declaro ser apoderado del postulante" antes de "agregar postulante".
- **C** (Paso 3 del video): al "agregar establecimiento" hay que aceptar el tipo de jornada y la adhesión al proyecto educativo y al reglamento interno.
- **D** (Paso 3 del video): advertencia inicial de alto riesgo — si el estudiante queda asignado en un colegio nuevo pierde de inmediato el cupo en el actual, acepte o rechace la asignación; y obligación de postular si el colegio actual no tiene continuidad de nivel.

**Mapeo a guía:**
- **HAX G1** (qué exige / qué implica el sistema) para **A** y **C**. Verificado contra `investigacion_ux_guide_ai_systems.md` L27: la guía pide que la primera pantalla de `/postulacion` explicite "qué hace el algoritmo y qué no decide la familia directamente" antes de pedir datos. Las condiciones que la familia debe afirmar (ser apoderado/a legal; aceptar jornada y adherir al PEI/reglamento) son parte de "qué exige el sistema real" — misma convención con la que el **Bloque A** mapea a G1 las reglas oficiales ausentes del prototipo (cf. S22-13). *Nota:* es una lectura extendida de G1 ("qué puede hacer el sistema") hacia "qué requisitos impone"; el fundamento fuerte es de fidelidad (video oficial), no de la guía.
- **HAX G16** (comunicar las consecuencias de las acciones del usuario) para **C** y **D**. Verificado contra L43: **D** es exactamente eso — la consecuencia de postular (perder el cupo actual). En **C**, aceptar el PEI/reglamento es una acción con consecuencia que el usuario debe conocer al ejecutarla.
- **BROOK** (gestión de expectativas; comunicar qué implica el sistema y qué no puede deshacer) — coherente con **D**. Verificado contra L16–L19.

**Trazabilidad en código:** comentarios `S22-12 (refinamiento)` (A), `S22-2 (refinamiento)` (C) y `S22-15 (refinamiento)` (D), todos con la etiqueta `fidelidad (analisis_video_paso_a_paso_sae.md brecha …)`. **No se creó sección del plan, no se sumó punto: 87/87 intacto** (`plan_mejora_sae.md` e `incisos.js` sin cambios). Commits `e745fe2` (documento fuente) y `be95578` (código). Archivos de contexto (`CLAUDE.md`, `CONTEXTO_CLAUDE_CODE.md` sec. 22) los actualizó el code-agent.
**Estado de validación:** ✅ lint+build OK (2026-08-27) — `npm run lint` y `npm run build` limpios (code-agent, reconfirmados en la sesión principal). Caso Muñoz González verificado end-to-end (San Martín nivel 2, Los Andes nivel 1).
**Impacto en cifras de la memoria:** ninguno — `asignacion.js`, la tabla `probAsignacion` y el umbral `>= 65` no se tocaron.

| # | Cambio | Ancla | Mapeo a guía | Cap. memoria |
|---|---|---|---|---|
| **L1 (A)** | Paso 1 ("Identifícate"): estado `declaraApoderado` (default `false`) + casilla obligatoria "Declaro ser el/la apoderado/a legal de este/a estudiante" (`aria-required="true"`) antes del botón "Vincular estudiante". El botón queda `disabled` hasta marcarla, con `form-hint` rojo `role="alert"` ("Debes marcar esta casilla para vincular al estudiante") y `title` explicativo | `PostulacionPage.jsx` `// A · fidelidad (…brecha A) · S22-12 (refinamiento)` — estado (~457), casilla (~955), `disabled` del botón (~1000) | **HAX G1** (doc L27; convención Bloque A) · fidelidad (`analisis_video_paso_a_paso_sae.md` sec. 2, brecha A) | 3 (decisión de diseño), 4 (fidelidad medible: ¿el usuario entiende que declara ser apoderado/a legal?) |
| **L2 (C)** | Paso 2: componente nuevo `AgregarColegioModal` (`<dialog>` nativo, reutiliza clases `.prio-modal*`, sin CSS nuevo) + helper `jornadaDeColegio(colegio, nivelAlumno)` (jornada del nivel del estudiante vía esquema v2 de `colegios.js`, con fallbacks). El botón "+ Agregar" de cada tarjeta hace `setColegioPendiente(c.id)` (`aria-haspopup="dialog"`) en vez de agregar directo; el modal pide dos casillas obligatorias — (1) "Acepto la jornada {jornada} de {colegio}." y (2) "Acepto adherir al proyecto educativo y al reglamento interno de {colegio}." — y solo con ambas se habilita "Agregar a mi lista". ESC / clic-fuera / "Cancelar" / ✕ cierran sin agregar; foco a la primera casilla al abrir, `aria-labelledby`, `aria-label` en la ✕, `key={colegioPendiente}`. Solo intercepta el agregar (no quitar ni reordenar). Los colegios de un borrador previo (`DRAFT_LIST_KEY`, S22-9) no se re-bloquean (aceptaciones dadas por hechas, comentado en el código) | `PostulacionPage.jsx` `// C · fidelidad (…brecha C) · S22-2 (refinamiento)` — `jornadaDeColegio` + `AgregarColegioModal` (~199), estado `colegioPendiente` (~461), `onClick` del botón "+ Agregar" (~1245), render del modal (~1391) | **HAX G1** (doc L27) · **HAX G16** (doc L43 — adherir al PEI/reglamento es acción con consecuencia) · fidelidad (sec. 2, brecha C) | 3, 4 (¿el usuario entiende que agregar un colegio implica aceptar su jornada y adherir al PEI?) |
| **L3 (D)** | Paso 2: `InfoBox tipo="alerta"` **siempre visible** (no depende de `modoTutorial`), primer elemento del `CardContent`. Título "Postula solo si necesitas cambiar de colegio". Tres párrafos: (1) postula solo si necesitas colegio nuevo o quieres cambiarlo; (2) si queda asignado/a en un colegio nuevo **pierde de inmediato el cupo en el actual aunque después rechaces**, y si no queda en ninguna preferencia mantiene su colegio de hoy (cruce con S22-15 resuelto inline); (3) obligación de postular si el colegio actual no sigue el próximo año con el nivel que le toca (ej. 8° básico sin media). Sin redundancia con el InfoBox "cómo se decide tu resultado" del paso 1 (P2 / HAX G1, Bloque I) ni con "¿Y si no quedo en ninguna?" del paso 3 (S22-15) | `PostulacionPage.jsx` `// D · fidelidad (…brecha D) · S22-15 (refinamiento)` (~1074) | **HAX G16** (doc L43 — consecuencia de postular) · **BROOK** (doc L16–L19 — gestión de expectativas) · fidelidad (sec. 2, brecha D) | 3, 4 (¿el usuario entiende que postular puede costar el cupo actual?), 5 (gestión de expectativas frente a la escasez de cupos) |

**Para la memoria (observación, no editar):** L1–L3 son material directo del cap. 4 como correcciones de fidelidad **medibles** en la prueba, equivalentes a E1–E6: la tarea dirigida puede verificar si el usuario entiende que declara ser apoderado/a legal, que agregar un colegio implica aceptar su jornada y su PEI, y que postular puede costarle el cupo actual. Conviene revisar al redactar si el instrumento Likert de comprensión (C1–C4) suma un ítem sobre el costo de postular.

---

### Bloque M — Cierre de la pasada de fidelidad: dirección de residencia (B acotado), aceptaciones de registro, aviso de reenvío (E, solo texto) (2026-09-01)

**Fundamento de origen:** fidelidad al SAE real, versión casi final del flujo para la reunión con la profesora guía del 2026-09-04. `analisis_video_paso_a_paso_sae.md` sec. 2 — brecha **B** (Indispensable: "dirección de residencia del estudiante: región + comuna + calle") y brecha **E** (Deseable-alto: "al modificar: reenviar la postulación y descargar el comprobante actualizado — el anterior queda obsoleto") — más las 14 capturas de una versión anterior de la plataforma (pantalla "Datos del postulante" con región/comuna/calle/número; registro con las dos aceptaciones obligatorias). Verificado contra el documento. **B se implementa en versión acotada por decisión explícita del usuario** (sec. 6 nº 8): se captura el dato con fidelidad de formulario pero **NO entra en la lógica de asignación**. **E se cubre solo con texto**; la invalidación efectiva del comprobante al editar queda para fase 2.

**Mapeo a guía:**
- **FORM-MS** — verificado contra `investigacion_ux_guide_ai_systems.md` sec. 7: "Validación en tiempo real con mensajes de corrección claros: reduce frustración y abandono". Aplica a los campos de dirección (validación suave `onBlur`, `role="alert"` por faltante, `aria-required`/`aria-invalid`) y al aviso de confirmación de las dos casillas de registro. *Nota:* "campos obligatorios claros" es una lectura **ligeramente extendida** de FORM-MS — el documento nombra explícitamente progreso, guardado/reanudación, volver sin perder datos y validación en tiempo real; la obligatoriedad clara de campos es práctica de formularios coherente con esa familia, no una cita literal.
- **HAX G16** (comunicar las consecuencias de las acciones del usuario) — verificado contra `investigacion_ux_guide_ai_systems.md` L43: el **aviso de reenvío (E)** es exactamente eso — cada modificación obliga a reenviar y el comprobante anterior deja de valer. G16 también aplica de forma secundaria a la nota "la dirección no cambia tu resultado" (comunicar que una acción/dato **no** tiene la consecuencia que la familia podría suponer).
- **Encuadre anti-mito** de la nota "la cercanía no es un criterio de prioridad del SAE": `caso_estudio_prueba_usabilidad_postulacion.md` sec. 3.1 punto 2 (**falso riesgo / creencia a corregir**). Verificado contra el documento. *Nota:* el doc trata el mito del **orden estratégico** de la lista, no el de la proximidad; el principio ("corregir las creencias sobre qué da o no ventaja en el SAE, no reforzarlas") es el mismo y se aplica por extensión.

**Trazabilidad en código:** `B · fidelidad (analisis_video_paso_a_paso_sae.md brecha B) · S22-1 (refinamiento)` (dirección de residencia) y `S22 (refinamiento) · fidelidad` (aceptaciones de registro y aviso de reenvío E). **No se creó sección del plan, no se sumó punto: 87/87 intacto** (`plan_mejora_sae.md` e `incisos.js` sin cambios). Commits `211d207` (documento de apoyo `comparativa_flujo_postulacion_v_final.md`) y `6db4156` (código). Archivos de contexto (`CLAUDE.md`, `CONTEXTO_CLAUDE_CODE.md` sec. 23) los actualizó el code-agent.
**Estado de validación:** ✅ lint+build OK (2026-09-01) — `npm run lint` (0 errores) y `npm run build` (limpio; `PostulacionPage` compila a 51.83 kB) corridos **en esta sesión** sobre el estado con el cambio aplicado.
**Impacto en cifras de la memoria:** ninguno — `asignacion.js`, la tabla `probAsignacion` y el umbral `>= 65` no se tocaron. Caso Muñoz González verificado end-to-end (Los Andes nivel 1, San Martín nivel 2).

| # | Cambio | Ancla | Mapeo a guía | Cap. memoria |
|---|---|---|---|---|
| **M1 (B acotado)** | Paso 1: bloque nuevo "Dirección del/de la estudiante" dentro de `post-alumno-block`, tras la casilla A (L1) y antes de "Vincular estudiante". Campos: **región** (el `<select>` `region` que ya existía, relabeleado "¿En qué región vive el/la estudiante?", obligatorio) + **comuna** (`<input type="text">` + `<datalist>` `COMUNAS_SUGERIDAS`, semilla = comunas de `colegios.js` + comunas frecuentes de RM y Valparaíso; obligatorio — decisión comentada: input+datalist en vez de `<select>` porque el catálogo completo de comunas por región es frágil) + **dirección (calle y número)** (`<input>`, placeholder "Ej: Av. Los Aromos 450"; obligatorio) + **número de casa o depto.** (`<input>`; opcional). `direccionCompleta` (región + comuna + calle) se suma a la condición `disabled` del botón "Vincular estudiante" junto a `rutValido`, nombre ≥ 3, nivel y `declaraApoderado`. Validación en vivo suave (`dirTocada`, `onBlur`, `role="alert"` por faltante, `aria-required`/`aria-invalid`). **Persistencia:** `confirmar()` escribe a `STORAGE_KEY` la clave nueva y **aditiva** `direccion: { region, regionLabel, comuna, calle, numero }`; el comprobante `.txt` suma el bloque "── DIRECCIÓN DE RESIDENCIA ──". **NO entra en la asignación** (`asignacion.js` intacto, `SeguimientoPage` no la lee); nota visible bajo el bloque: *"Tu dirección se usa para ubicar colegios cercanos en la búsqueda. No cambia tus probabilidades ni tu resultado: la cercanía no es un criterio de prioridad del SAE."* | `PostulacionPage.jsx` `// B · fidelidad (…brecha B) · S22-1 (refinamiento)` — `COMUNAS_SUGERIDAS` (~58), estado + `direccionCompleta` (~475), persistencia en `confirmar()` (~610) y comprobante `.txt` (~642), bloque de campos (~1026), `disabled` del botón (~1043); `index.css` `.post-direccion-block` (única regla nueva, ~6284) | **FORM-MS** (doc sec. 7 — validación en tiempo real; "campos obligatorios claros" = lectura extendida) · **caso_estudio sec. 3.1 pto 2** (nota anti-mito "la cercanía no prioriza") · **HAX G16** (secundario) · fidelidad (`analisis_video_paso_a_paso_sae.md` sec. 2, brecha B — resuelta en versión acotada por decisión del usuario) | **3** (decisión de diseño / alcance: capturar el dato sin usarlo en el algoritmo), **4** (fidelidad medible: ¿el usuario entiende que la dirección no da ventaja?) |
| **M2 (aceptaciones de registro)** | `RegistroPage.jsx`: las dos casillas obligatorias (`aceptaTerminos` = términos y condiciones del proceso de admisión; `aceptaDatos` = protocolo de manejo de datos) **ya existían** y ya bloqueaban "Registrarme" (`paso2Ok`). Se completó: `aria-required="true"` en ambos inputs, comentario de trazabilidad, enlaces simbólicos a `/cumplimiento` y aviso `role="alert"` al pie del `fieldset` ("Tienes que marcar las dos casillas para crear tu cuenta"), **mismo patrón que la casilla A del paso 1** de `/postulacion` (L1) | `RegistroPage.jsx` `S22 (refinamiento) · fidelidad` — fieldset `.rg-checks` (~384–425) | **FORM-MS** (doc sec. 7 — mensajes de corrección claros) · fidelidad (capturas del registro real) | **3**, **4** (fidelidad medible) |
| **M3 (aviso de reenvío E — solo texto, parcial)** | `PostulacionPage.jsx` paso 3 (`confirmado`), tras el botón de descarga del comprobante: `InfoBox tipo="alerta"` **siempre visible** "Si cambias tu lista después de enviar" — cada cambio obliga a **reenviar la postulación y descargar un comprobante nuevo**; el anterior deja de valer; vale el último descargado. Misma idea añadida al `InfoBox` de modo tutorial "¿Qué hacer ahora?". **Diferido a fase 2:** NO se invalida `comprobanteDescargado` al editar tras confirmar ni se rehace el flujo de envío (requeriría manejo de estado). **Resuelve parcialmente el pendiente P4→P3** (brecha E) | `PostulacionPage.jsx` `// E · fidelidad (…brecha E) · S22 (refinamiento)` (~1749) | **HAX G16** (doc L43 — comunicar consecuencias de las acciones del usuario) · fidelidad (`analisis_video_paso_a_paso_sae.md` sec. 2, brecha E) | **3**, **4** (fidelidad medible: ¿el usuario entiende que hay que reenviar al modificar?) |

**Para la memoria (observación, no editar):** M1–M3 son material del cap. 4 como correcciones de fidelidad medibles, equivalentes a E1–E6 y L1–L3. **M1 es además una decisión de alcance para el cap. 3:** se captura la dirección por fidelidad del formulario pero se declara explícitamente que **no entra en la asignación** (en Aceptación Diferida la distancia no prioriza). El documento de apoyo `comparativa_flujo_postulacion_v_final.md` consolida el inventario input por input (sec. 2) y su sec. 6 contiene el roadmap fase 2 (R1–R6) para el cap. 6.

---

### Bloque N — Panel de "condiciones detectadas" tras ClaveÚnica + atajo "Cargar caso de ejemplo" (2026-09-02)

**Fundamento de origen:** duda del usuario — al postular a un/a hijo/a, el sistema debería **mostrar de inmediato las condiciones de prioridad que ya tiene asociadas** (SEP, PIE, hermano/a matriculado/a) en vez de pedir que se declaren, tanto por realismo como para **reducir el tiempo de la sesión de prueba** (menos tipeo antes de llegar a lo que se testea: ordenar la lista, reaccionar a las advertencias, ver el resultado). Segunda observación del usuario sobre una captura del paso 2: los tres chips de "¿Tienes alguna de estas prioridades…?" se ven como un **menú de ventajas para elegir**, cuando deberían mostrar lo que el sistema **ya resolvió** para ese colegio → **N4**. Tercera observación, sobre una captura del paso 1: al marcar "¿Postulas también a un hermano o una hermana?" el sistema **debe capturar los datos básicos del hermano/a** (no solo mostrar texto) → **N5**. Alcance elegido por el usuario: **panel + carga de caso, sin tocar el núcleo** — PIE se muestra como información pero **NO entra al cálculo**; `asignacion.js`, la tabla `probAsignacion`, el umbral `>= 65` y la cifra 87/87 quedan intactos.

**Mapeo a guía:**
- **HAX G1** (deja claro qué puede hacer el sistema, *antes de pedir datos*) — verificado contra `investigacion_ux_guide_ai_systems.md` L27 ("conviene revisar que la primera pantalla de `/postulacion` explicite, antes de pedir datos, qué hace el algoritmo y qué no decide la familia directamente"). El panel "Esto es lo que el sistema ya sabe de tu hijo/a" hace explícito, apenas se ingresa, qué condiciones recupera el Estado y cuáles **no** hay que declarar.
- **PAIR — "Explainability + Trust"** (concepto, no cita literal): mostrar la **procedencia** de cada dato ("la determina el MINEDUC con el Registro Social de Hogares"; "en el sistema real llega ya cargado") en lugar de una casilla sin contexto.
- **`caso_estudio_prueba_usabilidad_postulacion.md` sec. 5 y sec. 9** (gap de datos del prototipo): esta entrada distingue el **PIE a nivel de estudiante** como dato del Estado (antes solo existía `nee.programa` por colegio). Cubre la parte de *representación*; el **gap de cálculo** (que el PIE modifique la probabilidad, procesándose antes del nivel 1) **sigue abierto** — ver sec. 6 nº 9.
- El atajo **"Cargar caso de ejemplo"** no tiene fundamento en una guía de UX: su base es **logística de la prueba** (decisión explícita del usuario para acortar la sesión) + el perfil de la familia del `caso_estudio…`. Es una herramienta de la demo, colapsada y rotulada como tal; no altera el flujo que se envía.
- **N4 — prioridad por colegio de solo lectura ("el sistema lo detecta, no lo pregunta"):** mismo fundamento **HAX G1** + **PAIR Explainability+Trust** que N1, aplicado a cada tarjeta del paso 2. Distinción de fidelidad SAE (verificada contra `investigacion_algoritmo_sae.md` §3.2, orden de procesamiento): `hermano/a` lo **verifica el sistema** por RUN contra el registro de matrícula → siempre solo lectura; `funcionario/a` y `exalumno/a` los **declara la familia y valida el colegio** → siguen siendo chips editables… salvo con el caso de ejemplo cargado, donde `casoPrioridades` ya trae la respuesta de los 6 colegios y todo se muestra de solo lectura + un `<details>` "¿Algo no calza? Corregir".

**Trazabilidad en código:** `S4 (refinamiento)` — extiende "`/perfil` es la fuente única de datos del estudiante" (Bloque K) y "condiciones del Estado como información de solo lectura". **No se creó sección del plan, no se sumó punto: 87/87 intacto** (`plan_mejora_sae.md` e `incisos.js` sin cambios).
**Estado de validación:** ✅ `npm run lint` (0 errores / 0 warnings), `npm run build` (limpio; `PostulacionPage` ~58.6 kB / ~16.9 kB gzip) y `npm test` (12/12) corridos **en esta sesión** sobre el estado con el cambio aplicado. Verificado end-to-end en el navegador: cargar el caso deja RUN/nombre/nivel + región + comuna + calle precargados, `loginOk`, y **la casilla de hermano/a marcada con los datos de Mateo Muñoz González (7° básico) precargados** (N5); el panel del paso 1 lista SEP, PIE (con la nota de que no afecta el %) y los 3 vínculos por establecimiento; en el paso 2, **Colegio San Martín** muestra "No detectamos ningún vínculo… entras por tu cuota SEP o el sorteo" (90 %), **Colegio Los Andes** muestra "✓ Hermano/a matriculado/a — verificado con el registro de matrícula" (92 %), sin chips editables; el `<details>` de corrección abre los chips y togglear "hermano/a" en San Martín sube el % a 96 (`calcularResultado` responde); el paso 3 muestra la línea "👨‍👩‍👧‍👦 Bloque familiar con Mateo Muñoz González…" y el `STORAGE_KEY` guarda `hermano: { nombre, run, nivel }`. **Escenario del fix N4:** con `sae_react_perfil` (caso) + `sae_react_postulacion_draft_list = [2,1]` en localStorage, recargando y pasando el paso 1 **sin** el botón "Cargar caso de ejemplo", Colegio Los Andes (restaurado del borrador) muestra "✓ Hermano/a matriculado/a — verificado…" y 92 %. Sin errores de consola.
**Impacto en cifras de la memoria:** ninguno — `asignacion.js`, `probAsignacion` y el umbral `>= 65` no se tocaron. Caso Muñoz González sin cambios (Los Andes nivel 1 / 92 %, San Martín nivel 2 / 90 %).

| # | Cambio | Ancla | Mapeo a guía | Cap. memoria |
|---|---|---|---|---|
| **N1 (panel de condiciones detectadas)** | Paso 1: apenas `loginOk`, InfoBox de solo lectura **"Esto es lo que el sistema ya sabe de tu hijo/a"** (componente `CondicionesDetectadas`). Lista: **SEP** (si `perfil.prioritario`), **PIE vigente** (si `perfil.pie`, con la frase explícita *"todavía no se refleja en el porcentaje estimado"*), y los **vínculos por establecimiento** (`VINCULOS_DETECTADOS`, derivados de `colegios.js` `casoPrioridades`: hermano/a → Los Andes, exalumno/a → República de Chile, funcionario/a → Villa del Sol) — estos últimos solo con el caso de ejemplo cargado (`perfil.caso === 'munoz-gonzalez'`), con la aclaración de que "se confirma al agregar ese colegio (paso 2)". Reemplaza el InfoBox SEP-only que estaba gateado por `region && alumnoOk` (se eliminó para no duplicar el mensaje). | `PostulacionPage.jsx` `// S4 (refinamiento) · transparencia (HAX G1)` — `VINCULOS_DETECTADOS` (~147), componente `CondicionesDetectadas` (~186), render tras "Ingreso exitoso" (~1010); `index.css` `.cond-detectadas` (~6291) | **HAX G1** (doc L27 — explicitar antes de pedir datos) · **PAIR Explainability+Trust** (procedencia del dato, concepto) · `caso_estudio…` sec. 5 (representación del PIE) | **3** (arquitectura de información: qué recupera el sistema vs. qué declara la familia), **4** (fidelidad medible: ¿el usuario entiende qué sabe el sistema y de dónde?) |
| **N2 (`pie` en el contrato de `/perfil`)** | `PerfilPage.jsx`: `PERFIL_KEY` gana el campo `pie: boolean`. Bloque 2 retitulado "Condiciones que el Estado ya tiene registradas" con una segunda `criterio-card` (PIE) junto a la de SEP; `guardar()` persiste `{ nombre, rut, nivel, prioritario, pie }`; el `sim-result` combina ambas ("SEP + PIE vigente"). La card PIE dice explícitamente que en el prototipo **no cambia el porcentaje estimado**. `cargarPerfilEstudiante()` de `PostulacionPage.jsx` lee `pie` y `caso` con forma estable (tolera perfiles antiguos). | `PerfilPage.jsx` estado `pie` (~52), Bloque 2 (~135–190), `guardar()` (~65); `PostulacionPage.jsx` `cargarPerfilEstudiante` (~40) | `caso_estudio…` sec. 5 y sec. 9 (gap de datos: PIE como dato a nivel estudiante) | **3** (modelo de datos), **4** |
| **N3 (atajo "Cargar caso de ejemplo")** | Paso 1, bloque `!loginOk`: `<details>` colapsado **"⚙️ Cargar caso de ejemplo (para la prueba de usabilidad)"** con un botón que escribe en `PERFIL_KEY` el perfil de la familia Muñoz González (Sofía, `4° básico`, `prioritario:true`, `pie:true`, `caso:'munoz-gonzalez'`), precarga RUN/nombre/nivel + región `RM` + comuna `La Florida` + calle, hace `setLoginOk(true)` y **siembra `prioridadesPorColegio` desde `casoPrioridades` para los colegios ya en `lista`** (p. ej. de un borrador guardado, que no pasan por `agregar()`). **Deja intactos** los pasos con valor de fidelidad: declarar apoderado/a, confirmar el nivel, agregar los 6 colegios con sus dos aceptaciones. Es una herramienta de la demo; no envía nada. | `PostulacionPage.jsx` `CASO_EJEMPLO` (~24), `cargarCasoEjemplo()` (~770), `<details className="post-demo">` (~1020); `index.css` `.post-demo` (~6305) | **sin guía** — logística de la prueba (decisión del usuario) + perfil de `caso_estudio_prueba_usabilidad_postulacion.md` sec. 2 | **3** (nota de método: cómo se reduce el tiempo de la sesión sin alterar la variable de contraste) |
| **N4 (prioridad por colegio de solo lectura — "el sistema lo detecta, no lo pregunta")** | `PrioridadColegioControl` reescrito. Título neutro *"Prioridad en {colegio}"* (antes *"¿Tienes alguna de estas prioridades…?"*). **`hermano/a` siempre de solo lectura** (fila `✓ … — verificado con el registro de matrícula` / `— no detectado`). **`funcionario/a` y `exalumno/a`:** chips editables *"declara si corresponde — el colegio lo verificará"* en modo normal; con el **caso de ejemplo cargado** (`prioridadDetectada = perfil.caso === 'munoz-gonzalez'`, prop `soloDeteccion`) **todo** es de solo lectura: se listan los vínculos detectados con su procedencia, o *"No detectamos ningún vínculo con {colegio}. Aquí entras por tu cuota de estudiante prioritario/a (SEP) o por el sorteo general."*, y un `<details>` *"¿Algo no calza? Corregir"* revela los chips editables (misma `onToggle` de siempre → `calcularResultado` responde). El bloque pasa de naranja (acción pendiente) a azul (informativo) en modo detección. Textos de introducción del paso 2 (InfoBox "¿Cómo funciona este paso?", leyenda "Prioridades que puede tener tu familia…", InfoBox "¿Dónde tienes esa prioridad?") con variante `prioridadDetectada`. **Fix (2026-09-02):** `prioridadesPorColegio` ahora se **inicializa** desde el borrador guardado (`sembrarPrioridadesDeBorrador()`, lazy initializer) — antes, un colegio restaurado de un borrador (p. ej. Colegio Los Andes tras recargar) no pasaba por `agregar()` y quedaba "sin vínculo detectado" aunque el perfil del caso siguiera activo. **`asignacion.js` sin cambios:** solo cambia cómo se muestra y edita `prioridadesPorColegio`, que sigue alimentando `nivelPrioridadEnColegio` igual. | `PostulacionPage.jsx` `PRIO_PROVENIENCIA` + helpers `filaDetectada`/`chipEditable` + `PrioridadColegioControl` (~420–505), `sembrarPrioridadesDeBorrador` (~610) + `useState` init (~625), `prioridadDetectada` (~700), textos del paso 2 con variante (~1480, ~1491, ~1560), prop `soloDeteccion` en el render (~1720); `index.css` `.post-prio-colegio--deteccion` / `.post-prio-colegio__detectadas` / `.post-prio-det*` / `.post-prio-colegio__correccion` (~5858) | **HAX G1** (doc L27) · **PAIR Explainability+Trust** (procedencia; concepto) · fidelidad — `investigacion_algoritmo_sae.md` §3.2 (qué prioridad verifica el sistema vs. cuál declara-y-valida el colegio) | **3** (arquitectura de información), **4** (fidelidad medible: ¿el usuario entiende que el sistema ya sabe qué vínculos tiene?) |
| **N5 (datos básicos del hermano/a en postulación de bloque)** | Observación del usuario sobre una captura del paso 1: al marcar *"¿Postulas también a un hermano o una hermana?"* solo se mostraba texto explicativo; **debe capturar los datos básicos del hermano/a**. Al marcar la casilla ahora aparece el bloque **"Datos del hermano o la hermana"** (nombre + RUN + nivel, mismo `<select>` de niveles que el estudiante principal), *antes* de la InfoBox "Postulación familiar en bloque" (que se conserva). `hermanoDatosOk = rutValido(hermanoRut) && nombre ≥ 3 && nivel` se suma a la condición `disabled` de "Vincular estudiante" (con `title` y `role="alert"` "Completa el nombre, el RUN y el nivel…"). Persistencia **aditiva**: `confirmar()` escribe `hermano: { nombre, run, nivel } | null` en `STORAGE_KEY`; el comprobante `.txt` gana la línea *"Postulación de bloque con: … (RUN …) — …"*; el resumen del paso 3 muestra *"👨‍👩‍👧‍👦 Bloque familiar con {nombre} ({nivel} · RUN {run})"*. **NO entra en `calcularResultado`** (el prototipo simula una postulación a la vez; sigue diciéndolo la InfoBox). El atajo N3 precarga al hermano = **Mateo Muñoz González, 7° básico** (`CASO_EJEMPLO.hermano*`) y marca la casilla. | `PostulacionPage.jsx` estado `hermanoRut/Nombre/Nivel` + `hermanoDatosOk` (~649), bloque de campos (~1232), `disabled`/`title` de "Vincular" (~1446), `confirmar()` payload (~834), comprobante `.txt` (~870), resumen paso 3 (~1905), `CASO_EJEMPLO` + `cargarCasoEjemplo()` (~24, ~789); `index.css` `.post-hermano-datos` (~6284) | **sin guía específica** — fidelidad del flujo real (en el SAE hay una postulación por cada hijo/a) + `caso_estudio…` sec. 2 (familia Muñoz González: Sofía + Mateo en bloque). Coherente con la familia **FORM-MS** (validación clara de campos obligatorios) ya usada en el Bloque M | **3** (fidelidad del caso: la postulación de bloque deja de ser solo un texto), **4** (fidelidad medible) |
| **N6 (variante "sin prioridad SEP" del atajo + fix visual `/seguimiento` + concordancia)** | El usuario detectó que **con la familia SEP el colegio N.°1 SIEMPRE queda asignado**: `nivelPrioridadEnColegio` devuelve ≤2 en cualquier colegio (SEP transversal) y `probAsignacion` para nivel 2 es 88/90/98 según demanda — todo ≥ 65 (umbral). Las ramas "no quedaste en tu 1.ª opción" / "¿y si no quedo en ninguna?" eran **código muerto para un participante SEP**. **(a) Variante de contraste:** nuevo `CASO_EJEMPLO_SIN_SEP` (`= { ...CASO_EJEMPLO, prioritario: false, pie: false }`, mismo `caso`) y `cargarCasoEjemplo(cfg)` parametrizado; el `<details>` del atajo pasa a dos botones ("Familia Muñoz González (con SEP)" / "Variante sin prioridad SEP") con nota que explica: sin SEP, San Martín como 1.ª opción baja de 90 % a **60 %** (nivel 5, demanda media) → **no** queda ahí → cae a la 2.ª opción (Los Andes, hermano, 92 %) y se dispara la explicación de "no quedaste…". `PrioridadColegioControl` gana prop `tieneSEP`: el texto "sin vínculo" dice *"entras solo por el sorteo: no tienes ninguna prioridad en este colegio"* en vez de mencionar la cuota SEP. **(b) Fix visual:** `.seg-hero-img-wrap` tenía `height` fijo + `overflow: hidden` → el nombre del colegio (bloque `--solid` que fluye después de la ilustración) quedaba recortado; se quitó el alto fijo. **(c) Concordancia:** `generarExplicacion` decía *"No quedaste en tus primeras 1 opcion"* → ahora *"No quedaste en tu primera opción"* (singular/plural correcto; este mensaje aparecía por primera vez de verdad con la variante sin SEP). | `PostulacionPage.jsx` `CASO_EJEMPLO_SIN_SEP` (~46), `cargarCasoEjemplo(cfg)` (~823), `<details>` con dos botones + nota (~1155), `PrioridadColegioControl` prop `tieneSEP` (~503, ~521) + render (~1866); `index.css` `.seg-hero-img-wrap` (~3679) + `.post-demo__botones` / `.post-demo__nota` (~6300); `SeguimientoPage.jsx` `generarExplicacion` (~56) | **`caso_estudio…` sec. 3.1 pto 1** (riesgo real: probabilidad más baja donde no hay vínculo) · **test doc sec. 7.1** (variable de contraste) · **HAX G16** (comunicar consecuencias del orden de la lista) | **3** (diseño del caso: la variante permite testear el escenario "no quedé en mi 1.ª opción"), **4** (fidelidad medible: ¿la página transmite el peso de un colegio sin ventaja?) |

**Para la memoria (observación, no editar):** N1–N2, N4, N5 y N6 son material del cap. 3 (arquitectura de información y fidelidad del caso Muñoz González) y del cap. 4 (fidelidad medible). N3 es una nota de método para el cap. 3 (§3.5): el atajo acorta el tiempo de tipeo de la sesión sin tocar lo que se observa. **N6 quedó superado por el Bloque O** (se decidió resolver por el caso, no por una variante).

---

### Bloque O — El caso Muñoz González deja de tener cuota SEP; "colegio en mente" = sin vínculo + demanda alta (2026-09-03)

**Decisión del usuario:** quitar del caso la cuota de estudiante prioritario/a (SEP) y también el PIE, *"ya que en realidad no aporta al estudio como quisiera"*. El caso debe tener un **"colegio en mente"** que la familia pone como 1.ª prioridad pero que **es probable que no obtenga**, porque tiene **demanda alta** de otros apoderados y la familia **no tiene ninguna prioridad destacada** ahí — para que el usuario interactúe y le nazcan dudas del tipo *"¿qué pasa si pongo este colegio primero? ¿o mejor este donde ya tengo a mi otro hijo matriculado? ¿o uno más cerca?"*.

**Por qué (argumento):**
- **En el mecanismo real, SEP es una CUOTA, no un rango** (`investigacion_algoritmo_sae.md` §3.2): reserva el 15 % de los cupos por nivel; los diseñadores la modelan como una "sub-escuela" con sus propias vacantes (*matching with contracts*, Kurata et al. 2017). Un/a estudiante SEP compite en **dos pozos** (el 15 % reservado + los cupos generales), no obtiene un salto en la fila general.
- **La cuota está sobre-suscrita** (§6): en 2018 los prioritarios fueron el **54,7 % de los postulantes** para una cuota de solo **15 %**. El propio doc lo marca como problema abierto.
- Por eso el modelo del prototipo —que trataba SEP (nivel 2) como ~88–98 % en toda demanda— **estaba mal en el mecanismo** *y* volvía imposible el escenario pedagógico: con la familia SEP, `nivelPrioridadEnColegio` devuelve ≤2 en los 6 colegios y `probAsignacion(2, ·)` ≥ 88 ≥ umbral 65 → el colegio N.°1 **siempre** quedaba asignado.
- **Se descartó recalibrar `probAsignacion`** (núcleo protegido; obliga a re-sincronizar `proyecto-tesis/`). La salida limpia es **quitar SEP del caso**: sin prioridad transversal, el "colegio en mente" (San Martín) resuelve a nivel 5 (sorteo) y, subiéndolo a **demanda alta**, da `probAsignacion(5, 'alta')` = **28 %** — incertidumbre genuina, sin tocar un solo número de la tabla.

**Mapeo a guía:**
- **HAX G2** ("Deja claro qué tan bien puede hacerlo") — verificado contra `investigacion_ux_guide_ai_systems.md` L29: la cifra es una *estimación*, mostrada con su fundamento (postulantes año anterior, vacantes) vía `ProbabilidadVisual`. Un 28 % honesto es exactamente lo que G2 pide.
- **HAX G16** ("Comunica las consecuencias de las acciones del usuario") — L43: antes de que la familia reordene, la página debe anticipar qué significa (y qué **no** — poner el soñado primero no quita nada en los demás).
- **PAIR — "Explainability + Trust"** (gestión de expectativas; concepto).
- **`caso_estudio_prueba_usabilidad_postulacion.md` §3.1** — separa *riesgo real* (28 %, amerita advertencia) de *falso riesgo estratégico* (el mito de que esconder el colegio abajo en la lista ayuda — la página lo corrige).

**Cambios en código:**

| # | Cambio | Ancla |
|---|---|---|
| **O1** | `colegios.js` — **Colegio San Martín: `demanda: 'media' → 'alta'`**; `postulantesAnterior` por nivel subido (básico 44→71, kínder 28→55, prekínder 31→58) y vacantes básico 28–32 → 24–28, para que la ficha sea coherente con "alta". Comentario nuevo explicando el rol de colegio en mente. | `src/data/colegios.js` id 2 (~59–83) |
| **O2** | `CASO_EJEMPLO` — **`prioritario: false`, `pie: false`**. La familia Muñoz González ya no tiene ninguna condición transversal; sus únicas ventajas son los vínculos por colegio (hermano/a → Los Andes, funcionario/a → Villa del Sol, exalumno/a → República). | `PostulacionPage.jsx` `CASO_EJEMPLO` (~30) |
| **O3** | Se **elimina** `CASO_EJEMPLO_SIN_SEP` y el doble botón (N6): la "variante sin SEP" pasa a ser **el** caso. `cargarCasoEjemplo()` vuelve a no tener parámetro; el `<details>` del atajo tiene un solo botón "Cargar familia Muñoz González". CSS `.post-demo__botones` / `.post-demo__nota` retirado. | `PostulacionPage.jsx` `cargarCasoEjemplo` (~820), `<details>` (~1140); `index.css` `.post-demo` (~6300) |
| **O4** | `flujo-postulacion.test.js` **reescrito** para el caso sin SEP: San Martín `demanda: 'alta'` y prob **28 %**; el "escenario clave" se invierte (San Martín 1.º → NO queda → cae a Los Andes 2.ª opción); nuevo test "ordenar por preferencia real". El guardarraíl **"núcleo protegido: la tabla probAsignacion mantiene sus valores" queda IDÉNTICO** (la tabla no se tocó). 12/12 verde. | `sae-react/tests/flujo-postulacion.test.js` |

**Números del caso (tabla `probAsignacion` intacta):**

| Colegio | nivel · demanda | % |
|---|---|---|
| Los Andes (hermano/a) | 1 · alta | **92** — el seguro |
| **San Martín (ninguna)** | **5 · alta** | **28** — colegio en mente, resultado incierto |
| República de Chile (exalumno/a) | 4 · baja | 96 |
| Simón Bolívar (ninguna) | 5 · media | 60 — bajo umbral |
| Villa del Sol (funcionario/a) | 3 · alta | 65 — justo en el umbral |
| Los Quillayes (ninguna) | 5 · media | 60 — bajo umbral |

**Escenario clave (San Martín 1.ª opción):** 28 < 65 → no queda → `calcularResultado` cae a Los Andes (2.ª opción, hermano, 92 %) → `generarExplicacion`: *"Quedaste en el Colegio Los Andes porque tienes un hermano… No quedaste en tu primera opción porque ese colegio tenía más postulantes con prioridad mayor…"*. **La cercanía** no da prioridad y se responde con microcopy (nota de dirección, S22-1), no con la tabla.

**Trazabilidad en código:** `S22-13 (refinamiento)`. **`probAsignacion` y el umbral 65 NO se tocaron**; los ejemplos de `%` en `PRIORIDADES_INFO` (`PostulacionPage.jsx`) siguen siendo válidos (escritos contra la tabla actual). **No se creó sección del plan, no se sumó punto: 87/87 intacto.**
**Estado de validación:** ✅ `npm run lint` (0/0), `npm run build` (limpio), `npm test` (12/12) — en esta sesión. Verificado end-to-end en el navegador: el atajo carga `{ prioritario: false, pie: false }`; el panel del paso 1 lista solo los 3 vínculos por colegio; en el paso 2, San Martín → **28 %** con *"entras solo por el sorteo: no tienes ninguna prioridad en este colegio"*; con San Martín 1.º, `calcularResultado` asigna **Los Andes en preferencia 2** y `/seguimiento` muestra la explicación de "no quedaste en tu primera opción". Sin errores de consola.
**Impacto en cifras de la memoria:** ninguno directo (`asignacion.js` intacto).
**Documentación de la prueba — HECHO (2026-09-03):** `caso_estudio_prueba_usabilidad_postulacion.md` actualizado (encabezado con nota de revisión; §1 "qué activa el caso"; §2 sin condiciones transversales; §3 tabla nueva con demanda + vínculo + `prob.`; §3.1 pto 1 y nota 2026-09-03; §5 marcado RESUELTO; §6 tareas 1/5/7 + nota de preparación con el atajo y el panel; §8 ítem C3 reformulado; §9 re-sincronización). `guion_moderador_prueba_usabilidad.md` actualizado (prep con el atajo, Tarea 1 con el panel de condiciones detectadas, Tarea 5 con "28 % / solo por el sorteo", Tarea 7 con el desenlace esperado). PDFs de ambos regenerados.
**Pendiente writing-agent:** (1) `proyecto-tesis/capitulos/03_metodologia.tex` — la descripción del caso (línea ~25) menciona *"condición de estudiante prioritario/a del 15 %"* entre los criterios que la familia activa; ya no aplica. (2) `material_prueba_usabilidad_postulacion.pdf` — la tarjeta de familia y las instrucciones al participante todavía asumen familia SEP + PIE; hay que regenerarlo (no hay `.md` fuente localizable — probablemente pandoc desde un origen que Diego tiene).
**Constancia para el escrito (pedido del usuario):** además de esta entrada, queda pendiente **argumentar por escrito en la memoria de dónde salen los porcentajes de `probAsignacion`**. Origen rastreado: `archivo/CLAUDE_v2.md` §3 definió rangos aproximados a ojo ("alta+nivel1-2: 90 %", "media+nivel5: 60 %", "baja: 95-99 %") etiquetados como "Gale-Shapley simplificado"; se concretaron en 15 valores finos en `archivo/prototipo_SAE_v2.html` y se copiaron al port React. **No hay calibración empírica.** Anclas cualitativas disponibles para la justificación: `investigacion_algoritmo_sae.md` §3.1 (rondas del DA), §3.2 (grupos de prioridad vs. cuotas; SEP como sub-escuela sobre-suscrita) y §6 (2018: 59,2 % a 1.ª preferencia, 82,5 % a alguna, 8,9 % sin asignar).

---

### Bloque P — Microcopy de la postulación familiar en bloque (2026-09-03)

**Observación del usuario:** el `InfoBox` "Postulación familiar en bloque" del paso 1 quedaba ambiguo — no se entendía si, al marcar la casilla y completar los datos del hermano/a, se estaba postulando a los dos o no. Empeoró con N5 (antes era solo texto explicativo; N5 agregó campos para tipear el RUN/nivel del hermano/a, lo que refuerza la sensación de "estoy postulándolo").

**Cambio (solo microcopy, sin lógica):** se reencuadra la casilla como **declaración de un hecho**, no una acción, y se explicita el alcance de la demo.
- **Casilla:** *"¿Postulas también a un hermano o una hermana?"* → *"{alumnoNombre} tiene un hermano o hermana que **también postula este año**"*.
- **Sublínea del bloque de datos:** nueva — *"Sirven para vincular las dos postulaciones. La postulación de {hermanoNombre} se hace por separado, con su propia lista de colegios."*
- **`InfoBox` reescrito:** 3 párrafos — (1) *"Ahora estás completando la postulación de {alumnoNombre}"* + qué hace la casilla (que el sistema intente dejarlos juntos); (2) *"En el sistema real harías una postulación por cada hijo/a… En esta demo completas y ves **solo la de {alumnoNombre}**"*; (3) el ejemplo de reordenamiento, atado a los nombres.
- **`InfoBox` de modo tutorial:** alineado ("En este flujo vinculas y postulas a **un/a estudiante**: {alumnoNombre}").
- **Tarjeta de estudiante vinculado (paso 1):** suma *"Vas a hacer **su** postulación (una lista de colegios)"* y, si hay hermano/a, *"👨‍👩‍👧‍👦 En bloque con {hermanoNombre} — su postulación se hace por separado"*.
- **Resumen paso 3:** *"Bloque familiar con…"* → *"En bloque con {hermanoNombre} ({hermanoNivel}) — su postulación va aparte"*.
- **Comprobante `.txt`:** *"Postulación de bloque con:…"* → *"En bloque con: … "* + línea aclaratoria *"(este comprobante es de {alumnoNombre}; {hermanoNombre} tiene su propia postulación)"*.

**Ancla:** `PostulacionPage.jsx` — casilla + sublínea (~1285), `InfoBox` bloque (~1361), `InfoBox` tutorial (~1373), tarjeta vinculado paso 1 (~1567), resumen paso 3 (~1966), comprobante `.txt` (~920).
**Mapeo a guía:** **NN/g — IA explicable** (`investigacion_ux_guide_ai_systems.md` §5: describir lo que el sistema hace en términos de proceso, sin sobre-prometer) · **HAX G1** (dejar claro qué hace y qué no hace el sistema). Sin fundamento en una guía para el fraseo exacto — es corrección de claridad.
**`asignacion.js` intacto. 87/87 intacto.** ✅ `npm run lint`/`build`/`test` (12/12) limpios (2026-09-03). Verificado en navegador: casilla, sublínea, ambos `InfoBox`, tarjeta del paso 1, resumen del paso 3 y el `.txt` del comprobante muestran el nuevo texto con los nombres interpolados (Sofía / Mateo).
**Pendiente writing-agent / material:** `caso_estudio…md` §6 tarea 4 y `guion_*` describen "activar la postulación familiar en bloque" — el concepto no cambió, pero conviene que el material aclare que el participante completa **solo** la postulación de Sofía.

---

### Bloque Q — Resultado provisional en vivo con el orden de la lista (2026-09-03)

**Observación del usuario:** *"no veo los cambios de porcentajes al cambiar de orden la lista"*. Diagnóstico: los porcentajes por colegio **no** cambian al reordenar (es lo correcto — el mecanismo es a prueba de estrategia), pero **nada mostraba la consecuencia** que sí tiene el orden: en qué colegio se termina asignado. Eso solo se veía tras confirmar / en `/seguimiento`. Es exactamente lo que **HAX G16** pide ("comunicar la consecuencia de la acción del usuario"; `caso_estudio §3.1` lo cita para el reordenamiento de la lista).

**Cambio:** nuevo componente `ResultadoProvisional({ resultado, modoTutorial })` que consume el `useMemo` `resultado` (recalculado en cada cambio de `lista`), así que **se actualiza al arrastrar/mover**. Se renderiza:
- **Paso 2**, justo debajo de la lista reordenable.
- **Paso 3**, antes del resumen de la lista.

Contenido: *"Con este orden, ¿dónde quedarías? — La simulación te asignaría en **{asignado.nombre}**"* + *"tu primera opción"* o *"tu preferencia N° {idx}. No quedarías en la(s) de más arriba porque su probabilidad estimada no alcanza."* + (solo con tutorial) *"El sistema recorre tu lista en el orden que elegiste y te asigna al primer colegio donde tu probabilidad estimada alcanza. **Reordenar no cambia los porcentajes**, pero sí puede cambiar en cuál quedas."* Si ningún colegio alcanza el umbral, `tipo="alerta"` y texto de "podrías quedar sin asignación en la ronda principal". Además, en el resumen del paso 3 la fila asignada lleva borde verde + *"— te asignarían aquí"*.

**Ancla:** `PostulacionPage.jsx` — `ResultadoProvisional` (~546), render paso 2 (~1940), render + marca de fila asignada en paso 3 (~2075, ~2116). Sin CSS nuevo (usa `InfoBox` y estilos inline).
**Mapeo a guía:** **HAX G16** (`investigacion_ux_guide_ai_systems.md` L43 — comunicar consecuencias de las acciones del usuario) · **HAX G2** (la cifra es estimación) · `caso_estudio…` §3.1 (falso riesgo estratégico: el reordenamiento no mejora las chances en los demás, pero sí decide cuál te toca).
**`asignacion.js` intacto. 87/87 intacto.** ✅ `npm run lint`/`build`/`test` (12/12) limpios (2026-09-03). Verificado en navegador: con [San Martín, Los Andes] el banner dice "Los Andes — preferencia N° 2"; al subir Los Andes, cambia a "es tu primera opción" en vivo; los `%` por colegio (San Martín 28, Los Andes 92) no se mueven.
**Para la memoria:** material del cap. 3/4 (comunicación de la consecuencia del orden — pieza central de la observación del *falso riesgo estratégico* en la prueba). El `caso_estudio §6` tarea 7 (predecir el resultado antes de verlo) queda **matizado**: ahora el paso 2/3 ya muestra la asignación estimada, así que la predicción pasa a ser "¿coincide el resultado con lo que la página te adelantó?" — un mejor indicador de comprensión. Anotar para el writing-agent.

**Doc de referencia nuevo (2026-09-03):** `docs/planificacion/mapa_resultados_caso_munoz_gonzalez.md` — enumera qué colegio queda asignado según el orden que elija el/la participante. **Reescrito para el modelo nuevo, ver Bloque R.**

**Refinamiento 2026-09-06 (`ResultadoProvisional` reescrito).** El texto original tenía tres problemas contra la investigación: (1) **umbral 65 % obsoleto** — decía *"donde tu probabilidad estimada alcanza"* / *"no alcanza"*, lógica que el Bloque R eliminó (ahora el desenlace sale de `resultado.sinAsignacionEnPreferencias` + `asignado.idx`); (2) **certeza sobre-afirmada** — *"No quedarías…"* suena a veredicto en un resultado con incertidumbre real (PAIR §3: calibrar, no sobreconfiar; RISK-NUM §6: formato de frecuencia); (3) **encuadre de "sorteo/azar"** — decisión del usuario (2026-09-06): el proyecto combate el estigma de "tómbola", así que este bloque **no** usa "sorteo" ni "azar". Ahora atribuye el desenlace a **prioridades legales + vacantes + demanda** (razón estructural: *"más familias que los piden que vacantes, y no tienes una prioridad ahí"*), usa *"X de cada 100"*, y encuadra como estimación con datos del año pasado vs. *"el resultado real lo calcula el sistema en octubre, con las mismas reglas y las postulaciones reales de este año"* + enlace *"Ver cómo se decide"* a `/algoritmo` (PAIR: explicaciones parciales — el detalle de cómo se resuelven los empates vive allí). Se conserva la frase de strategy-proofness (*"cambiar el orden no cambia estas probabilidades… poner primero el que más quieres"*). Ancla: `ResultadoProvisional` (~574). `asignacion.js`/`simulacionSae.js` intactos; 87/87 intacto; `npm run lint`/`build`/`test` (14/14) limpios; verificado en navegador con el orden canónico (San Martín 1.º → Los Andes 2.ª).

**Reformulación 2026-09-09 (`ResultadoProvisional`: de "pronóstico" a "ilustración de la regla").** El usuario cuestionó el recuadro: no existe en una postulación real y "predice un resultado". Diagnóstico: sí ejecuta el algoritmo (`calcularResultado`), pero **presentaba una simulación con semilla fija como un pronóstico confiado** (*"la estimación te deja en {X}… tu cupo es muy probable ({p} de cada 100)"*), actualizado en vivo al arrastrar, y con el descargo *"es una estimación… el resultado real en octubre puede salir distinto"* **oculto tras el modo tutorial**. Contra las guías: **Arteaga et al. 2022** (fuente nueva — `investigacion_guias_ux_transparencia_sae.md`) documenta que las familias del SAE **sobreestiman** sus chances (subjetiva ≈76 % vs. objetiva ≈44 %); un desenlace confiado mientras se arma la lista alimenta ese sesgo. **PAIR-ET:** calibrar la confianza, no maximizarla. **BROOK:** gestión de expectativas. **Cambios:** (a) encabezado *"Con este orden, ¿dónde quedarías?"* → *"¿Qué hace el orden de tu lista?"*; (b) el cuerpo encabeza con la **regla** ("el sistema recorre tu lista en este orden… cambiar el orden no cambia tus probabilidades por colegio, solo en cuál quedas… conviene poner primero el que más quieres"), el colegio queda secundario y sin *"muy probable"* ni repetir el `%`; (c) el descargo pasa a estar **siempre visible** (se quitó el gate `modoTutorial`; el prop se eliminó) e incluye la nota anclada a Arteaga: *"Muchas familias creen tener más posibilidades de las que después resultan; por eso conviene incluir varios colegios"*; (d) icono `🎯` → `📋`. Se conserva el enlace *"Ver cómo se decide"* → `/algoritmo` (PAIR: explicaciones parciales) y la rama `sinAsignacion` (BROOK). **Es explicabilidad del prototipo, NO existe en el SAE real** → se marca en el comentario del componente para **ocultarlo en la condición B** del estudio (F3, ver `f3_modo_control_inventario.md`). Ancla `ResultadoProvisional` (`PostulacionPage.jsx` ~566, render ~2055 y ~2189). `asignacion.js`/`simulacionSae.js` intactos; cifra de la matriz intacta; `npm run lint` (0/0), `npm run build`, `npm test` (16/16) limpios (2026-09-09). Sincronizados `caso_estudio…md` §6.2 y `guion_moderador…md` (Tarea 4) que citaban el nombre del recuadro.

**Criterio del usuario sobre "sorteo" / "azar" (2026-09-07):** los términos **sí se mantienen donde describen el mecanismo** (el desempate real: `/algoritmo`, `AlgoSimuladorPasos`, `prioridadLabels[5]` = *"Sorteo público transparente"*, el InfoBox *"cómo se decide tu resultado"* del paso 1) — nombrarlos en su proporción es parte de la transparencia. **Se evitan en el encuadre / la predicción** (cosas "externas" al mecanismo). Bajo ese criterio se cambió también `PrioridadColegioControl`: *"Aquí entras solo por el sorteo: no tienes ninguna prioridad"* → *"Sin una prioridad aquí, tu cupo depende de cuántas familias pidan este colegio y de las vacantes que tenga"* (rama `tieneSEP` reencuadrada en paralelo). Ancla `PrioridadColegioControl` (~535). Sincronizados `caso_estudio…md` §3.1/§5 y `guion_moderador…md` (Tarea 5) que citaban la frase. **"Tu prioridad aquí" para nivel 5 → helper `etiquetaPrioridad` (2026-09-07).** Varios sitios mostraban `prioridadLabels[5]` = *"Sorteo público transparente"* como si fuera la prioridad de la familia. Es un mal *"por qué"* del `%` (HAX G11 / PAIR: la razón real es demanda vs. vacantes, ya en las filas de arriba), suena raro ("sin ventaja" reetiquetado como "el sorteo es tu prioridad") y roza el estigma de "tómbola". `investigacion_algoritmo_sae.md §3.2`: el sorteo es un **desempate entre postulantes del mismo nivel**, no un nivel de prioridad. Ya había **inconsistencia** (el resumen del paso 3 y `SeguimientoPage` preferencias ya gateaban `nivel < 5`). **Cambio:** nuevo `etiquetaPrioridad(nivel)` en `asignacion.js` (`nivel < 5` → label normal; `nivel === 5` → *"Sin vínculo con este colegio"*; `null` → *"—"*). Aplicado en `ColegioAnalisis` ("Tu prioridad aquí"), el comprobante `.txt` (prioridad del colegio asignado + lista por colegio), `AlgoritmoPage` ("Prioridad:" del resultado del simulador) y `SeguimientoPage` ("Prioridad aplicada:" + resumen; el stat del hero usa *"Sin vínculo"* corto). **`prioridadLabels` sin tocar** (`asignacion.js` — lo usan los tests, y `/algoritmo` para nombrar el paso del sorteo, que sí es descripción del mecanismo). `AlgoSimuladorPasos:74` no se tocó: *"Sin prioridades especiales (Sorteo público)"* ya lidera con "sin prioridades". Solo se agregó el helper: la lógica del modelo no cambió; 87/87 intacto; `npm run lint`/`build`/`test` (14/14) limpios; verificado en `/algoritmo`.

---

### Bloque R — Reescritura del simulador: DA-por-colegio + Monte Carlo (plan C) (2026-09-03)

**Motivación (debate con el usuario).** La tabla `probAsignacion` (15 valores) no tenía calibración empírica y su estructura ("primer colegio con prob ≥ 65 %; si ninguno, el mayor con aviso") **no representa la Aceptación Diferida**: no hay umbral en el SAE real, ni un "primero que pasa la barra". El usuario quiere que el prototipo muestre *"una postulación real al SAE"*. Se descartaron la opción liviana (solo argumentar los números) y la pesada (DA multi-colegio con población completa); se eligió la **media (C)**: simular la competencia real *por colegio*.

**Checkpoint previo:** commit `c83d0b2` en `main`.

**Qué se hizo.**
- **`src/utils/simulacionSae.js` (nuevo):**
  - `mulberry32(seed)` — PRNG con semilla (todo el modelo es determinista).
  - `PARAMS_POBLACION` — parámetros de la multitud sintética, **decisión del usuario**: `pSep: 0.55` (ANCLADO — 54,7 % de prioritarios entre postulantes en 2018, `investigacion_algoritmo_sae.md §6`), `pHermano: 0.10`, `pFuncionario: 0.02`, `pExalumno: 0.03` (ESTIMACIONES plausibles, sin microdatos; ajustables), `cuotaSep: 0.15` (ley).
  - `simularCupo(colegio, nivelAlumno, familiaTramo, rng)` — **un** sorteo de cupo en un colegio: genera `A = postulantesAnterior` competidores sintéticos (dato real) + la familia, cada uno con un tramo de prioridad muestreado y un número de sorteo. **Fase 1 — cuota SEP:** los prioritarios compiten por `round(0.15·S)` cupos reservados, solo por sorteo (modelo "sub-escuela", `§3.2`). **Fase 2 — pozo general:** los no ubicados compiten por el resto por (rango legal, sorteo). `S = round((min+max)/2)` — **vacantes en el punto medio** (decisión del usuario). Devuelve `seated` + datos para narrar el proceso.
  - `probabilidadCupo(...)` — **Monte Carlo, 1000 iteraciones** (decisión del usuario), semilla fija por (colegio, nivel) → el `%` es estable y reproducible, y **no depende del orden de la lista**. Cacheado.
  - `tramoFamiliaEnColegio(perfil, id)` → `{ esSep, general }`. `TRAMO_SIN_PRIORIDAD`.
- **`src/utils/asignacion.js` (reescrito):** se **elimina `probAsignacion` y el umbral 65**. `calcularResultado(lista, perfil, nivelAlumno)` ahora: (a) el `%` de cada colegio sale de `probabilidadCupo`; (b) el colegio **asignado** sale de **un recorrido determinista de la lista** con semilla fija `SEED_CASO = 20260903` — la familia "propone" a su 1.ª opción; si el sorteo + su prioridad no la sientan, pasa a la 2.ª, etc. (`simularCupo` por colegio, `rng` compartido). Si no queda en ninguna → fallback al de mayor `%` con `sinAsignacionEnPreferencias: true`. Se conservan `nivelPrioridad`, `nivelPrioridadEnColegio` (para etiquetas), `prioridadLabels`, `PRIORIDADES_POR_COLEGIO`, y el vocabulario de `estado` (`asignado`/`no_evaluado`/`sin_cupos`/`prioridad_insuficiente`) para no romper `AlgoSimuladorPasos`.
  - `probPorcentaje(pRaw)` — acota a **[1, 99]**: un sistema con sorteo nunca garantiza ni prohíbe del todo un cupo (evita "resultados casi improbables" de 0 % / 100 %).
- **Ajuste de datos (`colegios.js`), tras analizar los resultados:** Simón Bolívar y Los Quillayes tenían `postulantesAnterior` de básico muy cerca de las vacantes (~1,2–1,4×), así que un/a postulante sin prioridad daba ~70–77 %, incoherente con "demanda media". Se subieron a ~1,7× (Simón Bolívar 42→60, Los Quillayes 48→62; y sus otros niveles en proporción) → ahora dan ~46–50 %. Los demás colegios ya eran coherentes con su etiqueta y no se tocaron.
- **Consumidores actualizados:** `PostulacionPage.jsx` (`ColegioAnalisis` y el `useMemo` de `resultado` pasan el nivel), `ColegioPage.jsx` (usa un nivel representativo del colegio). `AlgoritmoPage.jsx`/`AlgoSimuladorPasos.jsx`/`SeguimientoPage.jsx` funcionan sin cambios (misma forma de `resultado`).
- **`tests/flujo-postulacion.test.js` reescrito** (14 tests): snapshot calibrado de los 6 `%`, propiedades (prioridad ≥ 90; San Martín < 40; media 30–65), **strategy-proofness** (el `%` de un colegio no cambia con su posición), determinismo, el escenario clave, y un guardarraíl del núcleo del modelo (`PARAMS_POBLACION`, `SEED_CASO`, iteraciones). El guardarraíl de "la tabla probAsignacion no cambia" se retiró (ya no hay tabla).

**Números del caso Muñoz González (4° básico, sin SEP), Monte Carlo 1000:**

| Colegio | vínculo · demanda · sobredemanda | `%` (modelo nuevo) | antes (tabla) |
|---|---|---|---|
| Colegio Los Andes | hermano/a · alta · 3,4× | **99 %** | 92 % |
| Escuela República de Chile | exalumno/a · baja · 0,8× | **99 %** | 96 % |
| Colegio Villa del Sol | funcionario/a · alta · 2,9× | **99 %** | 65 % |
| Liceo Técnico Simón Bolívar | — · media · 1,7× | **50 %** | 60 % |
| Escuela Básica Los Quillayes | — · media · 1,8× | **46 %** | 60 % |
| **Colegio San Martín** | — · alta · 2,7× | **26 %** | 28 % |

Lectura pedagógica: **tener una prioridad legal → casi seguro (~99 %)** — es fiel: el SAE reserva a hermanos/funcionarios/exalumnos para que no dependan del sorteo. **Sin vínculo, la demanda manda:** alta → 26 % (San Martín, el "colegio en mente" incierto), media → moneda al aire (~48 %). El escenario clave se sostiene: San Martín 1.º → no queda → cae a Los Andes en 2.ª preferencia (verificado con `SEED_CASO`).

**Limitaciones declaradas (v1):** es DA *por colegio*, no DA multi-colegio (no modela que los demás postulantes también se desplazan entre colegios); no modela la cuota PIE (≤2 cupos/curso) ni la de alta exigencia académica; `pHermano`/`pFuncionario`/`pExalumno` son estimaciones (la calibración con microdatos queda fuera de alcance — sería el paso siguiente). Todo documentado en el encabezado de `simulacionSae.js` y en `mapa_resultados_caso_munoz_gonzalez.md`.

**`asignacion.js` reescrito → esto ES un cambio del núcleo, autorizado explícitamente por el usuario.** La cifra 87/87 del plan **no** cambia (no se creó sección). ✅ `npm run lint` (0/0), `npm run build` (limpio), `npm test` (14/14) — 2026-09-03. Verificado end-to-end en navegador: los 6 `%` en el flujo coinciden con el snapshot; `/colegio` (ficha) y `/algoritmo` (simulador) sin errores de consola.

**Pendiente writing-agent (grande):** `proyecto-tesis/capitulos/03_metodologia.tex` describe el simulador como "lógica Gale-Shapley simplificada" con una estimación por (demanda, prioridad). Ahora el modelo es una **simulación DA-por-colegio con Monte Carlo**; hay que reescribir esa parte del Cap. 3, con las limitaciones declaradas y los parámetros de población como supuesto metodológico anclado (parcialmente) a los datos 2018. Esto **subsume** la tarea previa de "argumentar de dónde salen los porcentajes".

---

### Bloque S — Paso 1 en modo verificación: identidad y domicilio precargados (2026-09-06)

**Fundamento de origen:** decisión del usuario (opción "a" — comportamiento permanente del flujo). El SAE real, tras ClaveÚnica, **precarga** los datos de identidad y domicilio del/de la estudiante desde el Estado; la familia los **verifica**, no los tipea. El paso 1 del prototipo todavía pedía rellenarlos en campos vacíos. Extiende el patrón de Bloque N ("el sistema lo detecta, no lo pregunta") de las prioridades a la identidad.

**Mapeo a guía:**
- **HAX G1** (`investigacion_ux_guide_ai_systems.md` L27 — explicitar, antes de pedir datos, qué sabe el sistema). El recap "Verifica los datos del estudiante" hace explícito qué recuperó ClaveÚnica.
- **PAIR — "Explainability + Trust"** (procedencia del dato; concepto): la nota *"en el sistema real, estos son los datos de tu hijo/a tal como están registrados en el Estado"*.
- **FORM-MS** (lectura extendida — campos claros, menos fricción): verificar en vez de tipear reduce el trabajo de un formulario multipaso.

**Cambios en código (`PostulacionPage.jsx` + `index.css`):**

| # | Cambio | Ancla |
|---|---|---|
| **S1** | `IDENTIDAD_CLAVEUNICA_DEMO` (identikit ficticio, incluye un `hermano`) + `ingresarConClaveUnica()`: al ingresar (ClaveÚnica o RUT) se siembran nombre/RUN/nivel/región/comuna/calle y, si el identikit trae hermano/a, sus datos + `postulaHermanos=true` — desde `/perfil` si existe, o del identikit demo si no. `|| ` guard: no pisa datos de `/perfil` ni del caso de ejemplo. | `PostulacionPage.jsx` `IDENTIDAD_CLAVEUNICA_DEMO` (~51), `ingresarConClaveUnica` (~877), ambos botones de login |
| **S2** | Bloque de identidad + bloque de dirección se muestran como **recap de solo lectura** (nombre, RUN, nivel, domicilio) con botón *"Algún dato no está bien — corregir"* (`editandoIdentidad`) que abre los campos editables + *"Listo, datos verificados"*. Título del bloque: *"Verifica los datos del estudiante"*. | `PostulacionPage.jsx` `editandoIdentidad` state (~715), recap/edit en `post-alumno-block` (~1300), gate del `post-direccion-block` (~1545); `index.css` `.post-identidad-verif` (~6319) |
| **S3** | Se conservan como **actos activos**, no precargados: la confirmación explícita del nivel (S22-12, *"Verifica el curso"*) y la casilla *"Declaro ser apoderado/a legal"* (declaración jurada). **El hermano/a en bloque también:** la casilla *"tiene un hermano/a que también postula"* y sus datos quedan **visibles en el flujo** (fuera del "corregir"), para que la familia **confirme explícitamente** esa postulación en bloque; la casilla aparece **marcada** y los campos **rellenados** desde ClaveÚnica (`ingresarConClaveUnica` siembra `hermano*` + `postulaHermanos=true`). Encabezado *"Verifica los datos del hermano o la hermana"*. | `PostulacionPage.jsx` bloque `post-hermano-datos` visible (~1417) |
| **S4b** | Microcopy: el hint de la región dice que *"viene de tu domicilio registrado; puedes cambiarla"*; el aviso `post-login-ok` ya decía *"Tus datos fueron cargados desde ClaveÚnica"* (ahora literal). | `PostulacionPage.jsx` hint de región (~1256) |
| **S5** | **La lista de colegios se arma desde cero en cada visita.** Primero se probó un reset manual (botón para descartar el borrador), pero el usuario prefirió que la lista simplemente **no se persista**: cada entrada a `/postulacion` parte vacía — lo que la prueba comparativa de **lista fija** necesita (sin arrastre de estado entre participantes). Se retiró la persistencia del borrador (`DRAFT_LIST_KEY`), `cargarBorradorInicial`/`sembrarPrioridadesDeBorrador`, el `useEffect` de guardado, `borradorCargado` + el aviso *"Retomaste tu borrador"*, y el badge *"✓ Borrador guardado"* (`borradorGuardado`, CSS `.post-draft-badge`). `lista` y `prioridadesPorColegio` arrancan en `[]` / `{}`. Navegar entre los 3 pasos **sí** conserva la lista (estado React). `limpiarTodo()` queda en el `<details>` de la demo, ahora solo para `PERFIL_KEY` + `STORAGE_KEY` (limpiar identidad y última postulación entre participantes). **Impacto en el plan:** inciso **22-9** reformulado (de "guardado visible de borrador + reanudación" a "la lista no se pierde al moverse entre pasos"); se mantiene ✅, **87/87 sin cambio**. **Pendiente writing-agent:** Cap. 3 Tabla 3.1 menciona "guardado visible de borrador" — suavizar a "la lista se mantiene durante el flujo; cada visita parte de cero". | `PostulacionPage.jsx` estado `lista`/`prioridadesPorColegio` (~715), `limpiarTodo` (~905), `<details className="post-demo">` (~1240); `index.css` `.post-demo__botones` (~6403) |

**Trazabilidad en código:** `S22-12 / S4 (refinamiento)`. **`asignacion.js` / `simulacionSae.js` intactos. 87/87 intacto** (`plan_mejora_sae.md` e `incisos.js` sin cambios).
**Estado de validación:** ✅ `npm run lint` (0/0), `npm run build` (limpio; `PostulacionPage` 63.4 kB), `npm test` (14/14) — en esta sesión. Verificado end-to-end en navegador (por inspección de DOM; la captura del panel salía en blanco): login → recap con Sofía Ríos Contreras / 21.457.883-4 / 4° básico / Pasaje Los Copihues 145, La Florida y región RM preseleccionada → "corregir" abre los inputs + el bloque de dirección + "Listo" → marcar apoderado/a habilita "Vincular" → "Verifica el curso" → tarjeta de vinculado. El atajo "Cargar familia Muñoz González" carga esos datos en el recap, con el panel de vínculos y Mateo precargado. Sin errores de consola.
**Impacto en cifras de la memoria:** ninguno.
**Pendiente writing-agent / material (fase F1):** `03_metodologia.tex` §3.5 y §6.2, `caso_estudio…md` (tareas 1 y 3), `guion_*` — la tarea de identificación pasa de *"rellenar"* a *"verificar los datos que la página precarga"*. Sube la fidelidad frente a `analisis_video_paso_a_paso_sae.md` brecha B (el SAE real precarga más de lo que hacía el prototipo).

---

### Bloque T — Correcciones tras la auditoría independiente (2026-09-08)

**Fundamento de origen:** auditoría independiente `docs/auditoria_independiente_sae_2026-09-08.md` (inspección de código y docs, sin usar comentarios como prueba). No proviene de las guías de UX-IA; son correcciones de **fidelidad al mecanismo** (sorteo por colegio, §3.2 de `investigacion_algoritmo_sae.md`) y de **honestidad de la explicación** (PAIR-ET: no narrar certeza donde no la hay). Se ejecutó un subconjunto acotado de la lista de la auditoría (el resto, decisión del autor / trabajo posterior).

**Estado de validación:** ✅ `npm run lint` (0/0), `npm run build` (limpio; `PostulacionPage` 64.16 kB, `SeguimientoPage` 15.52 kB), `npm test` (**16/16**, +2 tests nuevos) — 2026-09-08.
**Impacto en cifras de la memoria:** el **snapshot de `%` del caso no cambia** (99/99/99/50/46/26); el escenario canónico tampoco (San Martín 1.º → Los Andes 2.ª preferencia). Se corrige el conteo "87/87" en `plan_mejora_sae.md` y en la memoria (ver más abajo).

| # | Cambio | Ancla | Mapeo a guía | Cap. memoria |
|---|---|---|---|---|
| **T1** | **Sorteo del recorrido independiente por colegio.** `calcularResultado` creaba **un** `mulberry32(SEED_CASO)` y lo recorría en orden de lista: mover un colegio cambiaba su tramo del flujo pseudoaleatorio → su desenlace *queda/no queda* podía depender de la posición, contradiciendo *"el desempate se sortea de forma independiente en cada colegio"*. Ahora cada colegio del recorrido usa su **propia semilla** `mulberry32((SEED_CASO ^ (id·0x9e3779b1)) >>> 0)` — igual que `probabilidadCupo` ya hacía para el `%`. Escenario canónico intacto (16/16 tests). | `asignacion.js` `calcularResultado` (~116–133) | fidelidad — `investigacion_algoritmo_sae.md` §3.2 (regla *Multiple Tie-Breaking*) · **HAX G16** (la consecuencia del orden es real y consistente) | 3, 4 |
| **T2** | **El fallback deja de narrarse como asignación real.** Si ningún colegio sienta a la familia, `calcularResultado` marca el de mayor `%` como `asignado` + `sinAsignacionEnPreferencias: true`; `SeguimientoPage.generarExplicacion` **no** miraba esa bandera y decía *"Quedaste en el {colegio}… a través del sorteo público y transparente"* (afirma un cupo inexistente y con el lenguaje de "tómbola"). Ahora `generarExplicacion(asignado, sinAsignacion)` ramifica: sin cupo → *"el sistema recorrió tu lista y ningún colegio te dejó un cupo… en el proceso real pasarías al Periodo Complementario… {colegio} es el de mayor probabilidad estimada ({p}%), pero eso no es una asignación"*. También el hero (`badge`, `pref`) y el bloque *"¿Qué significa no quedar en tu primera opción?"* se condicionan a `!sinAsignacion`. No dispara en el caso canónico (Los Andes sienta al 99 %); es corrección de un bug latente. | `SeguimientoPage.jsx` `generarExplicacion` (~24), consumo (~180), hero (~228), card (~410) | **PAIR-ET** (calibrar la confianza; no mostrar certeza donde no la hay) · **HAX G11** (razón real del resultado) · **NNG-XAI** (no "sorteo" en el encuadre — bitácora 2026-09-07) | 3, 4, 5 |
| **T3** | **Clave de caché de `probabilidadCupo` incluye `params`.** La clave era `id|nivel|esSep|general|iteraciones` — omitía `params`: una llamada con parámetros de población distintos reutilizaba un resultado calculado con otros. Bug latente (la app nunca pasa `params` custom; los tests sí podrían). | `simulacionSae.js` `probabilidadCupo` (~195) | — (corrección de corrección; sin fundamento en guía) | — |
| **T4** | **Stepper de `/postulacion` con `role="progressbar"`** (`aria-valuemin/max/now/text`), que el plan 20.2 pedía y faltaba. Se conserva `aria-current="step"` por paso y el `<nav>` como contenedor. | `PostulacionPage.jsx` stepper (~1104) | **FORM-MS** (progreso accesible) · plan 20.2 | 3 |
| **T5** | **Tests +2 (16/16):** (a) *fallback* — `calcularResultado([SAN_MARTIN])` → `sinAsignacionEnPreferencias: true`, `asignado.id === SAN_MARTIN`, `estado === 'asignado'`, `prob === 26` (el `%` no se infla); (b) *independencia por colegio* — San Martín 1.º nunca sienta y el desenlace no cambia con la cola de la lista. Nota en el test: con los 6 colegios del caso no se puede "alcanzar" un colegio en posición > 1 (San Martín es el único que no sienta), así que la independencia total es estructural, no observable por test. | `tests/flujo-postulacion.test.js` (~162–210) | — | 3 |
| **T6** | **Conteo "87/87" corregido.** `plan_mejora_sae.md`: nota de integridad (102 filas numeradas; 4 no aplican → **98 requisitos aplicables**; el "62→87" nunca tuvo tabla de consolidación; varias filas ya estaban ⚠️/❌). El bloque "No aplica — 1 punto" pasa a **4 puntos** (4.3, 8.1, 8.2, 9.3). Memoria (00/03/04/06): "el 100 % / 87 de 87" → "la totalidad de los puntos aplicables… a nivel de código", con la salvedad de que el conteo agregado queda pendiente de re-tabular. **No se cambió `sae-react/` por esto** (solo docs). | `plan_mejora_sae.md` "Resumen de estado"; `03_metodologia.tex` (nota tras Tabla 3.1 + "Estado actual"); `04_resultados.tex` (~24); `00_resumen.tex` (~7); `06_conclusiones.tex` (~13) | regla 3/6 de `CLAUDE.md` (no inventar; honestidad epistémica) | 3, 4, 6, 0 |

**No hecho de la lista de la auditoría (decisión del autor / trabajo posterior):** teclado en el autocomplete de `InicioPage`; reclasificar/implementar videos y tutoriales de `/algoritmo`; decidir si PIE y hermanos-en-bloque entran al modelo o se retira la afirmación; unificar consecuencias de aceptar/rechazar en `/seguimiento` vs `/proceso`; invalidación efectiva del comprobante; limpieza de CSS huérfano / breakpoint 768 px / quick cards / foto real; reescritura mayor de §3.5 (prueba formativa N=8 → estudio comparativo N≈30) y regenerar `material_prueba_usabilidad_postulacion.pdf`. La reescritura de §3.5 sigue en el roadmap F1 (§7) y en `prompt_pendientes_revision_caso_sin_sep.md`.

---

### Bloque U — Explicación del resultado en `/seguimiento` según `caso_estudio §3.2` (2026-09-09)

**Fundamento de origen:** la reflexión de `caso_estudio_prueba_usabilidad_postulacion.md` §3.2 (argumento técnico del rechazo de San Martín + su traducción para el usuario). Se lleva el "texto de referencia" de §3.2-B al copy real de `SeguimientoPage.generarExplicacion` — hasta ahora vivía solo en el doc. Es un **refinamiento de S15-3** (explicación contextualizada del resultado); no crea sección del plan.

**Estado de validación:** ✅ `npm run lint` (0/0), `npm run build` (limpio; `SeguimientoPage` chunk ~15.5 kB), `npm test` (16/16) — 2026-09-09. Verificado en navegador con el caso canónico (Sofía → San Martín 1.º 26 % → Los Andes 2.ª 99 %): `get_page_text` de `/seguimiento` muestra las 6 frases nuevas en orden, sin errores de consola.
**Impacto en cifras de la memoria:** ninguno — `asignacion.js` / `simulacionSae.js` intactos, snapshot de `%` sin cambios. Solo microcopy de `SeguimientoPage.jsx`.

| # | Cambio | Ancla | Mapeo a guía | Cap. memoria |
|---|---|---|---|---|
| **U1** | `generarExplicacion(asignado, sinAsignacion, detalles)` — nueva firma: recibe `resultado.detalles`. Para `asignado.idx > 1`, la línea genérica *"…tenía más postulantes con prioridad mayor o no había vacantes…"* se reemplaza por una explicación **específica del colegio que no te sentó** (nombre + ordinal): "postularon más familias que cupos" + "no tenías un vínculo (hermano/a, funcionario/a, exalumno/a) ni la reserva del 15 %" + "competías por los cupos que quedaban". Si el colegio previo tenía nivel ≤ 2 (tenía prioridad pero `sin_cupos`), variante: "tenías prioridad, pero postularon tantas familias con esa prioridad o mayor que los cupos no alcanzaron". | `SeguimientoPage.jsx` `generarExplicacion` (~30–120), call site (~200) | **BROOK** ("más familias que cupos" primero) · **HAX G11** (la razón real, específica del colegio) · **HAX G5/G6** (hecho acotado, sin juicio a la familia) · `caso_estudio §3.2-B/C` | 3, 4, 5 |
| **U2** | El desempate entre familias sin vínculo se narra como *"cada establecimiento elige con su propio sorteo, el mismo para todas… esta vez tu número no alcanzó… ese sorteo vale solo para el {colegio}: no afectó tus posibilidades en los demás"*. "Sorteo" describe el mecanismo (permitido, criterio 2026-09-07) y va atado a **trato igual entre iguales** + **strategy-proofness**, no a "azar / mala suerte". | ídem | **PAIR-ET** (no inducir acción errónea; explicación específica del resultado) · criterio microcopy 2026-09-07 (Bloque Q) · **HAX G16** | 3, 5 |
| **U3** | Cierre con **BROOK**: *"Por eso el sistema siguió con tu lista y te asignó al {asignado}. El SAE reparte los cupos que existen: no puede crear cupos nuevos ni asegurar la primera opción cuando hay más familias que vacantes."* Dirige la frustración a la escasez, no al algoritmo. | ídem | **BROOK** · **HAX G1** ("siguió con tu lista" = proceso, no intención) | 5, 6 |
| **U4** | Formato de frecuencia **"X de cada 100"** en toda la prosa de `generarExplicacion` (antes "X%"): la del colegio no obtenido y la del asignado. Los chips/stats del hero y del detalle por preferencia siguen en "%". | ídem + rama `sinAsignacion` | **RISK-NUM** (`investigacion_guias_ux_transparencia_sae.md` §7.2: frecuencia natural con denominador consistente) · `caso_estudio §3.2-C` | 3, 4 |
| **U5** | Rama `default` (nivel 5, quedó por el desempate sin vínculo): se reencuadra — deja de encabezar con *"a través del sorteo público y transparente… desempate aleatorio"* y pasa a "no tenías un vínculo, competías con las familias en tu misma situación por los cupos que quedaban; cuando hay más familias que vacantes cada colegio elige con su propio sorteo, el mismo para todas — y este año tu cupo salió". Coherente con el criterio 2026-09-07. **`prioridadLabels[5]` no se tocó** (string compartido con `/algoritmo`). | `SeguimientoPage.jsx` `switch` `default` (~63) | criterio microcopy 2026-09-07 · **HAX G5/G6** | 3 |
| **U6** | Tarjeta *"¿Qué significa no quedar en tu primera opción?"* — párrafo nuevo de **strategy-proofness**: *"Poner ese colegio primero en tu lista no te quitó ninguna posibilidad en los demás: cada establecimiento revisa tu postulación por separado, sin importar en qué lugar de tu lista lo pusiste."* Alimenta el ítem **F3** y la abierta **A1** del estudio. | `SeguimientoPage.jsx` card `idx > 1 && !sinAsignacion` (~463) | **PAIR-ET** · `investigacion_algoritmo_sae.md §4.2` · `caso_estudio §3.2-D` | 3, 4, 5 |

**Para la memoria (observación):** U1–U6 son la materialización en código de `caso_estudio §3.2`. En el estudio comparativo, toda esta explicación es **exclusiva de la condición A**; la condición B (F3, ver Bloque V) solo muestra "quedaste en {colegio}". Ítems que lo miden: C4, C5, J2 (comprensión/justicia del "por qué no San Martín"), F3 + abierta A1 (strategy-proofness).

---

### Bloque V — F3: implementación del modo control (condición B) (2026-09-09)

**Fundamento de origen:** el inventario `docs/planificacion/f3_modo_control_inventario.md` (§7.7) y el diseño comparativo de §7. No es una guía de UX-IA: es la **condición de control** del estudio — el mismo flujo **sin** la capa de explicabilidad, para aislar su efecto. Cierra la fase **F3** del roadmap de §7.

**Enfoque:** *un switch, no una copia.* Contexto nuevo `ModoEstudioContext` con `esControl`; cada pieza de explicabilidad consulta ese flag y se oculta o simplifica. **`asignacion.js` / `simulacionSae.js` / `SEED_CASO` intactos** → el resultado de asignación es **idéntico** en A y B (Sofía → San Martín 1.º 26 % → Los Andes 2.ª 99 %); B solo no lo explica.

**Estado de validación:** ✅ `npm run lint` (0/0), `npm run build` (limpio; `index` 261 kB, `EstudioPage` 2.15 kB), `npm test` (**16/16**) — 2026-09-09. Verificado en navegador: condición A (`?modo=full` / default) y B (`?modo=control` y vía `/estudio`) — Navbar, `/algoritmo`, tour, `/postulacion` paso 1 y `/estudio`. El switch funciona en ambos sentidos; `/estudio` con nº par → B, redirige a `/` sin query (ceguera del participante), limpia `localStorage` de la sesión anterior.

| # | Cambio | Ancla | Mapeo | Cap. memoria |
|---|---|---|---|---|
| **V1** | **`ModoEstudioContext.jsx`** (nuevo): provider montado dentro de `<BrowserRouter>`; `useModoEstudio()` → `{ modo, esControl, condicion }`. Lee `?modo=control` / `?modo=full` (`useSearchParams`), persiste en `sessionStorage['sae_modo_estudio']` (`'full'`\|`'control'`); default `'full'` (fuera del estudio el prototipo se comporta igual que siempre). Helper `fijarCondicion(cond, n)` para `/estudio`: fija el modo + limpia `sae_react_postulacion` / `sae_react_perfil`. | `src/context/ModoEstudioContext.jsx`; montaje en `App.jsx` (~187) | §7.7 · diseño §7 | 3 |
| **V2** | **`EstudioPage.jsx` + ruta `/estudio`** (nuevo): pantalla del moderador (uso interno). `condicionDe(n)` = impares → A, pares → B; muestra la condición proyectada; "Empezar" llama `fijarCondicion` y hace `window.location.href = '/'` (recarga completa, sin query en la barra). Ruta `lazy` + entrada SEO en `App.jsx`. | `src/pages/EstudioPage.jsx`; `App.jsx` (~78, ~211, ~139) | D1 (la lista de 6 colegios va en papel, no por URL) | 3 |
| **V3** | **`/algoritmo` cableado a B — OCULTAR.** `AlgoritmoPage`: `if (esControl) return <Navigate to="/" replace />`. `Navbar`: el link `{ to:'/algoritmo', soloA:true }` se filtra (`visibles`). `InicioPage`: `pasosRapidos` quita el paso a `/algoritmo` (2 pasos en B). `ProcesoPage`: componente `EnlaceAlgoritmo` (lee el contexto) reemplaza 2 `<Link to="/algoritmo">` → `null` en B. Tour (`TourContext` + `GuidedTour`): `pasos` se filtra por `page !== '/algoritmo'` (12 → **8** pasos en B) y el paso `final` reescribe su texto para no mencionar "el algoritmo" ni "simular tu caso". | `AlgoritmoPage.jsx`, `Navbar.jsx`, `InicioPage.jsx`, `ProcesoPage.jsx`, `context/TourContext.jsx`, `components/GuidedTour.jsx` | §7.7 OCULTAR · **HAX** (la capa pedagógica no es parte del SAE real) | 3, 4 |
| **V4** | **`/postulacion` cableado a B.** OCULTAR: `ResultadoProvisional` → `return null`; InfoBox "Antes de empezar: cómo se decide tu resultado"; InfoBox "Consejo: ordena por tu preferencia real"; el 2.º `<p>` de "¿En qué orden se revisan las prioridades?" (el "ojo: el 15 %…"); la coletilla "por eso el % puede cambiar de un colegio a otro". SIMPLIFICAR: `ColegioAnalisis` oculta el chip `tut-prob` y `ProbabilidadVisual`; paso 3 oculta el badge de probabilidad, la explicación tutorial de probabilidad, el "te asignarían aquí" y el hint "los porcentajes son estimaciones". | `src/pages/PostulacionPage.jsx` (`ResultadoProvisional`, `ColegioAnalisis`, `PostulacionPage`) | §7.7 OCULTAR / SIMPLIFICAR · **RISK-NUM** no aplica en B | 3, 4 |
| **V5** | **`/seguimiento` cableado a B.** OCULTAR: enlace a `/algoritmo` del estado vacío; stat "Probabilidad {prob}%" del hero; tarjeta "¿Por qué te asignaron este colegio?" (`generarExplicacion`); tarjeta "¿Qué significa no quedar en tu primera opción?". SIMPLIFICAR: "Detalle por preferencia" muestra solo "✅ Asignado" para el colegio asignado (sin `estadoLabel` ni `%` por preferencia); el comprobante `.txt` lista los colegios sin estado por-preferencia salvo "— Asignado". | `src/pages/SeguimientoPage.jsx` | §7.7 OCULTAR / SIMPLIFICAR | 3, 4, 5 |
| **V6** | **`/colegio` cableado a B — OCULTAR.** `.probviz-block` (icon array "X de cada 100" sin prioridad) y `vacantes-infobox` (con enlace a `/algoritmo`) se ocultan; el resto de la ficha (identidad, SIMCE, vacantes, PIE, PEI) se MANTIENE. | `src/pages/ColegioPage.jsx` | §7.7 · la ficha sí existe en la vitrina real; la proyección de probabilidad no | 3 |
| **V7** | **`ChatAyuda.jsx` — MANTENER (decisión).** El FAQ describe el SAE real en términos neutros (proceso, prioridades por ley, sorteo con notario) — es contenido que la vitrina oficial también tiene, no explicabilidad añadida del prototipo. Se deja idéntico en A y B para no empobrecer artificialmente el control. Registrado como MANTENER, revirtiendo el "SIMPLIFICAR" tentativo del inventario. | `src/components/ChatAyuda.jsx` (sin cambios) | §7.7 (ajuste) | — |

**Cierre del inventario:** `f3_modo_control_inventario.md` §2 queda con el estado real por pieza (cableada / MANTENER). El `caso_estudio §6.1/§6.2` y `CONTEXTO_GENERAL_PROYECTO.md §8.5` pasan de "F3 sin implementar" a "F3 implementada (Bloque V)". **Pendiente writing-agent:** §3.5 debe describir la condición B como implementada (no "planificada").

---

### Bloque W — Calibración de las bandas de probabilidad en el resumen del paso 3 (2026-09-10)

**Fundamento de origen:** revisión visual del usuario sobre el resumen por colegio del paso 3 (condición A). Es un **refinamiento de S22-14** (el bloque de explicación por colegio) y del criterio de calibración de **Arteaga et al. 2022** ya aplicado en el Bloque Q: si el prototipo rotula un ~50 % como *"probabilidad baja"*, induce el mismo error de expectativas que el resto del flujo corrige (una familia lee "baja" y descarta el colegio, o lo lee al revés). Un resultado parejo hay que **nombrarlo parejo**.

**Estado de validación:** ✅ `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16) — 2026-09-10. Verificado en navegador con el caso canónico (Sofía, lista fija): San Martín 26 → 🔴 "Probabilidad baja" (`tut-prob--baja`); Simón Bolívar 50 / Los Quillayes 46 → ⚖️ "Resultado parejo" (`tut-prob--media`, ámbar, ya no rojo); Los Andes/República/Villa del Sol 99 → ✅ "Probabilidad alta" / 🟢 "Casi segura"; consistente entre el chip del paso 2 (`ColegioAnalisis`), la barra `ProbabilidadVisual` y el resumen del paso 3.
**Impacto en cifras de la memoria:** ninguno — `asignacion.js` / `simulacionSae.js` intactos, snapshot de `%` sin cambios (99/99/99/50/46/26). Solo microcopy y clases CSS de `PostulacionPage.jsx` + `ProbabilidadVisual.jsx`.

| # | Cambio | Ancla | Mapeo a guía | Cap. memoria |
|---|---|---|---|---|
| **W1** | **Nueva banda "⚖️ Resultado parejo" (40–59 %)** entre "moderada" y "baja". Antes todo `< 60` era *"🔴 Probabilidad baja"*; un 46–50 % (sin vínculo + demanda media, "moneda al aire" según los tests) quedaba mal rotulado. Texto de la banda: *"…aproximadamente {p} quedan asignados… puede pasar o no. En este colegio no tienes un vínculo que te dé prioridad, así que compites por los cupos que quedan."* | `PostulacionPage.jsx` resumen paso 3 (`resultado.detalles.map`, ~2284) | **Arteaga et al. 2022** (calibrar expectativas) · **PAIR-ET** (no inducir a descartar/confiar de más) · **RISK-NUM** | 3, 4 |
| **W2** | **Se quitó la contradicción "porque tiene alta demanda para tu nivel de prioridad".** Aparecía idéntica en los 3 colegios `< 60 %`, incluso en los rotulados *"media demanda"* dos líneas más arriba. Ahora la banda baja usa la **demanda real del colegio** (`d.demanda`): *"…hay {d.demanda} demanda, así que quedan pocos cupos para tu situación."* | ídem, banda `< 40` | **HAX G11** (razón real, sin afirmaciones falsas) · **NNG-XAI** | 3 |
| **W3** | **El consejo "suma algún colegio de demanda más baja" queda solo en la banda baja.** Antes *"agrega colegios con demanda media o baja"* aparecía también sobre colegios de demanda media (circular). | ídem | **BROOK** (gestión de expectativas accionable) | 3 |
| **W4** | **Umbral de color unificado a 40 %** en las tres superficies: `probClass` del chip del paso 3 (~2248), `probClass` de `ColegioAnalisis` (paso 2, ~661) y `claseProb` de `ProbabilidadVisual.jsx` (~12). `≥ 80` verde · `40–79` ámbar · `< 40` rojo. Antes el corte ámbar/rojo estaba en 60, y una barra al 50 % salía roja. | `PostulacionPage.jsx` (~661, ~2248), `components/ProbabilidadVisual.jsx` (~12) | coherencia visual · **RISK-NUM** (el color no debe contradecir la cifra) | 3 |
| **W5** | Microcopy menor: *"chances"* → *"posibilidades"*; *"Muy buena probabilidad"* → *"Probabilidad alta"*; *"Certeza muy alta"* → *"Casi segura"* (más cercano al nivel 6.º básico); denominador uniforme *"De cada 100 postulantes en tu misma condición…"* en todas las bandas. | ídem | lenguaje claro (`CLAUDE.md`) · **RISK-NUM** (denominador consistente) | 3 |

**Para la memoria (observación):** W1–W5 son microcopy de la **condición A**; en la condición B (F3, Bloque V) todo este bloque está oculto (`esControl`). No se creó sección del plan; **98 aplicables sin cambio**.

---

### Bloque X — Enriquecimiento de la ficha de resultado (`/seguimiento`) (2026-09-10)

**Fundamento de origen:** revisión de la ficha de resultado contra las guías (`propuestas_ficha_resultado.md`, elaborado a partir de `investigacion_ux_guide_ai_systems.md` + `investigacion_guias_ux_transparencia_sae.md`). El usuario eligió implementar 5 de las 7 propuestas. Refinamiento de **S15-4/5** (pantalla de resultado). Todo es **condición A** (`!esControl`); en la condición B la ficha queda como estaba.

**Estado de validación:** ✅ `npm run lint` (0/0), `npm run build` (limpio), `npm test` (**16/16**) — 2026-09-10. Verificado en navegador con el caso canónico (Sofía → San Martín 1.º 26 → Los Andes 2.ª 99), en A y en B, y a 375 px (sin desborde horizontal; la tabla X2 encaja).
**Impacto en cifras de la memoria:** ninguno — `asignacion.js` / `simulacionSae.js` intactos. Solo `SeguimientoPage.jsx` + CSS.

| # | Cambio | Ancla | Mapeo a guía | Cap. memoria |
|---|---|---|---|---|
| **X1** | **Tarjeta "Qué sigue ahora"** — 3 pasos con fecha (aceptar/rechazar 15–21 oct · matrícula 9–22 dic · Periodo Complementario 10–17 nov) + aviso destacado *"si no entras antes del 21 de octubre, el sistema acepta la asignación automáticamente"*. La línea de tiempo terminaba en "Resultado" y esa regla de alto riesgo (de `/proceso`) no estaba donde más importa. | `SeguimientoPage.jsx` (~455, tras el bloque aceptar/rechazar) | **GOV.UK** (confirmation pages: "qué ocurre después") · **BROOK** (el proceso no terminó) · **HAX G16** (consecuencia de *no* actuar) | 3, 4 |
| **X2** | **Tabla "por qué te asignaron este colegio"** — 2 filas de un vistazo (preferencia no obtenida: *sin vínculo · demanda alta → No alcanzó* / asignada: *hermano/a → Cupo*) delante de la explicación; los 3 párrafos de `generarExplicacion` pasan a un `<details>` *"Ver la explicación completa"*. Helper nuevo `comparacionResultado()`. Solo cuando hay contraste (`asignado.idx > 1 && !sinAsignacion`). | `SeguimientoPage.jsx` `comparacionResultado` (~123), render (~491) | **HAX G11/G4** (la razón real, junto a la decisión) · comunicación de riesgo (una tabla se procesa más rápido que 3 párrafos) · divulgación progresiva (Google) | 3, 4 |
| **X3** | **Contrafactual concreto** en la tarjeta *"¿Qué significa no quedar en tu primera opción?"* — *"Si hubieras puesto el {colegio asignado} en primer lugar, habrías quedado igual ahí: cada colegio revisa tu postulación por separado…"*. Hace **verificable** con esta lista la propiedad que antes se afirmaba en abstracto (`caso_estudio §3.2-D`). Los 3 párrafos quedan más cortos. | `SeguimientoPage.jsx` (~525) | **PAIR** (explicación específica del resultado, no la general) · **CORREA/BROOK** (strategy-proofness) · `caso_estudio §3.2` | 3, 4, 5 |
| **X4** | **Consecuencia de aceptar / rechazar** — una línea bajo los botones: *"Si aceptas: tu cupo en {colegio} queda asegurado…"* / *"Si rechazas: renuncias a este cupo. Solo quedas en lista de espera de {preferencias más arriba}; si no se libera un cupo ahí, puedes quedar sin colegio"* (regla real: lista de espera solo hacia preferencias superiores). | `SeguimientoPage.jsx` tarjeta "¿Aceptas esta asignación?" (~340) | **HAX G16** (consecuencia **antes** de la acción, no después) · **GOV.UK** | 3 |
| **X5** | **Detalle por preferencia con los datos que explican el %** — al desplegar cada colegio se agregan *"El año pasado: N postulantes para X–Y vacantes"* y *"Tu prioridad aquí: …"* (antes solo dirección / jornada / PIE). Va dentro del desplegable → no suma carga en la vista principal. Usa `vacantesDeNivel(col, data.alumno.nivel)`. | `SeguimientoPage.jsx` `seg-pref-item__detail` (~597) | **HAX G2/G11** (de qué está hecha la estimación) · **PAIR** (número con su fundamento) | 3 |
| **X0** | **Fidelidad (de paso):** dos fechas de `SeguimientoPage.jsx` estaban desalineadas con `/proceso` y se corrigieron **en A y en B** — aceptar/rechazar *"hasta el 21 de octubre de 2026"* (era "15 de noviembre") y matrícula *"entre el 9 y el 22 de diciembre de 2026"* (era "antes del 30 de noviembre"). | `SeguimientoPage.jsx` (~342, ~434) | fidelidad — `investigacion_paso_a_paso_sae.md` / `ProcesoPage.jsx` | 3 |

**No implementado (decisión pendiente con la profesora):** X6 — icon array "de cada 100" del "no quedaste" en la 1.ª opción (suma elemento visual, ojo con la carga de texto); X7 — lista de espera concreta con referencia de movimiento (requiere decidir si el prototipo la simula o solo la nombra).

**Para la memoria (observación):** X1–X5 son capa de explicación de la **condición A**. X1 y X4 son *borderline* (fidelidad al proceso / apoyo a una decisión real): si la profesora prefiere que estén también en la condición B, es quitar el `!esControl` de esos dos bloques. No se creó sección del plan; **98 aplicables sin cambio**.

---

### Bloque Y — Módulo de verificación del hermano/a unificado con el del/de la estudiante (2026-09-13)

**Fundamento de origen:** feedback de la profesora guía, relayado por el usuario con una captura de `/postulacion` paso 1. Es un **refinamiento de S22-13 / S4** (postulación familiar en bloque). No proviene de una guía de UX-IA sino de una inconsistencia de patrón detectada por revisión externa: el bloque del/de la estudiante principal (`.post-identidad-verif`) ya mostraba sus datos como un **recap de solo lectura** con un enlace *"Algún dato no está bien — corregir"* que revela los campos editables ("el sistema los trae, la familia los verifica, no los tipea" — Bloque S). El bloque del hermano/a, aunque su título decía *"Verifica los datos del hermano o la hermana"*, mostraba **directo los tres campos editables** (`<input>`/`<select>`) sin ese paso de verificación — mismo dato, dos tratamientos de interacción distintos, lo que la profesora marcó como falta de claridad.

**Cambio:** se extrajo un componente compartido `VerificacionDatosCard({ campos, hint, onCorregir, corregirLabel })` — el recap `<dl>` + botón "corregir" — y se usa **el mismo componente, literalmente**, en ambos bloques: el del/de la estudiante (ahora consumiéndolo, sin cambio de comportamiento) y el del hermano/a (que antes no lo tenía). El bloque del hermano/a gana el ciclo completo: recap de solo lectura (Nombre / RUN / Nivel al que postula) → "corregir" → campos editables → **"Listo, datos verificados"** (nuevo, mismo texto que cierra la edición del/de la estudiante) → vuelve al recap. Estado nuevo `editandoHermano` (arranca en `false`, igual que `editandoIdentidad`, sea que los datos vengan precargados de ClaveÚnica o vacíos).

**Estado de validación:** ✅ `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16) — 2026-09-13. Verificado en navegador con la identidad demo genérica (Sofía Ríos Contreras / hermano Tomás Ríos Contreras) y con el caso Muñoz González: el recap del hermano/a muestra los datos correctos, "corregir" revela los campos, "Listo, datos verificados" vuelve al recap; sin errores de consola; sin desborde horizontal a 375 px.
**Impacto en cifras de la memoria:** ninguno — `asignacion.js`/`simulacionSae.js` intactos; el hermano/a sigue sin entrar en `calcularResultado`. Solo `PostulacionPage.jsx`.

| # | Cambio | Ancla | Mapeo | Cap. memoria |
|---|---|---|---|---|
| **Y1** | Componente `VerificacionDatosCard` (nuevo, local a `PostulacionPage.jsx`): recap `<dl>` de solo lectura + botón "corregir", parametrizado por `campos`/`hint`. | `PostulacionPage.jsx` (~725, antes del componente principal) | consistencia de patrón (feedback profesora guía) | 3 |
| **Y2** | Bloque del/de la estudiante migrado a `VerificacionDatosCard` (mismo comportamiento, sin cambios visibles). | `PostulacionPage.jsx` (~1374) | ídem | 3 |
| **Y3** | Bloque del hermano/a: recap de solo lectura + "corregir" + "Listo, datos verificados" (antes: campos editables directos, sin recap). Estado `editandoHermano`. | `PostulacionPage.jsx` (~786, ~1471) | ídem | 3 |
| **Y4** | **Segunda parte del mismo feedback (2026-09-13):** se agrega al "bloque de hermanos" un hermano/a **ya matriculado/a** en otro colegio — antes invisible como persona: el panel "esto es lo que el sistema ya sabe" solo decía *"Hermano/a matriculado/a en Colegio Los Andes"*, sin decir de quién. Nueva identidad en `CASO_EJEMPLO` (Martina Muñoz González, 3.º medio, Colegio Los Andes — el mismo colegio de `colegios.js casoPrioridades: ['hermano']` para Los Andes). Se muestra con el **mismo** `VerificacionDatosCard`, ahora con `onCorregir` **opcional** (se omite aquí: es un registro del Estado, no algo que la familia declare/corrija) — Nombre / RUN / Nivel actual / **Colegio actual**. | `PostulacionPage.jsx` (`CASO_EJEMPLO` ~39, render ~1483) | consistencia de patrón + identidad concreta del vínculo (feedback profesora guía) | 3 |
| **Y5** | **Corrección el mismo día (2026-09-14):** Y4 quedó mal gateado a `perfilEstudiante.caso === 'munoz-gonzalez'` (solo aparecía con "Cargar familia Muñoz González"). El usuario hizo notar que **es información estándar**, no exclusiva del caso: `PrioridadColegioControl` ya verifica "hermano/a matriculado/a" de solo lectura **para cualquier identidad** (línea `filaDetectada('hermano', …)` fuera del `if (soloDeteccion)`), y `agregar()` siembra `casoPrioridades` del colegio sin mirar `perfil.caso`. Se corrige: nuevo estado `hermanoMatriculado` (objeto `{ nombre, rut, nivel, colegio }` \| `null`), sembrado en **ambos** puntos de entrada — `ingresarConClaveUnica()` desde `IDENTIDAD_CLAVEUNICA_DEMO.hermanoMatriculado` (identidad genérica, nueva: **Valentina Ríos Contreras**, 2.º medio, Colegio Los Andes) y `cargarCasoEjemplo()` desde `CASO_EJEMPLO.hermanaMatriculada` (reestructurado a un solo objeto, antes 4 campos sueltos). El render pasa de `perfilEstudiante.caso === 'munoz-gonzalez' && (…)` a `hermanoMatriculado && (…)`. | `PostulacionPage.jsx` (`IDENTIDAD_CLAVEUNICA_DEMO` ~80, `CASO_EJEMPLO` ~53, estado ~850, `ingresarConClaveUnica`/`cargarCasoEjemplo` ~985/~1016, render ~1526) | ídem — la corrección la pidió el usuario, no una guía | 3 |

**Estado de validación (Y4/Y5):** ✅ `npm run lint` (0/0), `npm run build`, `npm test` (16/16) — 2026-09-14. Verificado en navegador: identidad genérica de ClaveÚnica → tarjeta muestra **Valentina Ríos Contreras / 24.112.789-0 / 2° medio / Colegio Los Andes**; caso Muñoz González cargado → **Martina Muñoz González / 23.987.654-3 / 3° medio / Colegio Los Andes**; ambas sin botón "corregir"; sin errores de consola.

**Para la memoria (observación):** ajuste de consistencia de interacción dentro de la fase F1/pasada de fidelidad; no reabre S22-13 como sección nueva del plan.

**Y6 — Reordenamiento visual del paso 1: dirección editable junto con la identidad + sección "Hermanos y hermanas" con estado por color (2026-09-13).** Nuevo feedback de la profesora guía sobre la pantalla resultante de Y1–Y5: "se ve muy desordenado", "no muestra el colegio anterior siempre", "aclarar si está matriculado o no / si se está procesando" y "poder editar la dirección del/de la estudiante" — más la aclaración de que la postulación en bloque es con **un solo** hermano/a; la hermana ya matriculada NO se mueve de colegio.

Cambios, todos en `PostulacionPage.jsx` + `index.css` (sin tocar `asignacion.js`/`simulacionSae.js`/`colegios.js`):

1. **Dirección fusionada con la identidad.** El bloque `post-direccion-block` vivía **después** de toda la sección de hermanos y del checkbox de apoderado/a — muy lejos del botón "corregir" del recap de identidad, que la familia no relacionaba con poder editar la dirección. Ahora comuna/calle/número quedan **dentro** de la misma rama de edición (`editandoIdentidad`) que RUN/nombre/nivel: un solo "corregir" abre los 4 campos + dirección, un solo "Listo, datos verificados" los cierra todos. Se eliminó el bloque duplicado y su botón repetido.
2. **Sección "Hermanos y hermanas" como sub-bloque propio** (`.post-hermanos-section`, fondo blanco + borde propio + encabezado con ícono 👨‍👩‍👧‍👦), separada visualmente del resto del formulario de vinculación (que es azul, `.post-alumno-block`). Antes las dos cards de hermano vivían sueltas sobre ese mismo azul y se confundían entre sí.
3. **Color + badge por estado**, para que "matriculado" vs. "procesando" sea evidente de un vistazo (antes ambas cards eran del mismo azul): la card del hermano/a **ya matriculado/a** pasa a verde (`.post-hermano-datos--matriculado`, `badge badge--ok` "✓ Ya matriculado/a — no postula este año") y suma una frase explícita: **no es parte de la postulación en bloque, ya tiene colegio y no se mueve**. La card del hermano/a que **postula este año** pasa a naranja (`.post-hermano-datos--postulando`, `badge badge--progress` "🕓 Postulando este año — en trámite").
4. **"Colegio actual" del hermano/a que postula** (dato nuevo, puramente informativo, no entra en `calcularResultado`): antes solo la card del ya matriculado mostraba de dónde venía. Nuevo estado `hermanoColegioActual`, sembrado en `IDENTIDAD_CLAVEUNICA_DEMO.hermano.colegioActual` ("Escuela Básica Los Aromos", identidad genérica → Tomás Ríos Contreras) y en `CASO_EJEMPLO.hermanoColegioActual` ("Escuela Bernardo O'Higgins", caso Muñoz González → Mateo); nombres ficticios verificados contra los 6 colegios de `colegios.js` (sin coincidencias). Se suma como 4.º campo de `VerificacionDatosCard` y, en modo edición, como input de texto libre. Se agregó también al payload de `confirmar()` (`hermano.colegioActual`) y a la línea del comprobante `.txt`, sin afectar `perfilCompleto`.
5. **Etiqueta del checkbox reformulada** — "tiene **otro/a** hermano o hermana que también postula" — para no confundirse con el hermano/a ya matriculado/a de la card de arriba (antes ambigua: "tiene un hermano/a que también postula").
6. **InfoBox genérico de `modoTutorial` retirado** de este punto del flujo por redundancia: repetía casi literalmente lo que ya dice la InfoBox específica "Postulación familiar en bloque" (que se mantiene y se recortó un poco). El resto de los usos de `modoTutorial` en la página no se tocó.

CSS nuevo en `index.css`: `.post-hermano-datos--matriculado` / `--postulando` (modificadores de color sobre `.post-hermano-datos`), `.post-hermanos-section` / `__header` / `__titulo` / `__sep`.

**Estado de validación:** ✅ `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16) — 2026-09-13. Verificación de código exhaustiva (estructura JSX confirmada por `npm run build` sin errores de parseo; sin variables sin uso confirmado por lint) para el flujo de identidad+dirección combinados, ambas identidades demo (genérica y caso Muñoz González) y el nuevo campo "Colegio actual". **No se pudo verificar visualmente en un navegador real dentro de esta sesión** (el entorno del agente no tiene herramienta de navegador/captura disponible); queda pendiente una revisión visual manual a 375px antes de dar por cerrado el feedback de la profesora guía.

**Y7 — Verificación visual a 375px y fix de un bug de layout no capturado por lint/build (2026-09-13, misma tarde, sesión siguiente).** Se abrió el dev server en el navegador (móvil, 375px) con las dos identidades (ClaveÚnica genérica → Valentina/Tomás Ríos Contreras; "Cargar familia Muñoz González" → Martina/Mateo) y se confirmó visualmente todo lo descrito en Y6: sección "Hermanos y hermanas" separada, card verde del ya matriculado/a con la frase "no se mueve de colegio", card naranja del que postula con "Colegio actual", y el ciclo "corregir" abriendo RUN+nombre+nivel+dirección juntos (comuna editada a mano y confirmada en el recap tras cerrar).

Encontró un bug real: la etiqueta del checkbox `check-hermanos` (`.post-hermanos-check`, `display:flex; align-items:center`) tenía el texto suelto directamente como hijos del `<label>`, mezclado con **dos** `<strong>` ("otro/a" y "postula este año"). Con `display:flex` en el label, cada fragmento de texto entre elementos se convierte en su propio ítem flex — la oración se partía en columnas verticales ilegibles en vez de fluir como un párrafo (visible en 375px: "Sofía Muñoz González tiene" / "otro/a" / "hermano o hermana que también" / "postula este año" cada uno en su propia columna). Los otros checkboxes del mismo archivo (`acepta-jornada`, `acepta-proyecto`, en el modal de la brecha C) ya evitan esto envolviendo el texto en un `<span>`; `check-hermanos` no lo hacía — tampoco su versión anterior de Y1-Y5 con un solo `<strong>`, pero ahí el problema no se notaba al haber un solo punto de quiebre.

Fix en `PostulacionPage.jsx`: se envolvió todo el contenido de texto del label en un único `<span>` (mismo patrón que `acepta-jornada`) y se agregó `style={{ alignItems: 'flex-start' }}` al label, porque el texto ahora ocupa 3-4 líneas en mobile y el checkbox se veía descentrado con `align-items: center` (heredado de `.post-hermanos-check`). Re-verificado en navegador tras el fix: el texto fluye normal. `npm run lint` (0/0), `npm run build`, `npm test` (16/16) — sin cambios de lógica. Queda cerrado el pendiente de verificación visual de Y6.

**Para la memoria (observación):** ajuste de UI/UX dentro de la misma fase F1/pasada de fidelidad de Y1–Y5; no crea sección nueva del plan, no toca `asignacion.js`/`simulacionSae.js`, **98 aplicables sin cambio**.

**Y8 — Simplificar el hermano/a que postula (sin "corregir") + unificar "vincular" y "avanzar" en un solo popup de confirmación (2026-09-13, misma tarde).** Tercer round de feedback sobre el mismo paso 1: (a) el ciclo recap+"corregir" que Y3 le agregó al hermano/a que postula (para igualarlo al del/de la estudiante) resultó sobreingeniería para un checkbox que ya declara el hecho — se pidió volver a campos editables directos, sin recap ni segundo clic; (b) la casilla de declaración de apoderado/a **no se toca**, sigue exactamente igual; (c) el botón "Vincular estudiante" + el `InfoBox` de confirmación de nivel + el clic aparte en el botón global "Siguiente →" eran **tres pasos** para una sola decisión — se piden en **un solo botón** que abre un popup de confirmación final y el propio popup avanza al paso 2.

Cambios en `PostulacionPage.jsx`:
1. **Card del hermano/a que postula:** se retiró el ternario `!editandoHermano ? <VerificacionDatosCard onCorregir=.../> : <inputs/>` (Y3) y se dejaron los 4 campos (nombre/RUN/nivel/colegio actual) **siempre editables**, directo bajo el checkbox `check-hermanos`. Se eliminó el estado `editandoHermano` (ya sin uso). La card del hermano/a **ya matriculado/a** (verde, sin "corregir", Y4/Y5) no se tocó — nunca tuvo ciclo de edición.
2. **Nuevo componente `ConfirmarVinculacionModal`** (mismo archivo, mismo patrón `<dialog>`/`.prio-modal*`/ESC/clic-fuera/foco inicial que `AgregarColegioModal`, brecha C): reemplaza el `InfoBox` "Verifica el curso antes de continuar". Muestra el párrafo de confirmación de nivel (igual texto que antes) y, si `postulaHermanos`, un segundo párrafo que nombra al hermano/a y advierte que se van a postular **2 hijos/as sin cupo asegurado todavía** (la postulación del hermano/a se hace por separado). Botón "Sí, esto es correcto — continuar" hace `setAlumnoOk(true); setConfirmandoVinculacion(false); setPaso(2)` **en el mismo clic**; "Revisar de nuevo" solo cierra el modal.
3. **Estado `confirmandoNivel` renombrado a `confirmandoVinculacion`** (mismo propósito); el botón "Vincular estudiante" ahora solo abre el modal (`onClick={() => setConfirmandoVinculacion(true)}`), sin cambios en su `disabled`/`title` (sigue exigiendo apoderado/a, dirección completa y —si aplica— datos del hermano/a). El botón global "Siguiente →" y la función `siguiente()` **no se tocaron** — quedan como red de seguridad, pero el usuario ya no necesita usarlos para el paso 1→2.

**Estado de validación:** ✅ `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16) — 2026-09-13.

**Y9 — Verificación visual (2026-09-13, sesión siguiente, mismo día).** Probado en navegador con las dos identidades (ClaveÚnica genérica y "Cargar familia Muñoz González"): marcar `check-hermanos` muestra directo los 4 campos prellenados (nombre/RUN/nivel/colegio actual) sin ningún link de "corregir"; con la casilla de apoderado/a marcada, "Vincular estudiante" abre el modal con el párrafo de nivel y, solo si `postulaHermanos` está marcado, el segundo párrafo de "2 hijos/as sin cupo asegurado todavía" con el nombre correcto del hermano/a; sin el checkbox marcado, el modal muestra solo el primer párrafo (probado explícitamente, ya que `ingresarConClaveUnica()` deja `postulaHermanos` en `true` por defecto — hubo que destildarlo a mano para cubrir este caso). "Revisar de nuevo" cierra el modal sin avanzar de paso; "Sí, esto es correcto — continuar" deja al usuario directo en el paso 2 en un solo clic, sin tocar el botón global "Siguiente →". Al volver con "← Atrás", la tarjeta resumen "Estudiante vinculado" (con su botón "Editar") sigue funcionando igual que antes. Sin errores de consola. `npm run lint`/`build`/`test` (16/16) reconfirmados tras la verificación. Sin bugs encontrados esta vez — queda cerrado el pendiente de verificación visual de Y8.

**Impacto en cifras de la memoria:** ninguno — `asignacion.js`/`simulacionSae.js`/`colegios.js` intactos. Solo `PostulacionPage.jsx`. No se creó sección nueva del plan; **98 aplicables sin cambio**.

**Y10 — Hermano/a que postula pasa a solo lectura + Domicilio con "Editar" propio + se quita "Siguiente →" del paso 1 (2026-09-13, cuarto round de feedback, mismo día).** La profesora vio una captura del estado resultante de Y8/Y9 (card naranja del hermano/a que postula, 4 campos siempre editables, sin ciclo "corregir", bajo el checkbox) y pidió tres cambios puntuales sobre esa misma pantalla:

1. **La card del hermano/a que postula pasa a ser de SOLO LECTURA, igual que la del ya matriculado/a, y se quita el checkbox.** "No se puedan cambiar los datos, es decir debe ser el mismo módulo y quitemos el checkbox simplemente." Se eliminó el checkbox `check-hermanos` (y el estado `postulaHermanos`/`setPostulaHermanos` completo) y los 4 `<input>`/`<select>` editables (Y8); se reemplazaron por `VerificacionDatosCard` **sin `onCorregir`** (mismo componente que usa el hermano/a ya matriculado/a desde Y4), con los mismos 4 campos (Nombre/RUN/Nivel al que postula/Colegio actual). El bloque se gatea ahora por la **presencia del dato** — nueva constante derivada `hayHermanoPostulante = hermanoNombre.trim().length > 0` — no por un checkbox que el usuario marca; título simplificado a "Hermano/a que también postula" (badge naranja `badge--progress` sin cambios) y el párrafo pasa a "También postula este año, con su propia lista de colegios por separado." (ya no habla de "verificar" porque no hay más edición activa). `postulaHermanos` se reemplazó por `hayHermanoPostulante` en **todos** sus usos: el prop `postulaHermanos` de `ConfirmarVinculacionModal` (sin renombrar el prop del modal), el payload de `confirmar()`, `descargarComprobante()`, las dos tarjetas resumen "Estudiante vinculado" (paso 1 y paso 3, ya no comprueban `hermanoDatosOk` porque el dato ya no es editable) y la condición `disabled`/`title` del botón "Vincular estudiante" (se quitó por completo la cláusula `(postulaHermanos && !hermanoDatosOk)` y su `title` asociado — ya no hay nada que dejar incompleto ahí). Se eliminó también `hermanoDatosOk` (sin uso) y las llamadas `setPostulaHermanos(true)` de `ingresarConClaveUnica()`/`cargarCasoEjemplo()` (ya innecesarias: `hayHermanoPostulante` se deriva solo con sembrar `hermanoNombre`). La card verde del hermano/a **ya matriculado/a** no se tocó.
2. **Domicilio recupera su propio botón "Editar", separado del "corregir" de identidad.** Y6 (misma sesión) había fusionado dirección dentro del mismo `editandoIdentidad` que RUN/nombre/nivel para acercarlos físicamente; la profesora pidió separarlos de nuevo ("el domicilio principal del módulo de Sofía, debe haber un botón que diga editar, solo para ese dato"), pero **sin volver** a la posición original y lejana de Y6 (después de toda la sección de hermanos). `VerificacionDatosCard` de identidad vuelve a tener solo 3 campos (Nombre/RUN/Nivel); justo debajo, dentro del mismo `.post-alumno-block`, un bloque de Domicilio nuevo con su propio ciclo (`editandoDomicilio`, `useState(false)`) reutiliza el **mismo componente** `VerificacionDatosCard` con `corregirLabel="Editar"` para el recap (1 campo: Domicilio) y, al editar, los mismos 3 campos de dirección que ya existían (comuna con datalist, calle, número, con su validación `dirTocada` intacta) + "Listo, datos verificados". El botón "Vincular estudiante" sigue exigiendo `direccionCompleta` sin cambios.
3. **Se quita el botón global "Siguiente →" del paso 1.** Desde Y8, confirmar en `ConfirmarVinculacionModal` ya hace `setAlumnoOk(true); setPaso(2)` en el mismo clic — el botón global era necesario cuando "vincular" y "avanzar" eran dos acciones separadas y ahora sobraba ("mejora el flujo, al parecer se rompe"). Se cambió la condición de render de `{paso < 3 && (...)}` a `{paso === 2 && (...)}`; se simplificaron `disabled`/`title` del botón (ya sin las cláusulas de `paso === 1`, ahora inalcanzables). El paso 3 no depende de este botón (tiene su propio botón "Confirmar postulación" aparte, verificado por grep) — se mantiene igual para 2 → 3.

**Sospecha de "se rompe" (revisión de código, sin navegador disponible en esta sesión):** se revisó `ConfirmarVinculacionModal` y su `onConfirmar` (`setAlumnoOk(true); setConfirmandoVinculacion(false); setPaso(2)`) — las tres actualizaciones de estado se agrupan en el mismo *handler* de React (batching), no hay `useEffect` que asuma que el `<dialog>` sigue montado tras desmontarse (el único `useEffect` del modal es el de foco inicial al montar, no al desmontar) y `confirmandoVinculacion` pasa a `false` en el mismo render que cambia `paso`, así que el `<dialog>` se desmonta limpio junto con el resto del paso 1. No se encontró ninguna señal de ruptura en el código; **queda pendiente confirmarlo en navegador real** (no disponible en el entorno de esta sesión).

`asignacion.js`/`simulacionSae.js`/`colegios.js` intactos; no crea sección nueva del plan, **98 aplicables sin cambio**. `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16) — 2026-09-13.

**Y11 — Verificación visual (2026-09-13, sesión siguiente, mismo día).** Probado en navegador con el caso Muñoz González: la card del hermano/a que postula (Mateo) aparece de solo lectura, sin checkbox, siempre visible (título "Hermano/a que también postula", badge naranja, sin ningún link de "corregir"); "Editar" del Domicilio abre únicamente los 3 campos de dirección (confirmado por DOM: `dir-comuna` presente, `alum-run`/`alum-nombre` ausentes); el "corregir" de identidad, por separado, abre únicamente RUN/nombre/nivel (`alum-run` presente, `dir-comuna` ausente) — los dos ciclos quedaron efectivamente independientes. El botón global "Siguiente →" **no aparece** mientras se está en el paso 1 (confirmado ausente en el DOM); al confirmar en el popup, la app aterriza directo en el paso 2 (`onPaso2: true`) sin ningún error en consola — **la sospecha de "se rompe" no se confirmó**, la transición es limpia. Al volver con "← Atrás", la tarjeta resumen "Estudiante vinculado" sigue mostrando correctamente "En bloque con Mateo Muñoz González", y "Siguiente →" sigue sin aparecer en el paso 1. Sin bugs encontrados — queda cerrado el pendiente de verificación visual de Y10.

**Y12 — Rediseño visual de `ConfirmarVinculacionModal`: más llamativo, más contraste, menos texto, más grande (2026-09-13, quinto round de feedback, mismo día).** El usuario vio el popup resultante de Y8/Y10 y pidió mejorarlo: "más llamativo y simple, mejorar el contraste y menos texto y más grande". Cambios en `PostulacionPage.jsx` (solo este componente; `AgregarColegioModal`/`PrioridadModal`, que comparten la base `.prio-modal`, no se tocaron) + `index.css`:

1. **Contenido reestructurado** — de dos párrafos largos a un resumen escaneable: ícono grande (🎒) + título más corto y directo ("¿Confirmas estos datos?" en vez de "Confirma antes de continuar"); nombre del/de la estudiante destacado + nivel como chip/badge azul ("Postula a 4° básico"); el aviso de "curso incorrecto" pasa de oración dentro de un párrafo a un bloque de una línea con ⚠️, fondo ámbar y borde naranja (callout, no texto corrido); el aviso del hermano/a (antes un párrafo de 3 líneas con "vas a postular a 2 hijos/as sin cupo asegurado... se hace por separado, con su propia lista de colegios") se acorta a una frase con ícono 👨‍👩‍👧‍👦 en una tarjeta azul clara.
2. **Botones apilados a ancho completo y más grandes** (`btn--grande`, ya existía en el proyecto): "Sí, esto es correcto — continuar" → "Sí, continuar"; el secundario "Revisar de nuevo" gana un borde de 2px en `var(--gris-med)` (antes `var(--borde)`, un gris muy claro que se perdía contra el fondo blanco del modal — ese era el problema de contraste).
3. **Fondo oscurecido detrás del modal** (hallazgo de la verificación, no pedido explícitamente pero cubre "más llamativo"/"contraste"): el `<dialog>` se abre con el atributo `open` en vez de `.showModal()`, así que el pseudo-elemento `::backdrop` (ya definido en `.prio-modal::backdrop`, `rgba(0,0,0,0.5)`) nunca se pinta — el popup no oscurecía la página de atrás. Se agregó `background: rgba(20,24,33,.55)` con el selector específico `.prio-modal.confirmar-modal` para no afectar a los otros dos modales que comparten `.prio-modal` (mismo mecanismo, mismo bug, pero fuera de alcance de este pedido).

CSS nuevo: `.confirmar-modal__content/__close/__icono/__titulo/__alumno/__nivel/__aviso/__hermano/__acciones/__btn-secundario` (todo scoped a este modal, no reutiliza `.prio-modal__header/__titulo/__dl`). `asignacion.js`/`simulacionSae.js`/`colegios.js` intactos; no crea sección nueva del plan, 98 aplicables sin cambio. `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16). Verificado en navegador a 375px con el caso Muñoz González: contenido correcto con y sin párrafo de hermano, clic fuera del modal cierra (con el nuevo fondo oscuro, el área de "fuera" ya se distingue visualmente), "Sí, continuar" avanza a paso 2 sin errores de consola.

**Y13 — Se quita el callout "Revisa el curso: postular a uno incorrecto..." del popup (2026-09-13, mismo día, sexto ajuste).** El usuario pidió sacarlo: "no es necesario ese mensaje en esta etapa ya que en teoría el sistema lo encuentra automáticamente" — desde que el paso 1 pasó a modo verificación (Bloque S, `S22-12`), el nivel ya no lo tipea la familia: viene precargado de ClaveÚnica/`/perfil` y se corrige (si hace falta) en el "corregir" de identidad, no en este popup final. El aviso de "error más frecuente" tenía sentido cuando el nivel era un campo que la familia llenaba a mano; en el flujo de verificación actual es redundante. Se eliminó el `<p className="confirmar-modal__aviso">` del JSX y la regla `.confirmar-modal__aviso` (sin más usos) de `index.css`. El popup ahora va directo de ícono+título+chip de nivel al bloque del hermano/a (si aplica) y los botones — aún más corto. `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16); verificado en navegador a 375px.

**Y14 — Microcopy del bloque del hermano/a en el popup (2026-09-13, mismo día, séptimo ajuste).** "postula aparte, sin cupo asegurado todavía" sonaba inseguro/negativo para un mensaje de confirmación. Se reemplazó por **"con su propia postulación"** — más corto, sin el matiz de riesgo ("sin cupo asegurado") que ya se explica en detalle en otras partes del flujo (el resumen de bloque, la explicación de resultado); aquí solo hace falta confirmar que el sistema sabe que también postula. Frase completa: *"También confirmas a {hermanoNombre} ({hermanoNivel}), con su propia postulación."* `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16); verificado en navegador.

## Bloque Z — Reestructuración del paso 2 en dos vistas: catálogo (lista) y "Tu lista" (2026-09-13)

**Z1 — Feedback de la profesora guía sobre el paso 2 ("Agrega y ordena tus colegios"):** el grid de 2 columnas mezclaba en una sola pantalla el catálogo de colegios disponibles y la lista ya ordenada de preferencia, con "quitar" solo accesible desde la tarjeta del catálogo (no desde la lista de orden) y el aviso de mayor riesgo ("Postula solo si necesitas cambiar de colegio") como lo primero que se veía, antes de elegir ningún colegio. Cambios en `PostulacionPage.jsx` (paso 2) + `index.css`, sin tocar `asignacion.js`/`simulacionSae.js`/`colegios.js` ni el bloque de prioridades/tutorial (que sigue exactamente en su lugar, antes de las vistas):

1. **Nuevo estado `vistaColegios` (`'catalogo'` | `'mia'`, default `'catalogo'`)** controla cuál de las dos vistas se muestra.
2. **Vista "catálogo":** el grid de 2 columnas `.post-school-card` se reemplaza por una **lista de una columna** (`.post-colegio-lista` / `.post-colegio-fila`, nuevas clases) — filas con nombre/comuna/chip de demanda a la izquierda y una sola acción a la derecha. Los colegios ya agregados se muestran atenuados con un check ("✓ Ya en tu lista (N.°X)"), **sin botón de quitar** (quitar se centraliza en la vista "Tu lista"); los no agregados muestran "+ Agregar" (abre el mismo `AgregarColegioModal` de siempre, sin cambios internos). Si `lista.length > 0`, un botón "Ver mi lista (N) →" en el header permite saltar a la otra vista sin agregar otro colegio.
3. **Al confirmar `AgregarColegioModal`** (mismas dos aceptaciones de jornada/proyecto educativo, brecha C, sin cambios), además de `agregar(id)` se hace `setVistaColegios('mia')` — agregar un colegio aterriza directo en la vista de la lista ordenada.
4. **Vista "Tu lista":** es el bloque de orden existente (`<ul className="post-order-list">`, drag-and-drop + botones ↑↓ + `PrioridadColegioControl` + `ColegioAnalisis` en modo tutorial, `ResultadoProvisional`, InfoBoxes de progreso/consejo/lista corta), con dos agregados: (a) botón "+ Agregar otro colegio" al inicio que vuelve a `setVistaColegios('catalogo')`; (b) cada `<li>` gana un botón "✕ Quitar" (`.post-item__btn--quitar`, nuevo) junto a ↑↓, llamando a `quitar(id)` ya existente (limpia también las prioridades declaradas del colegio, comportamiento sin cambios). Edge case: si se navega a `'mia'` sin colegios en la lista, se muestra el mensaje "Todavía no agregas ningún colegio" + link de vuelta al catálogo.
5. **El aviso "Postula solo si necesitas cambiar de colegio" (D · fidelidad, S22-15) se mueve al final de la vista "Tu lista"** (antes era lo primero del paso 2, antes de elegir cualquier colegio) — mismo texto, sin cambios de contenido, solo de ubicación.

CSS nuevo (todo en `index.css`, reutiliza tokens existentes — `var(--azul)`, `var(--rojo)`, `var(--gris-borde)`, `.btn`/`.btn--primary`/`.btn--secondary`/`.btn--mini` ya existentes, sin dependencias nuevas): `.post-colegio-lista`, `.post-colegio-fila` (+ `--agregada`, `__info`, `__nombre`, `__meta`, `__estado`), `.post-item__btn--quitar`.

`asignacion.js`/`simulacionSae.js`/`colegios.js` intactos; no crea sección nueva del plan, **98 aplicables sin cambio**. `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16) — 2026-09-13.

**Z2 — Verificación visual + limpieza de CSS muerta (2026-09-13, sesión siguiente, mismo día).** Probado en navegador (375px, caso Muñoz González) los 7 puntos pendientes de Z1: **(a)** vista inicial = catálogo en lista de 6 filas, sin el grid de 2 columnas; **(b)** agregar Colegio Los Andes abre `AgregarColegioModal` y al confirmar aterriza directo en "Tu lista"; **(c)** "+ Agregar otro colegio" vuelve al catálogo, Los Andes se ve atenuado como "✓ Ya en tu lista (N.°1)" sin botón; **(d)** se agregó San Martín, se subió con ↑ (confirmando que drag-and-drop/reordenar sigue intacto) y "✕ Quitar" lo sacó correctamente de la lista; **(e)** el aviso "Postula solo si necesitas cambiar de colegio" aparece justo antes de "← Atrás / Siguiente →", al final de la vista "Tu lista" (confirmado por orden de índices en el texto de la página); **(f)** sin errores de consola en ningún paso; **(g)** sin desborde visible a 375px. Sin bugs encontrados.

De paso, se eliminaron las clases `.post-school-picker`/`.post-school-card*` (grid de 2 columnas, ~110 líneas) de `index.css` — Z1 las había dejado sin uso "por si acaso"; se confirmó por grep en todo `src/` que ningún JSX las referencia, así que se limpiaron junto con su override en el media query de 599px. `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16) tras la limpieza — sin regresiones.

**Z3 — El bloque de prioridades/tutorial pasa a vivir SOLO en "Tu lista", no en ambas vistas (2026-09-13, mismo día, tercer ajuste).** El usuario vio una captura y notó que el bloque "¿Cómo funciona este paso?" + "Prioridades que puede tener tu familia en un colegio" (chip row, nota SEP, "¿En qué orden se revisan las prioridades?", "¿Dónde tienes esa prioridad?") se repetía en **ambas** vistas — Z1 lo había dejado sin condicionar a `vistaColegios`, renderizándose siempre antes del `if` de vista. El usuario pidió sacarlo de ahí, con el razonamiento de que "el flujo debe empezar con la lista vacía": en la vista catálogo, con `lista` vacía, no hay ningún colegio sobre el cual declarar una prioridad todavía — el texto ni siquiera aplica ahí (habla explícitamente de "cada colegio de tu lista", "debajo de cada colegio de tu lista"). Se movió el bloque completo (sin cambiar una palabra de su contenido) para que viva **únicamente** dentro de `{vistaColegios === 'mia' && lista.length > 0 && (...)}`, justo antes del botón "+ Agregar otro colegio" (mismo orden visual que ya tenía el usuario en su captura). Efecto neto: el catálogo (vista inicial, con la lista siempre vacía al entrar al paso 2) queda más liviano/con menos texto — alineado con el objetivo general de la profesora guía de reducir texto en esta página — y el bloque solo aparece una vez que ya hay al menos un colegio agregado, que es cuando de verdad aplica. `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16); verificado en navegador: el catálogo inicial (lista vacía) no muestra el bloque; al agregar un colegio aparece en "Tu lista", en el orden correcto (prioridades → "+ Agregar otro colegio" → lista ordenada); al volver al catálogo con "+ Agregar otro colegio", el bloque no se repite; sin errores de consola.

**Z4 — Corrección: la vista de entrada es "Tu lista" (vacía), no el catálogo (2026-09-13, mismo día, cuarto ajuste).** El usuario aclaró que Z3 no interpretó bien "el flujo debe empezar con la lista vacía": no se refería a ocultar el bloque de prioridades en el catálogo (eso ya estaba bien resuelto), sino a que la **vista que se muestra primero** al entrar al paso 2 debe ser "Tu lista" — vacía al principio —, no el catálogo. Cambio de una línea: `useState('catalogo')` → `useState('mia')` para `vistaColegios`. Con esto, al entrar al paso 2 se ve directo el estado vacío ya existente ("Todavía no agregas ningún colegio. Ir al catálogo de colegios →", agregado en Z1) en vez del catálogo de 6 filas; el catálogo pasa a ser una vista secundaria a la que se llega desde ese link o desde "+ Agregar otro colegio". Sin cambios adicionales de lógica ni de estilos. `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16); verificado en navegador: al entrar al paso 2 se ve el estado vacío de "Tu lista" primero, "Ir al catálogo de colegios →" lleva al catálogo con los 6 colegios, sin errores de consola.

**Z8 — Reducción de texto (tarea 1) + subida de fuente base (tarea 2) + bloque de prioridades colapsable (tarea 5) (2026-09-13, mismo día).** Ejecuta las tareas 1, 2 y 5 de `docs/planificacion/carga_de_texto_flujo_postulacion.md` §6.4 (plan aprobado por el usuario tras el feedback de la profesora guía sobre carga de texto y tamaño de fuente; persona objetivo con alfabetización digital básica-intermedia, PISA/PIAAC). La tarea 4 del plan (consolidar más InfoBoxes de "Tu lista" en el paso 2) **se pospone**: tras revisar el código, esa vista ya había bajado de las 6-8 cajas diagnosticadas originalmente a 2-4 condicionales (varias mutuamente excluyentes por rango de `lista.length`), así que no se justificaba tocarla más en esta pasada. La tarea 6 (gatear el bloque de prioridades con el toggle Tutorial ON/OFF, decidir el default) **sigue pendiente de decisión del usuario**, no se tocó.

1. **Tarea 1 — oración de 65 palabras partida.** `PostulacionPage.jsx`, paso 1, `InfoBox` "Antes de empezar: cómo se decide tu resultado": el párrafo único que enumeraba las 5 prioridades en una sola oración larga se reemplazó por una oración corta + una lista `<ul className="cond-detectadas">` de 5 viñetas (reutiliza la clase que ya usa `CondicionesDetectadas` en el mismo archivo, sin CSS nuevo) + una oración de cierre corta sobre el sorteo. Sin cambios de contenido/significado, solo de formato.
2. **Tarea 2 — sube el tamaño de fuente base del sitio, no solo el modo "Grande".** El mecanismo de "Grande" ya subía `document.documentElement.style.fontSize` solo cuando `textoGrande` era `true` (`App.jsx`, `GlobalFontSize`); en modo Normal quedaba en el default del navegador (16px), y la mayoría de las personas nunca activa el toggle. Cambiado a `textoGrande ? '20px' : '17px'` (antes `'18px' : ''`) — ahora el modo Normal también sube (16→17px de raíz) y el modo Grande sube más (18→20px). Además, `index.css` tenía **dos** declaraciones `body { font-size: 16px; ... }` (la regla base y el reset de Tailwind dentro de `@layer base`) que fijaban un valor en `px` ignorando el `<html>` raíz — el texto plano sin clase propia no escalaba con el toggle. Cambiadas ambas a `font-size: 1rem` para que sí hereden el nuevo tamaño raíz. `.page--texto-grande { font-size: 1.06rem }` no se tocó, sigue aportando el extra de "Grande" sobre la nueva base. Efecto neto: ~17px de raíz en Normal (antes 16), ~21px efectivos en Grande (antes ~19).
3. **Tarea 5 — bloque de prioridades del paso 2 pasa a colapsable.** El bloque que Z3 dejó viviendo solo en "Tu lista" (InfoBox "¿Cómo funciona este paso?" + título "Prioridades que puede tener tu familia en un colegio" + chip row + nota SEP + hasta 3 InfoBox más) se envolvió en `<details className="post-demo"><summary>📋 ¿Cómo funcionan las prioridades acá?</summary>...</details>` — mismo patrón y misma clase que "⚙️ Herramientas de la prueba de usabilidad" (línea ~1454 del mismo archivo) y que la divulgación progresiva de `/proceso`. Llega **colapsado por defecto**; el contenido interno no cambió una palabra. El `Fragment` con los `PrioridadModal` ("?") sigue anidado igual, sin problemas de JSX.

`asignacion.js`/`simulacionSae.js`/`colegios.js` intactos; no crea sección nueva del plan, **98 aplicables sin cambio**. `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16) — 2026-09-13. **Verificación visual de la tarea 2 (la de mayor riesgo, por su alcance site-wide): no se pudo hacer en navegador real en esta sesión** (el entorno de esta sesión no expuso una herramienta de navegador). Se hizo una revisión conservadora por lectura de CSS de los selectores con `white-space: nowrap` combinados con contenedores de ancho fijo (candidatos de mayor riesgo: `.stepper__label`, `.demand-chip`, `.badge`, `.btn--mini`) — todos usan `flex: 1`/padding relativo o texto corto sin `width`/`max-width` en px que fuerce recorte, así que el riesgo de overflow por el ~6-11 % de aumento de fuente se estima bajo, pero **queda sin verificar en vivo** y debería confirmarse con `npm run dev` a 375px (Normal y Grande) en `/`, `/postulacion` (pasos 1-3), `/colegio/:id` y `/seguimiento` antes de dar la tarea por cerrada con certeza visual.

**Z9 — Verificación visual de Z8 en navegador real + un bug real encontrado y corregido (2026-09-13, sesión siguiente, mismo día).** Se probó a 375px, en Normal y en Grande (root 17px/20px confirmado con `getComputedStyle`), en `/` (Inicio), `/postulacion` (los 3 pasos, incluida la lista de viñetas nueva de la tarea 1 y el `<details>` colapsable de la tarea 5) y `/colegio?id=1` (ficha completa, incluido el icon-array de probabilidad). Sin overflow horizontal (`scrollWidth > clientWidth` = `false`) en ninguna de esas pantallas, en ningún modo.

**Bug real encontrado en Grande:** en la vista "Tu lista" del paso 2, la fila de cada colegio (`.post-item`) apretaba el nombre ("Colegio Los Andes") contra los botones ↑ / ↓ / ✕ Quitar en la misma línea — con la fuente más grande, el botón "✕ Quitar" (ancho automático según su texto) le dejaba tan poco espacio a `.post-item__body` (flex:1, min-width:0) que el nombre se partía en 3 líneas angostas y feas en vez de que los botones bajaran a su propia fila. Causa: el media query de 599px ya ponía `.post-item__acciones` en `flex-direction: row`, pero sin `flex-basis: 100%` — a diferencia de `.tut-colegio-info`/`.post-prio-colegio`, que sí fuerzan su propia fila completa dentro del mismo `.post-item` (flex-wrap). Fix de una regla: se agregó `flex-basis: 100%; justify-content: flex-end; margin-top: 4px;` a `.post-item__acciones` dentro de ese mismo media query — ahora el nombre ocupa toda la fila y los tres botones quedan alineados a la derecha, en su propia fila, debajo. Verificado visualmente que resuelve el problema por completo (antes/después comparado con screenshot). `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16) tras el fix — sin regresiones. No se encontró ningún otro desborde en el resto de las pantallas revisadas.

**Z10 — La confirmación de jornada/proyecto educativo se mueve de "agregar un colegio" a "confirmar y enviar la postulación" (2026-09-13, mismo día, sexto ajuste).** El usuario probó el flujo y notó que el popup de las dos casillas (brecha C, S22-2, vigente desde 2026-08-27) se repetía una vez **por cada colegio agregado** — con los 6 colegios del catálogo mostrando la misma jornada, era la misma pregunta seis veces sin variación real de contenido entre colegios ("llegamos a la conclusión de que todos los colegios eran lo mismo, entonces estaba de más ese pop up"). Se movió a una única confirmación, para la lista completa, justo antes de enviar:

1. El botón "+ Agregar" del catálogo ya no abre ningún modal — llama directo a `agregar(c.id)` y salta a "Tu lista" (`setVistaColegios('mia')`), igual que antes pero sin la parada intermedia.
2. `AgregarColegioModal` (por colegio, con jornada específica vía `jornadaDeColegio`) se reemplazó por **`ConfirmarEnvioModal`** (`lista`, `colegiosById`), con las mismas dos casillas pero en plural genérico — "Acepto la jornada de cada colegio de mi lista" / "...el proyecto educativo y el reglamento interno de cada colegio de mi lista" — y un contador ("aplican a los N colegios de tu lista"). Se eliminó `jornadaDeColegio` (quedó sin otros usos) y el estado `colegioPendiente` (reemplazado por `confirmandoEnvio`).
3. El botón "Confirmar y enviar postulación" (paso 3) ahora abre `ConfirmarEnvioModal` en vez de llamar a `confirmar()` directo; el botón "Sí, confirmar y enviar" del modal (deshabilitado hasta marcar ambas casillas) es el que efectivamente llama a `confirmar()`.

`asignacion.js`/`simulacionSae.js`/`colegios.js` intactos; no crea sección nueva del plan, 98 aplicables sin cambio (sigue siendo la misma brecha C, solo cambia el momento). `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16). Verificado en navegador con el caso Muñoz González: agregar 2 colegios (Los Andes, San Martín) no abre ningún popup; en el paso 3, "Confirmar y enviar postulación" abre el modal con el texto genérico correcto ("aplican a los 2 colegios de tu lista"); el botón de confirmar queda deshabilitado hasta marcar ambas casillas; al marcarlas y confirmar, la postulación se envía correctamente ("Postulación enviada"); sin errores de consola.

**Z11 — `ConfirmarEnvioModal` sin checkboxes + "Postulación enviada" pasa a ser su propia vista (2026-09-13, mismo día, séptimo ajuste).** El usuario probó Z10 y pidió dos ajustes más sobre el mismo modal y lo que viene después:

1. **Sin checkboxes.** Las dos casillas ("Acepto la jornada...", "Acepto adherir al proyecto...") pasan a ser texto informativo — una lista de 2 viñetas bajo "Al enviar, aceptas que se aplican a los N colegios de tu lista:" (reutiliza `.cond-detectadas`, sin CSS nuevo) — sin nada que marcar ni gate que cumplir. El botón "Sí, confirmar y enviar" (antes deshabilitado hasta marcar ambas) pasa a decir simplemente **"Siguiente"**, para no repetir la palabra "confirmar" tres veces en la misma pantalla (título del modal, este botón y el botón de más abajo que lo abre). Se corrigió también un detalle de concordancia: con 1 solo colegio dice "tu único colegio" en vez de "1 colegios".
2. **"Postulación enviada" pasa a ser su propia vista**, no un bloque que se agrega al final de la misma pantalla de revisión. El usuario notó que después de confirmar, el resumen completo (tarjeta del/de la estudiante, condición SEP, `ResultadoProvisional`, la lista de colegios completa con probabilidades, "¿Cuándo sabrás el resultado?", "¿Y si no quedo en ninguna opción?") seguía visible arriba del bloque de confirmación — se sentía mezclado. El paso 3 ahora es `{paso === 3 && (!confirmado ? <Card>...(todo el resumen editable + el botón que abre el modal)...</Card> : <Card>...(solo la confirmación)...</Card>)}`: dos `<Card>` distintas según `confirmado`, con su propio `CardTitle` ("Paso 3 de 3 — Revisa y confirma" vs. "✅ Postulación enviada"). De paso se limpiaron los `{!confirmado && (...)}` que envolvían los enlaces "Editar" dentro de la rama de revisión — ya eran redundantes ahí, siempre son `true` en esa rama.

`asignacion.js`/`simulacionSae.js`/`colegios.js` intactos; no crea sección nueva del plan, 98 aplicables sin cambio. `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16). Verificado en navegador con el caso Muñoz González (1 colegio): el modal ya no tiene casillas, dice "tu único colegio" (singular correcto), el botón dice "Siguiente"; al confirmar, la vista pasa a mostrar solo "Postulación enviada" (comprobante, descarga, avisos, enlace a "Ver mi resultado ahora"), sin la lista/resumen de arriba — confirmado por ausencia de "Tu lista de colegios:" en el texto de la página; sin errores de consola (un 500 transitorio de HMR durante el guardado del archivo se resolvió solo al recargar, `npm run build` confirmó que no era un error real de sintaxis).

**Z12 — `PrioridadColegioControl` unificado en un solo diseño simple: texto + "Editar" (2026-09-13, mismo día, octavo ajuste).** El usuario, dentro del mismo objetivo de reducir texto, pidió simplificar el bloque "Prioridad en {colegio}" de cada colegio de "Tu lista": antes tenía **dos diseños distintos** según si el caso de ejemplo estaba cargado (`soloDeteccion`) — uno mostraba las prioridades ya resueltas con un "¿Algo no calza? Corregir" escondido en un `<details>`; el otro (el de la captura, identidad genérica) mostraba siempre la fila de hermano/a **más** una oración invitando a declarar funcionario/a o exalumno/a **más** los dos chips ya abiertos. Se unificaron en un solo diseño: un texto breve con la prioridad detectada (o "Sin vínculo con {colegio}. Tu cupo depende de la demanda y las vacantes de este colegio.") y un botón mini **"Editar"** (`<details>`/`<summary>`, sin JS nuevo) que revela los chips de funcionario/a y exalumno/a — por si el sistema se equivocó o falta declarar un vínculo. Hermano/a sigue siendo de solo lectura (lo verifica el sistema), nunca aparece en el `<details>`. Se eliminó el prop `soloDeteccion` (ya no se usa en el componente ni en su invocación) y la clase CSS `.post-prio-colegio--deteccion` (azul vs. naranja), que quedó sin uso — todas las cards quedan con el mismo estilo naranja. `asignacion.js`/`simulacionSae.js`/`colegios.js` intactos; no crea sección nueva del plan, 98 aplicables sin cambio. `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16). Verificado en navegador con ambas identidades (genérica y caso Muñoz González): San Martín (sin vínculo) muestra el texto genérico + Editar revela los 2 chips; Los Andes (con hermano/a) muestra "✓ Hermano/a matriculado/a — verificado..." + Editar (sin exponer el hermano/a como editable); mismo comportamiento en las dos identidades — la distinción `soloDeteccion` no aportaba nada visualmente distinto que valiera la pena mantener.

**Z13 — Consolidación de InfoBoxes al final de "Tu lista" — la Tarea 4 pospuesta en Z8 (2026-09-13, mismo día, noveno ajuste).** El usuario confirmó que el flujo funciona bien pero pidió reducir el texto de esta sección específica: con 2 colegios (ambos de alta demanda) se apilaban **5 cajas** al final de "Tu lista" — "¿Qué hace el orden de tu lista?" (3 párrafos), "Tienes N colegios..." (progreso), "Consejo: ordena por tu preferencia real", "Tu lista es corta y toda de alta demanda", y "Postula solo si necesitas cambiar de colegio" (3 párrafos) — con contenido que se repetía (el "consejo de orden" decía lo mismo que ya dice la primera oración de "¿Qué hace el orden de tu lista?"; "progreso" y "lista corta" podían mostrarse juntas hablando ambas de sumar colegios). Era la Tarea 4 del plan de `carga_de_texto_flujo_postulacion.md` §6.4 que Z8 había pospuesto por parecer ya resuelta — al verla en vivo con una lista real, no lo estaba.

Cambios en `PostulacionPage.jsx`, solo texto/estructura (sin tocar `asignacion.js`/`simulacionSae.js`/`colegios.js` ni `listaCortaYAlta`):

1. **3 InfoBoxes → 1**, mutuamente excluyentes por estado: `listaCortaYAlta` (alerta, recorta el llamado a agregar colegios de demanda media/baja) → si no, `lista.length < 6` con `modoTutorial` (info, "más opciones = más probabilidades") → si no, `lista.length >= 6` (éxito). Se eliminó el consejo de "ordena por tu preferencia real" como caja aparte — ya lo dice `ResultadoProvisional` justo arriba ("Por eso conviene poner primero el que más quieres").
2. **`ResultadoProvisional`** ("¿Qué hace el orden de tu lista?"): párrafo 1 recortado (se quitó "según las prioridades que fija la ley y las vacantes de cada colegio", ya cubierto por el bloque de prioridades); párrafo del disclaimer recortado ("Es una simulación con datos del año pasado..." pierde la cláusula "con las mismas reglas y las postulaciones reales de este año" y comprime "muchas familias creen tener más posibilidades de las que después resultan" a "muchas familias tienen menos posibilidades de las que creían"). El párrafo central (el desenlace específico de la lista actual) no se tocó — es información que cambia según el estado, no texto de relleno.
3. **"Postula solo si necesitas cambiar de colegio"**: 3 párrafos → 2, fusionando la oración de apertura con la consecuencia de quedar asignado/a en un colegio nuevo; la excepción del colegio que no continúa el nivel se mantiene como párrafo aparte, sin recortar el contenido.

Ningún hecho se eliminó, solo se fusionaron oraciones y se quitaron repeticiones entre cajas. `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16). Verificado en navegador con el caso Muñoz González (San Martín + Los Andes, ambos alta demanda): la sección pasó de 5 cajas a 3, sin errores de consola.

**Z14 — "+ Agregar otro colegio" se mueve al final de la lista, antes de la advertencia (2026-09-13, mismo día, décimo ajuste).** El botón vivía al principio de la vista "Tu lista" (justo después del `<details>` de prioridades, antes de ver la lista misma). Se movió después de la lista ordenada, `ResultadoProvisional` y la caja de progreso/lista-corta consolidada en Z13, y justo antes de "Postula solo si necesitas cambiar de colegio" — primer intento. `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16); verificado en navegador con el caso Muñoz González.

**Z15 — Corrección: el botón va pegado a la lista misma, antes de "¿Qué hace el orden de tu lista?" (2026-09-13, mismo día, undécimo ajuste).** El usuario aclaró que Z14 no quedó donde quería: pidió el botón **antes de** "¿Qué hace el orden de tu lista?" (`ResultadoProvisional`), no después de esa caja ni de la de progreso/lista-corta. Se movió una vez más: ahora queda inmediatamente después de `</ul>` (la lista de colegios) y antes de `<ResultadoProvisional>`. Orden final de la vista "Tu lista": lista de colegios → **"+ Agregar otro colegio"** → "¿Qué hace el orden de tu lista?" → caja de progreso/lista-corta → "Postula solo si necesitas cambiar de colegio". Cambio de solo posición en el JSX (se eliminó la copia de Z14 y se agregó una nueva antes de `ResultadoProvisional`), mismo botón y `onClick`. `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16); verificado en navegador con el caso Muñoz González — orden confirmado por índice de texto: lista → botón → "¿Qué hace el orden de tu lista?" → "Tu lista es corta..." → advertencia final; sin errores de consola.

**Z16 — Segundo salto de tamaño de fuente base, más perceptible (2026-09-14, mismo bloque de feedback, duodécimo ajuste).** El salto del Bloque Z8 (16→17px normal, 18→20px root en Grande) resultó demasiado sutil — el usuario pidió subir la fuente de nuevo, esta vez de forma que se note. `App.jsx` (`GlobalFontSize`): `document.documentElement.style.fontSize = textoGrande ? '23px' : '19px'` (antes `'20px' : '17px'`) — mantiene la diferencia de ~4px entre modos, pero con una base bastante más grande. `.page--texto-grande { font-size: 1.06rem }` (index.css) no se tocó, sigue sumando su extra sobre esta base nueva. `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16). Verificación visual más exhaustiva que en Z9 dado el salto más grande: `/` (Inicio), `/postulacion` paso 2 vista "Tu lista" (el punto donde Z9 había encontrado y corregido un desborde real) y `/colegio?id=1`, en Normal (19px) y Grande (23px) — sin overflow horizontal en ninguna; el fix de Z9 (`flex-basis: 100%` en `.post-item__acciones`) sigue aguantando sin problema al tamaño mayor (nombre del colegio a ancho completo, botones ↑/↓/✕ Quitar en su propia fila); los badges/chips de la ficha de colegio (demanda, SIMCE) pasan a apilarse verticalmente en vez de en línea, sin recortarse. Sin errores de consola.

**Z17 — Reducción de texto en el paso 1: "Antes de empezar" colapsable + "Esto es lo que el sistema ya sabe" recortada (2026-09-14, mismo bloque de feedback, décimo tercer ajuste).** El usuario pidió reducir el texto de la parte inicial del paso 1 (recién llegado con Clave�única): el InfoBox "Antes de empezar: cómo se decide tu resultado" (intro + lista de 5 prioridades + 2 párrafos más) y el InfoBox "Esto es lo que el sistema ya sabe de tu hijo/a" quedaban ambos siempre expandidos, uno después del otro.

1. **"Antes de empezar: cómo se decide tu resultado" pasa a `<details className="post-demo">`**, colapsado por defecto, con `<summary>⚖️ ¿Cómo se decide tu resultado?</summary>` — mismo patrón de divulgación progresiva que "¿Cómo funcionan las prioridades acá?" (paso 2) y "Herramientas de la prueba de usabilidad". Contenido interno intacto (la lista de 5 prioridades y los 2 párrafos no cambiaron). Es contenido genérico (no depende del/de la estudiante), el mejor candidato para colapsar.
2. **"Esto es lo que el sistema ya sabe de tu hijo/a" (`CondicionesDetectadas`) se mantiene siempre visible** (es personalizado, no genérico) pero se recortó el texto: la intro pasó de dos oraciones ("Con tu ClaveÚnica, el sistema recupera las condiciones que ya están registradas en el Estado. No tienes que volver a declararlas.") a una ("Condiciones ya registradas en el Estado con tu ClaveÚnica — no hace falta declararlas de nuevo."); el cierre perdió la aclaración de prototipo como oración aparte ("En el sistema real, la condición SEP y el PIE llegan ya cargados; aquí se configuran en Mis datos solo para la simulación.") y quedó como paréntesis breve ("¿Algo no calza? Revísalo en Mis datos (ahí se configura para esta simulación)."). Los `<li>` condicionales (SEP, PIE, vínculos detectados) no se tocaron — son la información específica del caso, no relleno.

`asignacion.js`/`simulacionSae.js`/`colegios.js` intactos; no crea sección nueva del plan, 98 aplicables sin cambio. `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16). Verificado en navegador: el `<details>` llega colapsado y se expande correctamente al hacer clic; el texto de "Esto es lo que el sistema ya sabe" quedó notablemente más corto; sin errores de consola.

**Z18 — Botones ↑/↓/Quitar en fila también en escritorio, para aprovechar el espacio (2026-09-14, mismo bloque de feedback, décimo cuarto ajuste).** El usuario vio una captura de escritorio del paso 2: `.post-item__acciones` usaba `flex-direction: column` por defecto (solo pasaba a `row` bajo los 599px del media query mobile), así que en pantallas anchas el nombre del colegio quedaba a la izquierda con un vacío grande en el medio y los 3 botones apilados en una columna angosta a la derecha. Se preguntó si convenía "estirar" el módulo para aprovechar el espacio; se acotó a un ajuste de bajo riesgo (no un rediseño de la tarjeta completa): `.post-item__acciones` pasa a `flex-direction: row` **siempre** (antes columna por defecto) — en escritorio los botones quedan en la misma fila que el nombre, usando el ancho disponible; el media query de 599px (con su `flex-basis: 100%`, agregado en Z9 para evitar que el nombre se apretara) sigue bajándolos a su propia fila completa en mobile, sin cambios ahí. Un solo archivo (`index.css`), sin tocar `PostulacionPage.jsx`. `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16). Verificado con `getComputedStyle`/`getBoundingClientRect` en dos anchos (1024px y 375px, ya que la herramienta de captura de pantalla falló repetidamente en esta sesión por un problema del panel, no del sitio): a 1024px los botones comparten la fila del nombre (`sameRow: true`); a 375px siguen en su propia fila completa (`ownRow: true`), sin overflow horizontal en ninguno de los dos anchos.

**Z19 — `ColegioAnalisis` colapsable en "Tu lista", para ver más de un colegio a la vez (2026-09-14, mismo bloque de feedback, décimo quinto ajuste).** Con la fuente ya subida (Z16) y 2+ colegios en la lista, el bloque `ColegioAnalisis` (Opción N / barra de `%` / nombre / demanda / vacantes / prioridad) ocupaba tanto alto que apenas se veía un colegio y medio sin hacer scroll; el usuario preguntó si convenía "aplastar las cartas" y, tras pedir primero un commit del trabajo pendiente (ver commit `19a2a36`), aprobó aplicar el cambio para revisarlo antes de decidir. El `<div className="tut-colegio-info">` pasa a `<details className="tut-colegio-info">` con la cabecera ("Opción N" + badge `%`) dentro de un `<summary>`; colapsado por defecto muestra solo "Opción N · X% estimado", y al expandir aparece el resto (nombre, `ProbabilidadVisual`, demanda/vacantes/prioridad) sin quitar contenido. CSS nuevo mínimo: `.tut-colegio-info > summary { cursor: pointer; }`. `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16). Verificado con `getBoundingClientRect` (altura cerrada ~78px vs. abierta ~272px, cerrado por defecto, toggle funcional) dado que la herramienta de captura seguía sin funcionar. El usuario aprobó el resultado ("me gustó, creo que los cambios de la profe lograron ser aplicados").

**Z20 — `/seguimiento`: la tarjeta de resultado pasa de tarjeta ilustrada a texto plano (2026-09-14, mismo bloque de feedback, décimo sexto ajuste).** El usuario pidió simplificar la tarjeta principal de `SeguimientoPage.jsx` (`.seg-result-hero`: ilustración del colegio + insignias de color + fondo degradado azul + 4 datos en cajas). Tras varias rondas de aclaración sobre qué elemento exactamente "sobraba" (inicialmente se interpretó como `ResultadoProvisional` del paso 2, luego se creyó que la captura mostraba lo que había que *conservar*), el usuario confirmó explícitamente que la tarjeta ilustrada de la captura era la que "sobraba", y eligió (`AskUserQuestion`) la opción **"Simplificar todo a texto plano"**. Se reemplazó `.seg-result-hero` por `.seg-result-plano`: párrafo de estado + preferencia, `<h2>` con el nombre del colegio, dirección, una `<ul>` con Probabilidad/Prioridad/Demanda/Comprobante como pares texto-valor, y el mismo párrafo final de "Prioridad aplicada / Fecha postulación" — mismos datos y mismas condiciones (`!esControl` sigue ocultando la probabilidad en modo control), sin ilustración (`SchoolIllustration`, que se sigue usando en `ColegioPage.jsx`/`InicioPage.jsx`, se retira de este archivo) ni colores llamativos. CSS: se elimina el bloque `.seg-result-hero`/`.seg-hero-*` completo (base + las líneas específicas dentro del media query de `/seguimiento`, sin tocar las reglas vecinas `.seg-timeline__connector`/`.seg-pref-item*`/`.seg-actions-bar`) y se agrega CSS mínimo para `.seg-result-plano`. `asignacion.js`/`simulacionSae.js`/`colegios.js` intactos — el resultado de asignación no cambia, solo su presentación. `npm run lint` (0/0), `npm run build` (limpio), `npm test` (16/16). Verificado en navegador end-to-end con el caso Muñoz González (San Martín 1.º no queda, 26% → Los Andes 2.ª preferencia, 99%, hermano/a): la tarjeta de texto plano muestra estado/nombre/dirección/los 4 datos correctamente, sin errores de consola nuevos ni overflow horizontal introducido por el cambio (hay un desborde preexistente y no relacionado en el `topbar` a 1024px, fuera de alcance). **Dejado sin commitear para revisión del usuario**, igual que Z19.

---

## 5. Pendientes propuestos por las guías (aún NO implementados)

De `investigacion_ux_guide_ai_systems.md` sec. 8 y observaciones sec. 2–sec. 3. (El gap de datos que figuraba aquí como **P6** se implementó el 2026-08-26 — ver Bloque G; la auditoría de microcopy que figuraba como **P3** y **P4** se implementó el 2026-08-26 — ver Bloque H; la versión visual del formato de frecuencia —**P1**— y el encuadre del algoritmo en la primera pantalla —**P2**— se implementaron el 2026-08-26 — ver Bloque I; el lenguaje antropomórfico del `AlgoSimuladorPasos` —**P1** en la última numeración— se implementó el 2026-08-26 — ver Bloque J.) Estos son el insumo del "repaso punto a punto" para cerrar el desarrollo antes de la prueba.

Los pendientes **P3–P6** no provienen de las guías de UX-IA sino de la tabla de brechas de fidelidad (sec. 2) de `analisis_video_paso_a_paso_sae.md` (2026-08-27): son brechas del flujo oficial. La brecha **B** (dirección de residencia), que figuraba aquí como **P3**, se implementó en su versión acotada el **2026-09-01** — ver Bloque M (M1); la brecha **E** (reenvío + comprobante actualizado), que figuraba como **P4**, quedó **parcialmente** cubierta ese mismo día (aviso de texto — M3; falta la invalidación efectiva del comprobante) y se mantiene renumerada como **P3**. La brecha **H** (vista mapa de establecimientos) queda **fuera de alcance** (mobile-first, catálogo de 6 colegios) y no se registra como pendiente. El roadmap fase 2 consolidado (**R1–R6**) vive en `comparativa_flujo_postulacion_v_final.md` sec. 6 — P4/P5/P6 de abajo se solapan con R3 (filtros = brecha I) y R6 (IPA = brecha G).

| # | Pendiente | Fundamento | Prioridad sugerida | Bloquea la prueba |
|---|---|---|---|---|
| **P1** | Evaluar mostrar **N-mejores alternativas** ("si no quedas aquí, tus siguientes opciones más probables son…") | PAIR-ET (patrón de alternativas) | Baja | No |
| **P2** | `PostulacionPage.jsx` (~813), InfoBox "Postulación familiar en bloque" (S22-13): "el sistema **intenta** dejarlos en el mismo colegio". Bajo lectura estricta NN/g podría reencuadrarse en términos de proceso ("se procura dejarlos juntos según cupos y prioridades"). Detectado en la auditoría del Bloque J; "intenta" es defendible como proceso (puede no lograrse por cupos), por eso queda como pendiente menor y no como corrección inmediata. Fuera de `/algoritmo` pero **dentro del flujo**. Nota: `ProcesoPage.jsx` (~145) tiene el mismo "intenta" pero está fuera del flujo; `NotasPage.jsx` (~99, ~108) usa "el sistema sigue buscando" de forma **deliberada** (lenguaje tranquilizador citado de la literatura) — no es defecto, no se registra | NNG-XAI (antropomorfismo, lectura estricta) · sec. 5 | Baja | No |
| **P3** (parcial) | **Brecha E — invalidación efectiva del comprobante al modificar:** el Bloque M (M3) agregó el **aviso de texto** de que cada cambio obliga a reenviar y que el comprobante anterior deja de valer. **Falta la parte de estado:** invalidar `comprobanteDescargado` al editar tras confirmar y rehacer el flujo de envío (requiere manejo de estado). Diferido a fase 2 | Fidelidad — `analisis_video_paso_a_paso_sae.md` sec. 2 (brecha E, **Deseable-alto**; parte de texto ya cubierta por M3) | Media | No |
| **P4** | **Brecha F — obligación de postular sin continuidad de nivel:** el flujo real la enuncia como obligación explícita. El Bloque L (D) la cubre **parcialmente** (párrafo 3 de la InfoBox de alerta del paso 2); falta evaluar si merece tratamiento propio (p. ej. detección por el nivel del estudiante) | Fidelidad — `analisis_video_paso_a_paso_sae.md` sec. 2 (brecha F, **Deseable**, parcialmente cubierta por D) | Baja | No |
| **P5** | **Brecha G — IPA (identificador provisorio) como alternativa al RUT** en la identificación, para postulantes sin RUT (= R6 del roadmap fase 2) | Fidelidad — `analisis_video_paso_a_paso_sae.md` sec. 2 (brecha G, **Deseable menor** para un prototipo pedagógico) | Baja | No |
| **P6** | **Brecha I — filtros de búsqueda de establecimientos:** jornada, mixto / un solo género, internado, técnico-profesional, dependencia (público / particular subvencionado), continuidad hasta enseñanza media. Varios ya existen como campos del colegio (`jornada`, `dependencia`, `orientacion`) pero no como filtros en `InicioPage` (= R3 del roadmap fase 2) | Fidelidad — `analisis_video_paso_a_paso_sae.md` sec. 2 (brecha I, **Deseable**, encontrabilidad S5) | Baja | No |

---

## 6. Decisiones abiertas del usuario

1. **¿El paso de resultado inmediato entra como S23 en `plan_mejora_sae.md`?** Mientras no se defina, queda como "extensión fuera de la matriz" y la cifra 87/87 de la memoria no se toca (Bloque E4).
2. **Cambio de foco de la memoria** (`plan_cambio_foco_postulacion.md`, 2026-08-08, no ejecutado): ¿se ajusta el subtítulo de la tesis hacia "flujo de postulación"? ¿Se reescriben los objetivos específicos 4 y 5 con la mención explícita? El objetivo general vigente sigue siendo el amplio.
3. **Citar HAX / PAIR-ET directamente en el cap. 2**, o dejarlos solo como respaldo de diseño en el cap. 3 (pregunta que quedó para la reunión con el profesor guía).
4. **Alta exigencia académica** (`caso_estudio…` sec. 4): ¿se aborda o queda fuera de alcance? Hoy ningún colegio del prototipo tiene esa modalidad.
5. **Comité de ética UTFSM:** ¿se requiere para la prueba aunque los datos sean ficticios?
6. **Variantes de grupo** en la prueba: ¿un solo grupo de 8 o subgrupos? (recomendación en el caso de estudio: un solo grupo).
7. **Sincronización doc↔código de la prueba de usabilidad (pendiente, trabajo del `writing-agent` / decisión del usuario).** Tras el Bloque K (2026-08-27), tres documentos de la prueba asumen que la condición de estudiante prioritario/a (SEP) se marca **dentro del flujo** de postulación, cuando ahora se configura en `/perfil` y en el flujo aparece como información de solo lectura: `caso_estudio_prueba_usabilidad_postulacion.md`, `material_prueba_usabilidad_postulacion.pdf` y `guion_moderador_prueba_usabilidad.md` (+ su PDF). **El caso Muñoz González en sí no cambia** (la familia sigue siendo prioritaria y con los mismos vínculos por colegio); solo cambia dónde se configura ese dato en el prototipo. Requiere actualizar las instrucciones al participante y, eventualmente, el guion del moderador (paso previo: dejar el perfil ya cargado o pedir configurarlo en `/perfil` antes de la tarea). **Actualización 2026-08-27 (Bloque L):** el flujo ganó además dos *gates* nuevos que el guion de moderador y el material de participantes deberían reflejar — la casilla obligatoria "declaro ser apoderado/a legal" en el paso 1 (A) y el modal de dos aceptaciones (jornada + PEI/reglamento) al agregar cada colegio en el paso 2 (C). Ninguno cambia el caso Muñoz González, pero sí añaden pasos que el participante debe ejecutar para completar la tarea. **Actualización 2026-09-01 (Bloque M):** el paso 1 ganó además el bloque obligatorio de **dirección de residencia** (región + comuna + calle y número) y el registro (`RegistroPage.jsx`) ahora exige marcar **dos casillas de aceptación** antes de "Registrarme". El guion de moderador y el material de participantes deberían reflejar estos campos adicionales del paso 1 y del registro. Ninguno cambia el caso Muñoz González.

8. **Brecha B — dirección de residencia del estudiante. RESUELTA (2026-09-01, Bloque M).** El usuario optó por la **versión acotada**: capturar región + comuna + calle y número con fidelidad de formulario, **sin** usar el dato en la lógica de asignación (en Aceptación Diferida la distancia no prioriza; la nota en pantalla lo dice explícito). Implementada en M1. El uso de la dirección para proximidad real queda como **roadmap fase 2 (R5** de `comparativa_flujo_postulacion_v_final.md`), no como pendiente activo.

9. **Gaps de modelo que tocarían el núcleo protegido (`asignacion.js` + tabla `probAsignacion`). Requieren OK explícito del usuario.** El Bloque N (2026-09-02) los dejó documentados y con paliativos de UI, sin tocar la lógica:
   - **¿El PIE entra en el cálculo?** Hoy es dato a nivel de estudiante (`/perfil` → `pie`), se **muestra** en el panel de condiciones detectadas y en el flujo con la aclaración de que **no cambia el porcentaje**. Cerrarlo (`caso_estudio…` sec. 5/9: PIE como cuota antes del nivel 1, solo en colegios con `nee.programa`) implica tocar `nivelPrioridadEnColegio`/`calcularResultado` y añadir un tramo a `probAsignacion`. Mientras tanto, Sofía entra por su cuota **SEP**, no por PIE.
   - **¿La postulación de bloque hace una asignación real para el hermano/a?** Hoy se captura (`hermano: {…}`) pero `calcularResultado` corre solo para el estudiante principal.
   - ~~¿Recalibrar `probAsignacion`?~~ / ~~¿Argumentar de dónde salen los porcentajes?~~ → **RESUELTO por el Bloque R (2026-09-03):** se reemplazó la tabla entera por una **simulación DA-por-colegio + Monte Carlo** (`simulacionSae.js`). Los `%` ya no se eligen a mano: salen de simular la competencia real por los cupos, con datos reales (`vacantes`, `postulantesAnterior`) y parámetros de población documentados. Queda para el writing-agent redactar esto en el Cap. 3.

10. **Reorientación de la prueba a diseño comparativo (reunión profesora guía, 2026-09-03).** Ver **sec. 7** (roadmap en 3 fases: F1 caso y orden canónico → F2 explicatividad del algoritmo → F3 modo control). Reencuadra varios puntos de esta sección: el nº 1 (S23) y el nº 6 (variantes de grupo) se subordinan al nuevo diseño entre-sujetos N≈30; el nº 5 (comité de ética) gana peso. Decisiones abiertas concretas listadas en sec. 7.3.

---

## 7. Reorientación del estudio a diseño comparativo — roadmap (reunión con la profesora guía, 2026-09-03)

**Estado:** `— sin código (planificación)`. Ninguna de las tres fases está implementada; esta sección existe para no perder de vista el rumbo acordado. Se cierra cada fase moviéndola a un Bloque de la sec. 4 cuando se ejecute.

### 7.1 Qué cambió de rumbo

En la reunión, la profesora guía sugirió **reorientar la prueba de usabilidad**: de un estudio **formativo de final abierto** (el/la participante elige el orden de los colegios; se observa su estrategia y comprensión, N≈8) a un **estudio comparativo controlado**:

- **Orden de postulación fijo**, entregado al participante. Acota el espacio de opciones, elimina el confound "a fulano le tocó una lista más fácil que a mengano" y hace que **todos vean el mismo resultado** → se mide limpiamente "¿esta UX me ayudó a entender *este* resultado?".
- **Dos condiciones:** (A) UX que **explica** el algoritmo — el prototipo actual, construido sobre las guías de `investigacion_ux_guide_ai_systems.md` (HAX / PAIR-ET / NNG-XAI / BROOK / RISK-NUM); (B) UX que **no** lo explica — un proxy fiel del SAE / vitrina real (`investigacion_vitrina_sae.md`).
- **Objetivo reafirmado:** el foco es **explicar el algoritmo apoyándose en esas guías ya documentadas**. La condición B es el control que permite atribuir a la explicación las diferencias de comprensión / confianza / percepción de justicia.
- **Diseño entre-sujetos**, N≈30 (≈15 por condición). Dentro-sujetos no sirve: una vez que se entiende el algoritmo no se puede "desentender" para el control. Esto explica el "al menos 30" que pidió la profesora.

### 7.2 Roadmap en tres fases (orden acordado con el usuario, 2026-09-03)

| Fase | Qué | Fundamento / insumo | Entregables | Depende de |
|---|---|---|---|---|
| **F1 — Caso y orden canónico** | Fijar la lista de 6 colegios y su **orden de postulación** que se entrega a todos los participantes. Escenario candidato: **San Martín (el "colegio en mente") 1.º** → no queda → asignación en **Colegio Los Andes (2.ª preferencia, por hermano/a)**. Verificar con la semilla definitiva. Resolver el asunto de `SEED_CASO` / demanda media (hoy Simón Bolívar y Los Quillayes sientan de forma determinista con la semilla actual aunque marquen ~48 % — con el orden fijo esto puede o no importar, hay que decidirlo). Decidir si se entrega como "postulá exactamente estos 6 en este orden" o "estos 6, y ordenás con esta lógica que te doy". | `caso_estudio_prueba_usabilidad_postulacion.md` §3 / §3.1 · `mapa_resultados_caso_munoz_gonzalez.md` · Bloque R (modelo actual) · `investigacion_algoritmo_sae.md §4.2` (strategy-proofness) | `caso_estudio…md`, `mapa_resultados…md`, `guion_moderador…md`, `guion_participante…md` actualizados + PDFs. Confirmación funcional del escenario con la semilla elegida. | — |
| **F2 — Explicatividad del algoritmo en la página** | Repaso **punto a punto** de toda la superficie de explicación del prototipo y su refuerzo según las guías de Google/Microsoft: `AlgoritmoPage.jsx`, `AlgoSimuladorPasos.jsx`, `ResultadoProvisional` (Bloque Q), `ColegioAnalisis`, `SeguimientoPage.generarExplicacion`, el InfoBox de "cómo se decide tu resultado" del paso 1, `ProbabilidadVisual` (Bloque I). Es la **condición de tratamiento**: tiene que estar tan sólida como las guías permitan **antes** de que la comparación signifique algo. | `investigacion_ux_guide_ai_systems.md` completo — HAX **G1** (qué puede/no puede), **G2** (qué tan bien / incertidumbre), **G4** (info contextual), **G11** (por qué este resultado), **G16** (consecuencia de la acción); **PAIR-ET** (calibrar confianza, explicación específica vs. general, visualizar certeza); **NNG-XAI** (ubicación visible, lenguaje accionable, no antropomórfico); **BROOK** (qué NO puede hacer el sistema; video animado); **RISK-NUM** (formato de frecuencia, icon arrays) | Cambios en `sae-react/` con trazabilidad en esta bitácora (Bloque nuevo por cada pieza tocada). `npm run lint`/`build`/`test`. | F1 (para saber qué resultado se está explicando) |
| **F3 — Modo control** | Versión del flujo que **oculta** la capa de explicación del algoritmo (link a `/algoritmo`, `ResultadoProvisional`, la visual "X de cada 100", el "por qué te asignaron este colegio", el InfoBox inicial de "cómo se decide"). Implementación candidata: `?modo=control` o ruta paralela + bandera de sesión. **Anclar el control a la vitrina / SAE real** (`investigacion_vitrina_sae.md`, `analisis_video_paso_a_paso_sae.md`), no a una versión mutilada a propósito. Mecanismo de asignación entre-sujetos (número de participante → condición). | `investigacion_vitrina_sae.md` · `analisis_flujo_postulacion.md` · diseño experimental del Cap. 3 | Modo control en `sae-react/` + doc de metodología comparativa. Plan de implementación revisado por el usuario **antes** de tocar código. | F1, F2 |

### 7.3 Implicancias y decisiones abiertas

- **Cap. 3 (Metodología) — reescritura mayor (writing-agent).** De "prueba de usabilidad formativa N≈8, tareas dirigidas, guion de moderador, Likert 11 ítems" a "estudio comparativo entre-sujetos N≈30, dos condiciones (UX con/sin explicación del algoritmo), tarea de postulación con **lista y orden fijos**, mismo desenlace para todos". Es más grande que el pendiente ya anotado de reescribir la descripción del simulador (Bloque R) — **conviene hacer las dos cosas juntas**. El instrumento (comprensión / confianza / percepción de justicia + pregunta abierta) se conserva pero pasa a ser **comparativo entre condiciones**; el ítem línea base "tómbola" queda como covariable.
- **El mapa de las 720 ordenaciones** (discutido el 2026-09-03, ver `mapa_resultados_caso_munoz_gonzalez.md`) **queda aparcado**: con orden fijo no hace falta enumerar el espacio de resultados. Se retoma solo si se decide dar libertad de orden en alguna variante.
- **Comité de ética UTFSM** (sec. 6 nº 5): un estudio comparativo con N≈30 y reclutamiento por conveniencia refuerza la necesidad de pasar por el comité, aunque los datos sean ficticios.
- **Cuidado 1 — el control no debe ser un espantapájaros.** Si la condición B es una versión deliberadamente pobre, el hallazgo es trivial ("obvio que la que explica ganó"). Debe representar de forma justa el SAE/vitrina que ya se documentó.
- **Cuidado 2 — no perder lo cualitativo.** Dejar 1–2 preguntas abiertas tipo "¿habrías ordenado distinto? ¿por qué?" para conservar la lectura del *falso riesgo estratégico*, aunque ya no sea la medida principal.
- **Decisión de F1** semilla y libertad de orden → **resuelta 2026-09-06, ver 7.5.**
- **Decisión de F3** alcance de lo que oculta el control → **resuelta 2026-09-06 (qué se oculta); pendiente lo técnico, ver 7.5.**

### 7.4 Lectura del usuario para la reunión (registrada)

"No conforme con el estado actual, pero avanzando hacia algo que me gusta." Defendible: el prototipo pasó de un `%` inventado en una tabla (`archivo/CLAUDE_v2.md §3`, sin calibración) a un `%` derivado de datos reales + un mecanismo documentado con limitaciones explícitas (Bloque R); y la prueba pasa de observación formativa a un diseño que **puede contrastar la hipótesis central** de la tesis.

### 7.5 Decisiones del autor sobre F1 y F3 (2026-09-06)

Resuelven las decisiones abiertas listadas en 7.3.

**D1 — Orden de la lista: fijo estricto.** A cada participante se le entrega la lista con los **6 colegios en un orden exacto**, sin margen para reordenar ("postulá exactamente estos 6 en este orden"). No es "estos 6, ordenás con esta lógica".

**D2 — Semilla: `SEED_CASO` único, no varía por participante.** El orden sí importa (decide en cuál colegio se cae al recorrer la lista), pero el sorteo es el mismo para todos → **todos ven el mismo desenlace determinista**. No se implementa semilla-por-participante.

**D3 — Resultado canónico: San Martín 1.º → asignación en Colegio Los Andes (2.ª preferencia).** El "colegio en mente" va primero; no queda ahí (26 %) y la familia cae a Los Andes por el vínculo de hermano/a (99 %). Se eligió sobre la alternativa "colegio con vínculo 1.º → queda en su 1.ª preferencia" porque el contraste entre la condición que explica el algoritmo y la que no **solo significa algo si hay algo no obvio que explicar**: "no obtuviste tu primera opción, y este es el porqué". Conserva la lectura del *falso riesgo estratégico* como observación cualitativa (7.3, Cuidado 2).

**Orden canónico F1 (verificado contra el código real, `calcularResultado` + `SEED_CASO = 20260903`, 4.º básico, familia sin SEP):**

| pos | colegio | vínculo | demanda | `%` | estado en el recorrido |
|---|---|---|---|---|---|
| 1 | Colegio San Martín | — | alta | **26** | prioridad_insuficiente (no queda) |
| 2 | **Colegio Los Andes** | hermano/a | alta | **99** | **asignado** — preferencia N.º 2, nivel 1 |
| 3 | Escuela República de Chile | exalumno/a | baja | 99 | no evaluado |
| 4 | Colegio Villa del Sol | funcionario/a | alta | 99 | no evaluado |
| 5 | Liceo Técnico Simón Bolívar | — | media | 50 | no evaluado |
| 6 | Escuela Básica Los Quillayes | — | media | 46 | no evaluado |

`sinAsignacionEnPreferencias: false`. Coincide con el test `paso 3 (escenario clave)` de `flujo-postulacion.test.js` (14/14 verde). Las posiciones 3–6 se dejaron en el orden "resto de vínculos primero, luego los sin vínculo de demanda media"; como Los Andes queda 2.º, el desenlace no depende de ese tramo.

**D4 — Modo control (F3): oculta toda la capa de explicabilidad, lo que diferencia el prototipo de la vitrina/SAE actual.** Concretamente, se ocultan: el enlace a `/algoritmo` y el simulador paso a paso (`AlgoSimuladorPasos`), `ResultadoProvisional` ("con este orden, ¿dónde quedarías?", Bloque Q), la visual "X de cada 100" (`ProbabilidadVisual`, Bloque I), la explicación contextualizada "por qué te asignaron este colegio" de `SeguimientoPage.generarExplicacion`, el InfoBox inicial "cómo se decide tu resultado" (paso 1) y los textos de `ColegioAnalisis` que fundamentan el `%` (demanda, postulantes/vacantes). Queda un proxy fiel del flujo real: buscar, armar la lista (en el orden fijo dado), confirmar, descargar comprobante, y ver el resultado **a secas** (colegio asignado, sin el porqué). Debe representar de forma justa la vitrina ya documentada (7.3, Cuidado 1), no una versión mutilada.

**Pendiente técnico de F3 (no bloquea F1):** `?modo=control` vs. ruta paralela vs. bandera de sesión; y el mecanismo de asignación de condición en terreno (número de participante → A/B). Se decide al planificar F3, con revisión del autor antes de tocar código.

**Estado:** F1 desbloqueada. Sus entregables (`caso_estudio…md`, `mapa_resultados…md`, `guion_moderador…md`, `guion_participante…md` + PDFs, y el Cap. 3 vía writing-agent) se re-sincronizan con esta especificación y con los números del Bloque R.

### 7.6 Avance de F1 — el caso listo para revisión de la profesora guía (2026-09-09)

Se dejó `caso_estudio_prueba_usabilidad_postulacion.md` en estado presentable para que la profesora guía apruebe el **diseño** del estudio (paso previo al testing). Cambios de esta fecha, solo en `docs/`:

- **`caso_estudio…md`:** caja "Estado del documento" al inicio (qué está cerrado / qué falta para ejecutar); **§8.4 Hipótesis** explícitas (H1 comprensión A>B principal; H2 confianza A>B; H3 justicia — diferencia menor o nula, exploratoria; H4 mito estratégico, cualitativa); **§8.5 Plan de análisis** (descriptivo + U de Mann-Whitney por constructo + tamaños de efecto r; estratificación por covariables en vez de ANCOVA por el N; codificación temática con doble codificación de una submuestra; criterio de "resultado positivo" = ventaja consistente en las 3 fuentes); **§10 Amenazas a la validez** consolidadas (interna: moderador no ciego, ítems que presuponen info de A, un solo caso/desenlace; externa: muestra por conveniencia, prototipo con simulador didáctico, laboratorio; constructo; estadísticas con N≈15/grupo). §9 firma las decisiones antes abiertas: **alta exigencia académica → fuera de alcance**; **paso de resultado inmediato → herramienta de la prueba, no S23**; **ética → acción del autor con la Dirección de la carrera, no bloquea la aprobación del diseño**; **F3 bloquea la ejecución, no la aprobación**. Números verificados contra el código actual (16/16 tests; canónico San Martín 1.º 26 % → Los Andes 2.ª 99 %).
- **`guion_moderador_prueba_usabilidad.md`:** reescrito de la versión formativa de una sola condición a **A/B**. Lista de 6 colegios entregada en **orden fijo** (antes "Tarea 3: ordena la lista como creas que conviene"); 8 tareas alineadas con §6.2; pre-tarea suma el registro de experiencia previa con el SAE; duración 45–55 min; instrucción de asignar la condición antes de la sesión y no revelar que hay dos versiones.
- **`guion_participante_prueba_postulacion.md` v3:** tabla con los 6 colegios en el orden canónico + "ingrésalos así, no hace falta que los cambies" (antes v2 pedía "ordénala como prefieras"). Ficha de familia sin SEP/PIE. Nota para el moderador: si intenta reordenar, recordárselo y anotarlo.
- **`mapa_resultados_caso_munoz_gonzalez.md`:** nota de que la enumeración de recorridos es material de respaldo; el escenario vigente es el canónico de orden fijo.

**PDF regenerados el 2026-09-09** (`caso_estudio…pdf`, `guion_moderador…pdf`, `guion_participante…pdf`, `material_prueba_usabilidad_postulacion.pdf`) — no con pandoc/LaTeX (no hay toolchain) sino con `marked` + Chrome headless; script en el scratchpad de la sesión. Se creó además `material_prueba_usabilidad_postulacion.md` como fuente regenerable del cuadernillo del participante (consentimiento + pre-tarea + tarjeta de familia + lista fija + cuestionario post-tarea), reemplazando el material de agosto que asumía familia SEP+PIE.

**§3.2 nueva en `caso_estudio…md` (2026-09-09):** "Por qué no queda en el Colegio San Martín: el argumento del algoritmo y su traducción para el usuario". (A) argumento técnico — fila única de San Martín por grupos de prioridad legal + sorteo propio (`investigacion_algoritmo_sae.md` §3.2); la familia en el último grupo; cuenta ilustrativa cupos − postulantes con prioridad = remanente para sin-vínculo → ≈1/5 en un sorteo → Monte Carlo ≈26/100; estabilidad / sin envidia justificada = rechazo justo bajo la Ley de Inclusión; strategy-proofness; garantía de recorrido (cae a Los Andes 2.ª); tabla de contrafactuales. (B) traducción para el usuario — texto de referencia para la explicación de `/seguimiento` en la **condición A** (reemplaza la línea pobre actual del código: *"tenía más postulantes con prioridad mayor o no había vacantes"*); versión de una línea; el dato crudo al lado (HAX G2). (C) mapeo redacción→guía (BROOK "más familias que cupos" primero; HAX G11 "por ley"; HAX G5/G6 sin juicio a la familia; PAIR-ET "el sorteo vale solo para ese colegio"; NNG-XAI lenguaje de proceso; RISK-NUM "26 de cada 100"). (D) es exclusiva de la condición A; ítems C4/C5/J2 + abierta A1 lo miden. El único uso de "sorteo" (el desempate entre familias sin vínculo, enmarcado como trato igual entre iguales) es coherente con el criterio de microcopy del 2026-09-07. **Hecho 2026-09-09 (Bloque U):** el texto B de §3.2 se llevó al copy real de `SeguimientoPage.generarExplicacion`. **Pendiente writing-agent:** Cap. 3 §3.5.

**Pendiente de F1 tras esto:** la reescritura de **`03_metodologia.tex` §3.5** (writing-agent).

### 7.7 F3 — inventario de la capa de explicabilidad (2026-09-09)

> **Implementado el 2026-09-09 — ver Bloque V** (§4). El inventario de abajo es el spec; el estado real por pieza (cableada / MANTENER) vive en `f3_modo_control_inventario.md` §2.

Se levantó **`docs/planificacion/f3_modo_control_inventario.md`**: identifica **todos** los componentes/piezas que aportan explicabilidad y define, para cada uno, qué hace la condición **B** — **OCULTAR** (explicabilidad pura, sin equivalente real: `/algoritmo` + `AlgoSimuladorPasos`, `ResultadoProvisional`, `ProbabilidadVisual`, todos los `%` / "de cada 100" del flujo y de `/seguimiento`, `generarExplicacion` — las dos tarjetas, el InfoBox "cómo se decide tu resultado", el "Consejo: ordena por tu preferencia real", el `.probviz-block` de `/colegio`, los enlaces a `/algoritmo`), **SIMPLIFICAR** (dejar el núcleo de fidelidad, quitar la capa pedagógica: `ColegioAnalisis`, `PrioridadModal`, el InfoBox del orden de prioridades, el aviso de lista corta, el detalle por preferencia de `/seguimiento`), o **MANTENER** (fidelidad al SAE real, va en A y B: `CondicionesDetectadas`, prioridad por colegio de solo lectura, gates de aceptación, dirección, comprobante, `/proceso`, calendario, la pantalla de resultado con aceptar/rechazar, la ficha de `/colegio` sin el `.probviz-block`). Invariante: **el resultado de asignación es idéntico en A y B** (mismo `SEED_CASO`, mismo `calcularResultado`); B solo **no muestra** la capa. Enfoque técnico recomendado: contexto `ModoEstudio` (`esControl`) leído de `?modo=control` + flag de sesión, con una mini pantalla `/estudio` para el moderador. La lista de 6 colegios y su orden se entregan **en papel** (D1), no por URL. **Sin código todavía** — es el spec de la fase F3; el autor revisa el inventario y el enfoque antes de implementar.

**F3 hecha (Bloque V, 2026-09-09).** Queda **F2**: endurecer la explicabilidad de la condición A punto por punto.

---

## 8. Protocolo de actualización (para `bitacora-agent`)

1. Se dispara **después** de cualquier cambio a `PostulacionPage.jsx` (flujo) o a la parte de `SeguimientoPage.jsx` que consume el resultado de la postulación, y después de cambios de datos en `colegios.js`/`asignacion.js` que afecten el % estimado o las prioridades.
2. Para cada cambio, agregar fila/entrada en el Bloque correspondiente de sec. 4 con: **ID / código de trazabilidad**, fecha, qué se cambió (1–2 frases), **fundamento de origen**, **mapeo a guía** (sigla del sec. 2 + número o concepto), **ancla** (`archivo` + código `S..-..` o nº de línea aproximado), **capítulo(s) de la memoria**, **estado de validación**.
3. Si el cambio **no** tiene fundamento en una guía o estudio del sec. 2 o en los documentos previos listados, marcarlo `⚠ SIN FUNDAMENTO DOCUMENTADO` y avisar al usuario en el resumen. **No inventar la cita.**
4. Verificar el fundamento contra `investigacion_ux_guide_ai_systems.md` (no citar de memoria): la guía invocada debe decir efectivamente lo que se le atribuye.
5. Si un pendiente de sec. 5 se implementa, moverlo a sec. 4 (con su ID nuevo) y quitarlo de sec. 5. Si aparece un pendiente nuevo derivado de las guías, agregarlo a sec. 5.
6. Si el cambio altera una cifra citada en la memoria (`proyecto-tesis/`), o el estado 87/87, decirlo explícitamente en el resumen — no editar la memoria.
7. Registrar el estado real de `npm run lint` / `npm run build`: `✅` solo si se corrieron y pasaron en esta sesión; si no, `⏳ pendiente` con el motivo.
8. No tocar código, no tocar `proyecto-tesis/`, no tocar otros documentos de `docs/` salvo esta bitácora. Actualizar `CLAUDE.md` / `CONTEXTO_CLAUDE_CODE.md` es responsabilidad del `code-agent` que hizo el cambio, no de este agente.
